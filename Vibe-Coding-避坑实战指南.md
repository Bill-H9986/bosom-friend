# Vibe Coding 避坑实战指南

> 版本：v2.0
> 日期：2026-09-08
> 定位：把「AI 作为协作开发工具」的经验、流程与规矩固化为可执行的工程约束。它同时以 `vibe-coding-guide` 技能形式绑定（见 `.agents/skills/vibe-coding-guide/SKILL.md`），并纳入本仓库开发文档体系。与 `项目铁律.md`、`工程化流程与可追溯体系规范.md` 一致，本文档只写「能落地、可校验」的规则，不写口号。

## 0. 一句话总纲

> 你遇到的所有问题，本质上都是同一个：**把 AI 当成「全自动程序员」用，而不是当成「必须明确约束的协作工具」。**

AI 放大的不是你的技术上限，而是你的「表达与约束能力」：它能高速产出，但不会替你负责；它能高效执行，却理解不了「边界」与「代价」。因此，**Vibe Coding 的效率上限不取决于提示词多精妙，而取决于你前置了多少工程约束。**

---

## 1. 先定角色：AI 是「带量产能力的实习生」，你是「负责人」

| 角色 | 职责 | 红线 |
| --- | --- | --- |
| 你（负责人） | 定目标、给约束、审结果、扛责任 | 不能只丢一句「做个 XX」就不管；不能把 AI 输出当结论直接上线 |
| AI（执行者） | 按约束产出方案与代码、主动暴露风险 | 不能不问边界就下手；不能把「跑通」当「做对」；不能乱借外部依赖 |

**责任边界**：AI 写的代码出了问题，锅是你的，不是 AI 的。所以审查与验证不可外包，「AI 说完成了」永远不等于「可以交付」。

**一条必须遵守的纪律**：先方案、后代码；先对齐、再动手。让 AI 先把「改哪些文件、为什么改、有什么风险、怎么验证」用 markdown 写清楚，你确认后再让它写。禁止在方案未确认前让它一次性铺开大量改动。

---

## 2. 八类常见失控与对策

### 痛点 1：代码失控——改完不知道改了啥

**表现**：AI 一次改十几个文件，跑起来报错，找不到根因。

**对策（每条都是硬要求）**：

1. **小步提交**：一次只让 AI 改一个可独立验证的小功能，改完立即 `git commit`。
2. **先方案后代码**：AI 先输出「改哪些文件、每处为什么改、影响面、验收方式」，你批准后再编码。
3. **原子化变更**：一个任务只做一件事；prompt 里显式写「不要顺便改 XX / 不重构无关代码」。
4. **改动可回滚**：进入大改动前打 tag 或建分支；每个 commit 都能独立回退。

**本仓库落地**：门禁（qa/run-gate.mjs）要求每个里程碑独立验证明、独立留痕；提交信息遵循 feat/fix/test/chore/docs 前缀。

### 痛点 2：疯狂新建文件、重复逻辑蔓延

**表现**：文件越来越多，同一工具函数写了三遍。

**对策**：

1. **前置目录规范**：项目开始前写好目录结构，告诉 AI「必须遵守，不许随意新建」。
2. **强制复用**：prompt 里明确「优先使用已有 utils / components / 服务，禁止重复造轮子」。
3. **定期去重**：每完成一个模块，让 AI 做一次代码审查，找重复逻辑并合并。
4. **新文件有理由**：AI 建议新建文件时，必须先说明「现有文件为何不能容纳它」。

**本仓库落地**：铁律 #0「能搬则搬、能抄则抄、优先成熟方案」；服务端 server/src/ 已有清晰分层（api.ts / routes-* / store / kernel-client），新增路由与能力必须复用既有模块，不得另起炉灶。

### 痛点 3：上下文污染——越写越偏

**表现**：同一会话一会儿写前端一会儿写后端，输出越来越混乱。

**对策**：

1. **模块化会话**：不同模块 / 任务开新会话，保持上下文纯净。
2. **文档锚定**：维护 PROJECT.md（技术栈 / 目录 / 约定 / 关键决策），每个新会话先喂它。
3. **及时止损**：发现 AI 开始「胡说八道」，立刻重开会话，不要在烂上下文里硬救。
4. **长任务拆解**：超过一个会话才能完成的事，先写清「已完成 / 待办」，新会话据此续作。

### 痛点 4：不验证不测试，bug 越积越多

**表现**：AI 说「完成了」，你直接跑，结果到处是坑。

**对策**：

1. **测试先行**：先写用例，再让 AI 写实现（TDD 思想）。
2. **自动化门禁**：每次 AI 写完，必须跑通 lint + build + test 才能继续。
3. **人工验收**：核心流程必须自己跑一遍；AI 说「完成」不算数。
4. **失败留档**：失败、超时、平台报错必须如实记录并给原因，禁止悄悄跳过或改成 PASS。

**本仓库落地**：质量门禁体系 products/bosom-friend/qa/（qa-all.mjs = S 级前端静态门槛 → qa-guard → qa-guard2 → qa-docsync），以及 S 级前端监考协议 acceptance/EXAM_PROTOCOL.md。红灯不放行、不交付。

### 痛点 5：安全漏洞（注入、XSS、硬编码密钥）

**表现**：登录 / 查询代码存在注入，密钥直接写进代码。

**对策**：

1. **安全规范前置**：PROJECT.md 明确「参数化查询、输入校验与转义、密钥走环境变量、接口做权限校验」。
2. **安全扫描**：Python 用 bandit，JS/TS 用 eslint-plugin-security 等。
3. **关键代码人工审查**：认证、授权、支付、数据操作等核心逻辑必须人工过一遍。

**本仓库落地**：密钥一律走环境变量 / 独立数据根（~/.bosom-friend），禁止硬编码；server/src/security.ts 负责安全加固与备份轮转；真实账号与平台副作用必须显式批准并留档。

### 痛点 6：提示词模糊——做出来的不是你想要的

**表现**：你说「做个登录页」，AI 给了你完全不相干的东西。

**对策**：

1. **结构化提示词**：目标 + 技术栈 + 输入输出 + 样式要求 + 约束条件。
2. **先对齐再动手**：让 AI 先用 markdown 输出实现方案，确认后再写代码。
3. **给参考**：贴一段类似代码或设计，让 AI 对齐风格。

**提示词模板**：

```text
【目标】实现用户登录功能
【技术栈】React + TypeScript + TailwindCSS
【输入】手机号 + 密码
【输出】登录成功跳转首页，失败显示错误提示
【约束】
- 使用已有 api/auth.ts 中的 login 函数
- 表单校验：手机号格式、密码不少于 6 位
- 样式参考 src/components/Register.tsx
- 不要新建文件，在 src/pages/Login.tsx 中修改
【验收】跑通 lint + build + test；给出验证截图
```

### 痛点 7：范围蔓延——需求越堆越多

**表现**：「再加个 XX 功能」说了十几次，项目越来越乱。

**对策**：

1. **维护 TODO 列表**：所有需求写下来，按优先级排序，一次只做一个。
2. **分版本规划**：MVP / v1.0 / v2.0 分开，不要一次塞所有功能。
3. **变更评估**：每个新需求先评估「影响哪些文件、多大工作量」，再决定做不做。
4. **需求冻结**：进入验收 / 发布阶段的范围内，不再临时加功能；新需求另开版本。

### 痛点 8：代码能跑但看不懂——两周后不敢改

**表现**：技术债爆炸，可维护性极差。

**对策**：

1. **规范前置**：ESLint / Prettier / 类型检查，AI 必须遵守。
2. **要求注释**：复杂逻辑必须写注释，说明「做什么、为什么这么做」。
3. **定期重构**：每完成一个大功能，安排一次重构与代码审查。

**本仓库落地**：本仓库 strict: true + noImplicitAny；注释与文档表述按 dsh-prose-standard（直接、具体、不用隐喻、不写推理过程转述）；TODO 用 FIXME/TODO/XXX 分级。

---

## 3. 推荐工作流（从混乱到可控）

```text
┌──────────────────────────────────────────────────────┐
│ 1. 准备阶段                                            │
│    ├─ 写 PROJECT.md（技术栈 / 目录 / 约定 / 安全）          │
│    └─ 初始化 git + lint + test 配置                      │
├──────────────────────────────────────────────────────┤
│ 2. 规划阶段                                            │
│    ├─ 拆解任务，写 TODO 列表，按优先级排序                │
│    └─ 确定本次范围（一件小事）                            │
├──────────────────────────────────────────────────────┤
│ 3. 开发循环（每个任务重复）                               │
│    ├─ a. 写结构化 prompt（目标 + 约束 + 参考）            │
│    ├─ b. 让 AI 先给方案，确认后再写代码                   │
│    ├─ c. AI 写完 → 跑 lint / build / test (门禁)         │
│    ├─ d. 人工验收核心逻辑                                 │
│    └─ e. git commit（小步、可回滚）                       │
├──────────────────────────────────────────────────────┤
│ 4. 收尾阶段                                            │
│    ├─ 整体测试 + 安全扫描 + 门禁                         │
│    ├─ 代码审查 + 重构 + 去重                             │
│    └─ 完善文档、更新 PROJECT.md / 变更日志               │
└──────────────────────────────────────────────────────┘
```

---

## 4. 项目模板（每个项目必写）

### 4.1 PROJECT.md 模板

```markdown
# 项目名称

## 技术栈
- 前端：React 18 + TypeScript + TailwindCSS + Vite
- 后端：Node.js + Express + PostgreSQL
- 工具：ESLint + Prettier + Vitest

## 目录结构
src/
├── components/    # 通用组件
├── pages/         # 页面
├── hooks/         # 自定义 hooks
├── utils/         # 工具函数
├── api/           # API 封装
└── types/         # 类型定义

## 编码规范
- 使用函数组件 + TypeScript，禁止 any
- 组件文件用 PascalCase，工具函数用 camelCase
- 优先使用已有的 utils 和 components，禁止重复造轮子
- 复杂逻辑必须写注释

## 安全要求
- 所有数据库查询必须参数化，禁止字符串拼接
- 用户输入必须校验和转义
- 密钥 / Token 必须走环境变量，禁止硬编码
- API 接口必须做权限校验

## 关键决策记录
- YYYY-MM-DD：选择 XXX 方案，因为 XXX
```

---

## 5. 日常检查清单

### 每次让 AI 写代码前

- [ ] 我有没有说清楚目标（要什么结果）？
- [ ] 有没有指定在哪个文件 / 模块改？
- [ ] 有没有说「优先使用已有代码」？
- [ ] 有没有给参考或示例？
- [ ] 有没有明确约束（不要做什么）？
- [ ] 有没有说清验收方式？

### AI 写完后

- [ ] 跑了 lint 吗？
- [ ] 跑了 build 吗？
- [ ] 跑了 test / 门禁吗？
- [ ] 核心逻辑我自己看了吗？
- [ ] 有验收截图 / 可复现证据吗？
- [ ] git commit 了吗？（小步、信息清晰）

### 每天结束前

- [ ] 今天的代码都提交了吗？
- [ ] 有没有遗留的 bug / 未关闭的失败项？
- [ ] 明天要做什么，写进 TODO 了吗？
- [ ] 关键决策写进 PROJECT.md 了吗？

---

## 6. 核心心法（专业版）

1. **AI 是「带量产能力的实习生」，不是技术总监**：你要给它明确任务、约束与验收标准；不能甩一句「做个 XX」就不管了。
2. **慢就是快**：花 2 分钟写清 prompt、花 3 分钟审代码，远比花 2 小时 debug 划算。
3. **小步快跑**：一次只做一件事，做完验证完再继续；每一步都留痕、可回滚。
4. **文档是锚点**：PROJECT.md + TODO.md + 变更日志是防止 AI 跑偏的最重要工具。
5. **你才是负责人**：AI 出问题锅是你的；所以必须审查、必须验证、必须留证据。
6. **证据高于结论**：每个「已完成 / 已修复 / 已通过」都必须附带截图、结果或可复现步骤；无证据的结论一律视为未完成（对应铁律 #4）。
7. **实事求是**：做不到就说做不到，未配置就说未配置，失败就如实记录原因；任何伪装成功（假文案、假数据、假成功）都是最高优先级违规（对应铁律 #7）。

---

> 记住：Vibe Coding 的效率上限，不取决于提示词多精妙，而取决于你前置了多少工程约束。
