# Bosom Friend 功能清单（三大核心功能版）

> 版本：0.2.40 · 2026-09-10（本轮打包版本）
> 组织方式：**整个项目按「内容创作 / 账号管理 / AI 智能体」三大核心功能划分小功能区**，
> 每个小功能区再拆到可验收的功能点；公共底座单列（三大核心共用，不属于任何单一核心）。
> 状态口径：**✅ 自检覆盖且连续通过** · **🟡 已实现、自检未覆盖或仅人工抽查** · **⛔ 未实现/被外部条件阻塞**。

---

## 0. 总览（功能点计数与执行状态）

| 核心功能 | 小功能区 | 功能点 | ✅ 自检连续通过 | 🟡 已实现待覆盖 | ⛔ 未实现 |
| --- | --- | --- | --- | --- | --- |
| **A 内容创作** | 创作工作台 / 草稿箱 / 素材库 / 发布 / 数据回流 | 34 | 31 | 3 | 0 |
| **B 账号管理** | 频道连接 / 账号库 / 平台目录 / 账号数据 | 20 | 17 | 3 | 0 |
| **C AI 智能体** | 对话与动作 / 我的任务 / 热点互动 / 7×24 接待 / 知识库（含蒸馏） / 全局监控 | 35 | 35 | 0 | 0 |
| **D 公共底座** | 模型配置 / 桌面壳 / 数据与安全 / QA 门禁 / 用户体系 | 19 | 15 | 4 | 0 |
| **合计** | — | **108** | **98** | **10** | **0** |

> 2026-09-10 更新：自检从 26 项扩到 **82 项**（L1 小功能点 67 · L2 小功能区 8 · L3 核心板块 3 · L4 整体集成 4），
> 并连续通过，上表 ✅ 即"被自检覆盖且通过"。
> 同日晚间做 **AI 智能体板块专项测试**：C2-03 中断、C2-04 分享链接由「🟡 已实现」转为「✅ 门禁覆盖」，
> 新增 `qa/verify-agent-task-actions.mjs`（18 项，接入 `run-gate.mjs`）；期间修掉 2 个产品缺陷（见 E2 第 7、8 条）。
> 同日下午做**账号管理专项 debug**：A1–A8 的断言从"页面文案"改成"落库回读 + 真实提示"，
> 新增 A9–A17 共 9 项（含账号数据对账与"平台没给的指标要如实标注"），期间修掉 2 个产品缺陷、
> 2 处自检/审计自身缺陷，以及 2 处"平台没给却说成 0"的诚实化问题（见 E4）。
> 账号管理专项证据：`FULL_CHAIN PASS checks=20 blockers=0`（L1 17 · L2 2 · L3 1）。
> 全量四层证据（干净环境，A16 加入前 80 项）：`LAYER_GATE PASS` + `FULL_CHAIN PASS checks=80 blockers=0`
> （L1 65/65 · L2 8/8 · L3 3/3 · L4 4/4；A 21/21 · B 17/17 · C 25/25 · D 2/2；apiFailures=0、pageErrors=0）。
> A16 加入后的 81 项全量复跑赶上另一会话重启 dev 运行时而中断（`B9` 报 `ERR_CONNECTION_REFUSED`，
> 服务进程 19:37:53 被替换），该轮账号板块仍 18/18 全绿；81 项全量待干净环境复跑。
> 门禁 `run-gate.mjs` 全绿（黄灯两项仍是既有的 AC 覆盖率与 S 级监考）。
> 剩余 🟡 主要是三类：**需要扫码**（小红书/抖音授权、扫码发布）、**需要真实发布额度**（任务重试/取消）、
> **流程性证据**（AC 覆盖率、S 级监考、32 位机器实测）。

## 1. 怎么读

- 编号：`A/B/C/D + 小功能区号 + 功能点号`，例如 `A4-05 真实发布`。
- 每条含四要素：**入口（用户点哪里）· 说明（做什么）· 实现位置（代码在哪）· 状态**。
- 「自检」指 `qa/acceptance/full-chain-loop.mjs` 的 82 项（按三大核心汇总输出）；「门禁」指 `qa/run-gate.mjs` 等脚本。
- 只查一个板块时用 `$env:BF_CHAIN_ONLY='A,L2-B,L3-B'`（id 前缀过滤），层闸门只统计跑过的项。

### 分层测试（层层递进，低层失败即停）

自检脚本按四层执行，**上一层不过就不跑下一层**：

| 层 | 覆盖 | 命令 | 说明 |
| --- | --- | --- | --- |
| **L1 小功能点** | 67 项 | `$env:BF_CHAIN_LAYER=1; node qa/acceptance/full-chain-loop.mjs` | 每个功能点单独验，页面每步重载 |
| **L2 小功能区** | 8 项 | `$env:BF_CHAIN_LAYER=2; node qa/acceptance/full-chain-loop.mjs` | 同一小功能区内的功能串起来跑 |
| **L3 核心板块** | 3 项 | `$env:BF_CHAIN_LAYER=3; node qa/acceptance/full-chain-loop.mjs` | 内容创作 / 账号管理 / AI 智能体各一条主链路 |
| **L4 整体集成** | 4 项 | `$env:BF_CHAIN_LAYER=4; node qa/acceptance/full-chain-loop.mjs` | 跨核心联动：智能体→发布、知识库→生成、账号→创作、设置→对话 |

不带 `BF_CHAIN_LAYER` 时按 L1→L2→L3→L4 顺序全跑，并在每层结束打印闸门结果：

```
--- L1 闸门：67/67 通过 ---
--- L2 闸门：8/8 通过 ---
--- L3 闸门：3/3 通过 ---
--- L4 闸门：4/4 通过 ---
LAYER_GATE PASS（逐层通过）
```

> 安全约定：自检**绝不触发真实发布**——L4-1 会先关闭「跟随模式」，
> 只验证动作卡能打开发布弹窗并预填，不点提交。
- 前端页面与入口：侧栏 7 项（内容创作 / AI互动 / 我的任务 / 发布日历 / 数据中心 / 全局监控 / 知识库）
  ＋ 底部「添加频道」＋「Bosom Friend 客户端 → 设置」。

---

## A. 内容创作（核心一）

用户目标：**一句话生成内容 → 挑好素材 → 一键发到平台 → 看到结果**。

### A1 创作工作台（页面 `#/draft-box` → 页签「创作」）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A1-01 | 提示词输入 | 创作页顶部输入框 | 输入需求；输入 `@` 可引用已上传素材 | `components/draft-box/.../MediaMentionPromptInput` | ✅ 自检 B2 |
| A1-02 | 生成类型切换 | 「生成草稿(视频)」胶囊 | 视频 / 图文两种生成模式 | `AiBatchGenerateBar` | ✅ 自检 B2（视频） |
| A1-03 | 目标平台选择 | 「N 个平台」胶囊 → 弹层 | 勾选目标平台；**与「添加频道」白名单同源**（小红书 / 抖音 / 快手 / 视频号；闲鱼登录已通、发布未接通，只出现在「添加频道」） | `hooks/usePlatformMetadata.ts` `getTaskChannelPlatformInfos` | ✅ 自检 B1 + 门禁 E2E `content-params.spec.ts` |
| A1-04 | 参数限制 | 「参数限制」按钮 | 按所选平台显示标题/正文/话题/图片数量上限 | `InlinePlatformSelector` | ✅ 自检 B9 |
| A1-05 | 模型 / 分辨率 / 风格 / 比例 / 时长 / 数量 | 工具栏 6 个胶囊 | 选择生成模型与视频参数（默认带出设置里的用户模型）；**图片与视频同为 720p/1080p 档位**，成品像素按当前画面比例给出（如 1080p + 3:4 → 1080x1440），生成后按该尺寸裁剪落盘 | `AiBatchGenerateBar/utils/constants.ts` `server/src/image-ratio.ts` | ✅ 自检 B2 + 门禁 E2E `content-params.spec.ts` + 服务端功能测试 |
| A1-06 | 文案要求 | 「文案要求」按钮 | 追加写作约束（语气/字数/禁词） | `CaptionPromptField` | ✅ 自检 B10 |
| A1-07 | 提示词编辑器 | 输入框右侧按钮 | 打开长文本编辑器与模板 | `draftbox-ai-open-prompt-editor-btn` | ✅ 自检 B7 |
| A1-08 | 重置配置 | 「重置配置」按钮 | 清空当前提示词与参数 | `draftbox-ai-reset-btn` | ✅ 自检 B7 |
| A1-09 | 本地素材上传 | 输入框左侧「＋」 | 上传图片/视频/音频作为参考素材；模型不支持参考素材时按钮**保留但置灰 + 说明原因** | `AiBatchGenerateBar/components/ImageStack` | ✅ 自检 B11 / B18（支持参考素材的模型可真上传；纯文生视频模型按钮禁用并给出原因） |
| A1-10 | AI 生成 | 「AI 生成」按钮 | 提交生成任务，实时进度，完成后落到草稿箱 | `POST ai/draft-generation/v2` | ✅ 自检 B2 |
| A1-11 | 生成记录 | 草稿箱右上「生成记录」 | 查看每次生成的任务与结果 | `GET ai/draft-generation` | ✅ 自检 B12 |

### A2 草稿箱（`#/draft-box` → 「全部 / 草稿 / 视频 / 图片」）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A2-01 | 草稿列表 | 页签切换 | 全部/草稿/视频/图片四类视图，含数量 | `DraftListSection` / `AllListSection` | ✅ 自检 B2 / B5 |
| A2-02 | 草稿详情 | 点草稿卡片 | 打开详情弹窗：封面/正文/话题/素材 | `draftbox-detail-dialog` | ✅ 自检 B5 |
| A2-03 | 从详情发布 | 详情内「发布」 | 带草稿内容打开发布弹窗（标题/正文/媒体预填） | `DraftDetailDialog` → `openPublishDialog` | ✅ 自检 B5 |
| A2-04 | 草稿编辑 / 删除 | 详情内按钮 | 修改草稿内容 / 删除草稿 | `PUT material/info/:id` `DELETE material/:id` | ✅ 自检 B13（编辑入口 + 删除二次确认） |
| A2-05 | 批量移动 / 批量移除 | 列表右上 | 进入批量模式，多选后移动分组或删除 | `media/ids` `material/list` | ✅ 自检 B8（进出批量模式） |
| A2-06 | 生成中任务卡 | 列表顶部 | 展示排队/生成中任务与进度 | `draftbox-generating-task-card` | ✅ 自检 B14 |

### A3 素材库（`#/draft-box` → 页签「素材库」）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A3-01 | 素材列表 | 「素材库」页签 | 展示 AI 生成素材与上传素材，按组过滤 | `media/list` | ✅ 自检 B3 |
| A3-02 | 素材分组 | 顶部「默认素材组」 | 按素材组切换；当前只有一个持久化组 | `material/group/*` | 🟡 已实现 |
| A3-03 | 素材移动 / 删除 | 批量模式 | 移动到其它组 / 移除素材 | `POST media/transfer` `DELETE media/ids` | ✅ 自检 B8（入口） |
| A3-04 | 素材预览 | 点素材卡片 | 图片/视频预览 | `MediaPreview` | ✅ 自检 B15 |

### A4 发布

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A4-01 | 工具栏一键发布 | 创作页「一键发布」 | 打开发布弹窗（左侧内嵌创作区，右侧发布表单） | `DraftContentModule.openDirectPublish` | ✅ 自检 B4 |
| A4-02 | 发布弹窗 | `publish-dialog-container` | 账号选择 / 标题 / 正文 / 话题 / 封面 / 文件选择 | `components/PublishDialog` | ✅ 自检 B4 / B5 |
| A4-03 | 账号选择器 | 弹窗顶部头像行 | 选择发布到哪些频道（与账号库同源） | `publish-account-selector` | ✅ 自检 B5 |
| A4-04 | 参数校验汇总 | 弹窗黄色「发现 N 个账号存在问题」 | 逐账号列出错误/警告（如缺媒体、超字数） | `PublishDialog/compoents/ErrorSummary` | ✅ 自检 B20（发布弹窗字段齐备 + 素材/校验提示） |
| A4-05 | 真实发布 | 「发布」按钮 | 提交平台，回写真实作品 ID / 链接；失败写原因 | `POST v2/channels/publish/flows` + 引擎 | ✅ 自检 B6（真实发布拿到作品链接） |
| A4-06 | 定时发布 / 排期 | 发布日历点某天 | 选择时间后进入排期，到点自动发布 | `PATCH publish/tasks/:id/publish-at` | ✅ 自检 E2（入口） |
| A4-07 | 任务操作 | 发布记录 / 任务 | 立即发布 / 重试 / 取消 / 删除 | `publish/tasks/:id/*` | 🟡 已实现 |
| A4-08 | 发布记录 | 数据中心 / 日历 | 记录状态、平台、链接、失败原因 | `GET v2/channels/publish/records` | ✅ 自检 D2 |
| A4-09 | 扫码发布（抖音小程序） | 发布弹窗 | 生成二维码，用户扫码后在 App 内完成发布 | `plat/douyin/miniapp-auth/*` | 🟡 已实现（需真实扫码） |

### A5 数据回流（内容创作的"结果"）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A5-01 | 数据中心 | `#/data-statistics` | 作品数、播放/浏览、点赞、评论、分享、收藏、增长趋势 | `statistics/published-content-summary/dashboard` | ✅ 自检 D2 / E4 |
| A5-02 | 平台作品同步 | 「同步」/ 自动 | 从平台拉取作品与互动数据（分页拉全才允许对账） | `POST v2/channels/accounts/:id/sync` | ✅ 实测（数据中心出现真实作品） |
| A5-03 | 作品排行 / 平台贡献 | 数据中心下半部 | 按作品/平台聚合指标 | 同上 | ✅ 自检 B17（无数据时走空态） |
| A5-04 | 发布日历 | `#/calendar` | 周/月视图展示排期与已发布，含农历/节日 | `components/CalendarTiming` | ✅ 自检 D3 / E2 |

---

## B. 账号管理（核心二）

用户目标：**把平台账号接进来，随时知道它能不能用，粉丝和作品数据自动回来**。

### B1 频道连接

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B1-01 | 添加频道 | 侧栏底部「添加频道」/ 账号页 | 打开频道管理弹框 | `sidebar-account-entry` | ✅ 自检 A1 |
| B1-02 | 可连接平台列表 | 「连接新频道」 | 频道白名单：小红书 / 抖音 / 快手 / 视频号 / 闲鱼，能力由引擎源码扫描得出（登录与发布分开报告） | `ChannelManager/ConnectChannelList` + `server/src/platform-catalog.ts` | ✅ 自检 A5 + 门禁 E2E `content-params.spec.ts` + `qa/probes/verify-channel-login-start.mjs`（三平台二维码实测） |
| B1-03 | 扫码授权 | 平台卡片 | 生成二维码 → 轮询状态 → 落库账号 | `v2/channels/accounts/auth/:platform` | 🟡 需真实扫码 |
| B1-04 | 抖音小程序授权 | 抖音卡片 | 小程序内授权后回填 | `plat/douyin/miniapp-auth/complete` | 🟡 需真实扫码 |
| B1-05 | 取消 / 重试 / 重新授权 | 授权页按钮 | 授权失败可取消、重试、重新发起 | `AuthLoadingPage` | ✅ 自检 A8（授权页取消并返回） |

### B2 账号库

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B2-01 | 我的频道 | 频道管理侧栏 | 平台 + 账号数 + 昵称/头像/状态/粉丝 | `ChannelSidebar` | ✅ 自检 A1 |
| B2-02 | 账号分组 | 「新建分组」/ 分组菜单 | 新建 / 重命名 / 上移下移 / 删除（默认分组不可删） | `v2/channels/account-groups` | ✅ 自检 A3 + A9（改名与排序都做落库回读） |
| B2-03 | 账号归属分组 | 分组卡片 | 按分组展示账号；删组后账号回落默认分组 | 同上 | ✅ 自检 A3 + A13（合成账号进组→删组→回落 grp-default） |
| B2-04 | 刷新粉丝数 | 「刷新全部平台」/ 单账号刷新 | 1 小时限频，登录失效自动跳过 | `platform-sync` + `useFansRefresh` | ✅ 自检 A2（真实提示 + lastStatsTime 前进）+ A10（二次刷新被限频且不再打平台）+ A15（失效账号被跳过并如实提示） |
| B2-05 | 登录态判定 | 账号页徽标 / 频道行 | valid → 「在线/正常」；invalid → **「需重新登录」**（含平台原因） | `AccountStatusView` + `accountCore.tsx` | ✅ 自检 A11（响应改写为 invalid，账号页与频道行都必须显示） |
| B2-06 | 退出登录 / 删除账号 | 账号行按钮 | 退出仅清 cookie；删除级联清该账号记录与统计，**删除前自动快照** | `v2/channels/accounts/:id(/logout)` | ✅ 自检 A7（入口）+ A12（合成账号：未确认被拦且无落盘副作用、退出只清 cookie、确认删除留快照） |
| B2-07 | 账号资料补全 | 自动 | 昵称/头像/粉丝数后台补传，不回写假数据；平台返回占位昵称时回退"平台名 · uid"以便区分同平台多账号 | `enrichAccountProfile` + `cleanPlatformNickname` | ✅ 自检 A6（昵称/粉丝非空）+ 单点实测（`No name` → `抖音 · XchanM520`） |
| B2-08 | 账号页 | `#/accounts` | 我的账号卡片 + 接待**规则配置**（评论/私信待办与记录已合并到「全局监控」，本页不再重复提供执行面） | `app/[lng]/accounts/accountCore.tsx` | ✅ 自检 A4 / C11 |

### B3 平台目录与元数据

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B3-01 | 平台元数据 | 全局 | 平台能力（授权/发布/分析/作品）、内容限制、媒体规则 | `GET v2/channels/platforms` | ✅ 自检 A14（11 个平台 + 抖音/小红书能力声明齐全） |
| B3-02 | 平台图标与名称 | 全站 | 本地静态图标优先，名称按语言本地化 | `store/platformMetadata/*` | ✅ 自检 A14（频道行显示「抖音」而不是平台 id；连接卡片同样） |
| B3-03 | 同步能力声明 | 账号页 / 同步 | 声明各平台支持的作品/粉丝/评论/私信能力 | `GET v2/platform-sync/capabilities` | ✅ 自检 B19（抖音/小红书声明可同步） |

### B4 账号数据

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| B4-01 | 账号作品列表 | 数据中心 / 账号 | 从平台真实拉取作品（分页拉全） | `syncPlatformWorks` | ✅ 自检 A16（账号↔记录↔指标↔数据中心接口四者对账） |
| B4-02 | 按账号筛选 | 数据中心「账号筛选」 | 全部账号 / 单账号切换 | `data-statistics` | ✅ 自检 E4 + L2-B2 |
| B4-03 | 数据同步 | 「同步」按钮 / 自动 | 手动或定时同步作品与互动数据 | `v2/channels/accounts/:id/sync` | ✅ 自检 A16 + 原始接口核对（平台 work_list 的统计字段与落库逐条一致） |
| B4-04 | 作品删除对账 | 自动 | 平台侧删除后数据中心同步消失（仅拉全时执行） | `reconcileRemovedWorks` | 🟡 已实现 |

---

## C. AI 智能体（核心三）

用户目标：**说一句话，智能体自己查数据、生成内容、发布、接待、记账**。

### C1 对话与动作

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C1-01 | AI 助手侧栏 | 右侧「AI 助手」 | 流式对话，随时可用；移动端有入口 | `AIAssistantPanel` | ✅ 自检 C1 |
| C1-02 | 真实模型回复 | 同上 | 走用户自己的模型（BYOK），未配置时明确提示而非假装 | `POST ai/chat` | ✅ 自检 C1（连续多轮非模板回复） |
| C1-03 | 动作卡 | 对话内卡片 | 「去发布」等动作，点击后在前端执行并回传截图 | `ActionCard/executePublishAction` | ✅ 实测（智能体→发布弹窗→发布成功） |
| C1-04 | 跟随模式 | 侧栏开关 | 跟随中=自动执行动作；关闭=只建议 | `store/agentFollow.ts` | ✅ 自检 C2 |
| C1-05 | 新对话 | 侧栏按钮 | 开新会话 | 同上 | ✅ 自检 C3 |
| C1-06 | 附件上传 | 输入框「＋」 | 上传图片/视频作为对话上下文 | `Chat/MediaUpload` | ✅ 自检 C4 |
| C1-07 | 任务评分 / 收藏 / 分享 / 删除 | 任务详情 | 对智能体执行结果评分、收藏、生成分享链接 | `agent/tasks/:id/*` | ✅ 自检 C18（收藏/分享弹窗/评分入口）+ 门禁 `AGENT_TASK_ACTIONS`（收藏、评分落库回读） |

### C2 我的任务（`#/tasks-history`）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C2-01 | 任务列表 | 侧栏「我的任务」 | 按状态/时间列出智能体任务 | `GET agent/tasks` | ✅ 自检 D1 |
| C2-02 | 任务详情 | 点任务 | 展示执行步骤、工具调用与结果 | `agent/tasks/:id/messages` | ✅ 自检 E3 |
| C2-03 | 中断 / 继续 | 详情内 | 运行中可中断，稍后可继续 | `agent/tasks/:id/abort`（回报 `data.interrupted`） | ✅ 门禁 `AGENT_TASK_ACTIONS` 27 项（中断真的停：落库 aborted + 只留已送达的部分正文；**收尾阶段停止不被覆盖回 completed、不再挂动作卡**；同任务可接着跑完） |
| C2-04 | 分享链接 | 详情内 | 生成只读分享页 | `agent/tasks/:id/share` | ✅ 门禁 `AGENT_TASK_ACTIONS`（链接指向应用内只读页、有效期按请求生效并落盘、重启不失效） |

### C3 热点与互动（`#/ai-interaction`）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C3-01 | 热点内容 | 页签「热点内容」 | 实时热榜（微博/抖音/小红书/知乎/B站），分类与来源 | `v2/hot-content/*` | ✅ 自检 E1 |
| C3-02 | 评论搜索 | 页签「评论搜索」 | 关键词搜笔记 → 评论列表 → 热词/情感分析 → 导出 | `v2/statistics/note-comment-search/*` | ✅ 自检 C6 |
| C3-03 | 平台导航 | 顶部平台图标 | 一键跳到各平台搜索页 | `HotContentHomeContent` | ✅ 自检 C7 |

### C4 7×24 全自动接待

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C4-01 | 接待规则 | 账号页「接待规则」 | 触发关键词、匹配方式、排除词、适用账号/平台、回复方式、优先级、启用 | `v2/customer-reception/rules` | ✅ 自检 A4（页面可见规则表） |
| C4-02 | 推荐模板 | 规则页 | 报价咨询/合作洽谈/售后客服/新品促销一键套用 | 同上 | ✅ 自检 C8 |
| C4-03 | 规则测试 | 「测试接待」 | 输入一句话，返回是否命中与回复内容 | `POST v2/customer-reception/test` | ✅ 自检 C9 |
| C4-04 | 待办（评论/私信） | 全局监控「待办处理」区 | 引擎只读采集 → 登记待办 → 前端人工回复（真实平台发送，结果以平台确认为准） | `v2/customer-reception/pending` + `ReceptionMonitor`/`DmReception` | ✅ 实测（监控页有真实扫描数） |
| C4-05 | 自动发送 / 手动确认 | 「7×24 全自动接待」开关 | 开启=前端智能体自动发送；关闭=你在平台侧确认 | `reception-engine.ts` | ✅ 自检 C10（开关状态可切换并回读） |
| C4-06 | ~~接待记录~~（已合并删除） | — | 原「接待记录」页签的唯一写入方 `POST v2/customer-reception/handle` 没有任何调用方，页签恒为空；真实接待结果一直由 C6-04 的 `reception-replies.json` 承载，故本轮删除该页签与路由 | 已移除（原 `v2/customer-reception/logs`） | ✅ 自检 C11 断言旧页签不存在 |
| C4-07 | 引擎状态与轮次 | 全局监控 | 常驻待命、已完成轮次、上次轮询时间 | `v2/customer-reception/status` | ✅ 自检 D4 |
| C4-08 | 平台统一监控 | 全局监控 | 各平台绑定账号数、评论/私信接待状态 | 同上 | ✅ 自检 D4 |

### C5 知识库（`#/knowledge`）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C5-01 | 笔记树 | 左侧栏 | 新建/打开/删除笔记 | `knowledge/tree` `knowledge/notes/*` | ✅ 自检 E5 / D5 |
| C5-02 | Markdown 编辑与预览 | 中间编辑区 | 编辑、保存、预览 | 同上 | ✅ 自检 C12（新建→编辑→保存→回读，用完自动清理） |
| C5-03 | 搜索 / 反向链接 | 左侧 / 右下 | 全文搜索；`[[笔记名]]` 双向链接 | `knowledge/search` `knowledge/backlinks` | ✅ 搜索自检 C13；反向链接自检 C19 |
| C5-04 | 外部库挂载 | 「挂载外部库」 | 指定本地目录作为知识库 | `knowledge/choose-vault` | ✅ 自检 C14 |
| C5-05 | 生成记录沉淀 | 自动 | 每次生成成功写入「自动沉淀/生成记录.md」 | `appendKnowledgeRoutes` | ✅ 自检 C17（自动沉淀/生成记录.md 存在） |
| C5-06 | 知识注入（检索→上下文） | 自动 | 生成/对话前按指令检索笔记，命中即作为「已知知识」进模型上下文 | `knowledge-distill.ts` `buildKnowledgeContext` | ✅ 门禁 `KNOWLEDGE_DISTILL`（模型真的引用到专属事实） |
| C5-07 | 注入预览 | 知识库→蒸馏页签 | 输入一句话，看这次会命中哪些笔记（含被排除的审计笔记） | `POST knowledge/inject-preview` | ✅ 门禁 |
| C5-08 | 蒸馏样本 | 自动 | 每次生成/对话留结构化样本：指令、输出、模型、注入来源、状态、评分 | `recordDistillation` → `knowledge.json.distillations` | ✅ 门禁 |
| C5-09 | 微调数据集导出 | 知识库→蒸馏页签 | 一键导出 JSONL（OpenAI messages 结构 + 元数据），可按任务筛选 | `GET knowledge/distill/export` | ✅ 门禁 |
| C5-10 | 样本评分 | 蒸馏页签 | 1–5 星评分回写，高分样本优先作为少样本示例 | `POST knowledge/distill/rate` | ✅ 门禁 |

### C6 全局监控（`#/monitor`）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C6-01 | 引擎参数 | 折叠区「引擎参数与 7×24 全自动接待」 | 轮询间隔、每账号每轮上限、去重窗口、失败重试冷却 | `POST v2/customer-reception/config` | ✅ 自检 C15 |
| C6-02 | 立即处理待办 | 折叠区内 | 手动触发一次处理 | `POST v2/customer-reception/poll-now` | ✅ 自检 C16 |
| C6-03 | 账号级接待状态 | 折叠区「平台统一监控总览与账号级状态」 | 各账号扫描/待确认/发送失败/风控计数 | `v2/customer-reception/status` | ✅ 自检 D4 |
| C6-04 | **按客户收纳的 AI 回复** | 页面主体 | **评论 / 私信分成两个页签**，各自独立的客户列表；左栏按客户（评论者/私信会话）收纳，右栏显示「对方原文 → AI 实际回复 → 平台回执」，含平台/账号/状态筛选与关键词搜索 | `GET/DELETE v2/customer-reception/replies` + `reception-replies.ts` + `GlobalMonitor.tsx` | ✅ 单测 `RECEPTION_REPLIES_OK checks=7` + 页面实测两页签渲染 |
| C6-05 | 回复记录沉淀 | 自动 | 发起回复即落 `reception-replies.json`（客户键、原文、回复内容）；成功/失败一律由平台互动任务状态核对，超时 10 分钟判失败 | `recordReceptionReply` / `reconcileReceptionReplies` | ✅ 单测同上 |
| C6-06 | **查看原对话** | 客户线程右上「查看原对话」→ 抽屉 | 抽屉内分两段：**平台原对话**（私信读整段会话 / 评论读该作品评论线程，可重复拉取，读不到时如实显示平台原因）与**本地记录**（AI 回复了什么）；平台快照按 `replyId` 缓存到记录上可离线回看 | `POST/GET v2/customer-reception/conversation` + 引擎 op `conversation`（`interactions.py`）+ `attachReceptionConversation` | ✅ 接口实测（真实拉起→轮询→平台回执）；抖音气泡抽取为启发式，需真实会话复核 |

---

## D. 公共底座（三大核心共用）

| 编号 | 功能 | 入口 | 说明 | 实现位置 | 状态 |
| --- | --- | --- | --- | --- | --- |
| D3-03 | 用户体系 | 登录/注册/个人资料 | 邮箱/手机号登录、注册、改密、个人资料、安全信息 | `login/*` `auth/*` `user/*` | 🟡 已实现（开发版免登录） |
| D3-04 | 通知与反馈 | 客户端下拉菜单 | 消息通知、联系我们 | `notification/list` `contact/feedback` | ✅ 自检 D6 |
| D1-01 | AI 模型服务（BYOK，多服务 / 多模型） | 设置 → 自定义大模型 | 「我的模型服务」列表：添加 / 设为当前 / 删除多个服务；每个服务填接口地址 / API Key / 模型名，可存多个模型并点标签切换当前模型；「自动获取可用模型」；图片模型 / 视频模型（选填）；保存后立刻探活 | `SettingsModal/tabs/CustomLlmTab.tsx`（`tabs/customLlm/`：不预建任何默认服务，服务区只放「Agnes AI 国内站」官网按钮；卡片抬头给名称/状态点/编辑/删除，编辑区含 API 密钥与可折叠的「自定义设置」——显示名称 / API 地址 / API 协议 / **模型目录**；目录里每一行 = 模型 id + **用途（对话/图片/视频）** + 显示名称 + 删除，图片与视频模型都在这里标明用途而不再另开配置区；「获取可用模型」弹「选择要添加的模型」勾选清单） | ✅ 设计基线 D03 5/5 + 门禁 `LLM_CONFIG_GATE`、`MULTI_MODEL_GATE` 14/14 + E2E `content-params.spec.ts` |
| D1-02 | 模型配置生效 | 自动 | 写入 DSH `settings.yaml` / `.credentials.yaml`，内核路由随之切换 | `server/src/api.ts writeKernelModelSettings` | ✅ 门禁 4/4 全 PASS |
| D1-03 | 通用设置 | 设置 → 通用 | 主题（浅色/深色/跟随系统）、网站语言 | `GeneralTab` | ✅ 自检 E6 |
| D1-04 | 系统与更新 | 设置 → 系统与更新 | 版本号、更新状态、数据安全（备份位置/完整性） | `OtaTelemetryTab` | ✅ 自检 E6 |
| D2-01 | 桌面启动 | 双击应用 | 端口被占自动接入既有实例；握手失败给出原因 + 重试/完全退出 | `desktop/electron/main.cjs` | ✅ 实测（两种场景截图） |
| D2-02 | 托盘与完全退出 | 托盘菜单 | 关闭窗口最小化到托盘；「完全退出」清理整棵进程树 | 同上 | ✅ 实测 |
| D2-03 | 随包引擎 | 自动 | 便携 Python + 浏览器 + ffmpeg；`pyvenv.cfg` 自动改写 | `build-engine-portable.ps1` | ✅ 实测 |
| D2-04 | 32 位系统提示 | 启动 | 明确提示"需要 64 位 Windows" | `main.cjs` | 🟡 代码路径（无 32 位机器实测） |
| D3-01 | 数据备份 | 自动 | 启动时全量快照，保留最近 30 份；删除账号前另存 `pre-delete-account-*` | `security.ts` `routes-channels.ts` | ✅ 实测 |
| D3-02 | 编码与数据完整性 | 门禁 | UTF-8 无 BOM；编码损坏即红灯 | `qa/check-data-integrity.mjs` | ✅ 门禁绿 |
| D4-01 | 全链路自检 | `node qa/acceptance/full-chain-loop.mjs` | 26 项/轮，覆盖三大核心 + 全部页面 | `full-chain-loop.mjs` | ✅ 连续多轮 0 阻塞 |
| D4-02 | 设计基线回归 | `node qa/acceptance/verify-design-0231.mjs` | 0.2.31 版式 + 平台对齐 | `verify-design-0231.mjs` | ✅ 5/5 PASS |
| D4-03 | 模型配置链路门禁 | `qa/verify-llm-config.mjs` | 保存 → 落盘 → DSH 设置/凭据 → 内核注册 | 同上 | ✅ PASS |
| D4-04 | 多服务多模型门禁 | `qa/acceptance/verify-multi-model.mjs` | 界面添加第二个服务 → 保存 → 切为当前 → 同服务加第二个模型并切换 → 删除；回读 providers[] / activeProviderId / settings.yaml 路由，运行前后自动备份恢复 | `verify-multi-model.mjs` | ✅ 14/14 PASS（截图 `qa/acceptance/multi-model/`） |
| D4-04 | 门禁红绿灯 | `node qa/run-gate.mjs` | 结构/依赖/完整性/AC 覆盖率/缺陷闭环 | `run-gate.mjs` | 🟡 黄灯（AC 覆盖率待补） |
| D4-05 | S 级前端监考 | `qa/acceptance/*.mjs` | 客户视角关键路径独立监考 | `EXAM_PROTOCOL.md` | 🟡 部分执行 |
| D4-06 | 业务域 CRUD 四步矩阵 | `node qa/crud-matrix.mjs` | 五域增/查/改/删 + 落盘 + 空态，34 项 | `crud-matrix.mjs` | ✅ 门禁绿（已接入 `run-gate.mjs`） |
| D4-07 | 知识库蒸馏链路 | `node qa/knowledge-distill.mjs` | 检索→注入→蒸馏→导出，12 项 | `knowledge-distill.mjs` | ✅ 门禁绿（已接入 `run-gate.mjs`） |

---

## E. 执行清单（本轮实测结果）

### E1 全链路自检矩阵（核心链路代表项；完整 67 项见脚本与分层表）

| 自检项 | 归属 | 覆盖功能 | 最近结果 |
| --- | --- | --- | --- |
| A1 频道管理列表 | 账号管理 | B1-01 B2-01 | ✅ 连续通过 |
| A2 刷新粉丝数 | 账号管理 | B2-04 | ✅ 连续通过 |
| A3 新建/删除分组 | 账号管理 | B2-02 B2-03 | ✅ 连续通过（修复误删默认分组后） |
| A4 账号页 | 账号管理 | B2-05 B2-08 | ✅ 连续通过 |
| A5 连接新频道 | 账号管理 | B1-02 B1-05 | ✅ 连续通过 |
| B1 目标平台对齐 | 内容创作 | A1-03 | ✅ 连续通过 |
| B2 AI 生成草稿 | 内容创作 | A1-01 A1-10 A2-01 | ✅ 连续通过 |
| B3 素材库 | 内容创作 | A3-01 | ✅ 连续通过 |
| B4 工具栏一键发布 | 内容创作 | A4-01 A4-02 | ✅ 连续通过 |
| B5 草稿→详情→发布预填 | 内容创作 | A2-02 A2-03 A4-03 | ✅ 连续通过 |
| B6 真实发布 | 内容创作 | A4-05 | ✅ 拿到真实作品链接（当天 3 次发布额度用尽） |
| B7 提示词编辑器/重置 | 内容创作 | A1-07 A1-08 | ✅ 连续通过 |
| B8 批量模式 | 内容创作 | A2-05 A3-03 | ✅ 连续通过 |
| C1 AI 真实回复 | AI 智能体 | C1-01 C1-02 | ✅ 连续通过 |
| C2 跟随模式 | AI 智能体 | C1-04 | ✅ 连续通过 |
| C3 新对话 | AI 智能体 | C1-05 | ✅ 连续通过 |
| E1 AI 互动页 | AI 智能体 | C3-01 | ✅ 连续通过 |
| E2 发布日历点日期 | 内容创作 | A4-06 A5-04 | ✅ 连续通过 |
| E3 任务详情 | AI 智能体 | C2-01 C2-02 | ✅ 连续通过 |
| E4 数据中心查询 | 账号管理 | A5-01 B4-02 | ✅ 连续通过 |
| E5 知识库新建 | AI 智能体 | C5-01 | ✅ 连续通过 |
| E6 设置两页签 | 公共底座 | D1-01 D1-03 D1-04 | ✅ 连续通过 |
| D1 我的任务页 | AI 智能体 | C2-01 | ✅ 连续通过 |
| D2 数据中心页 | 账号管理 | A5-01 | ✅ 连续通过 |
| D3 发布日历页 | 内容创作 | A5-04 | ✅ 连续通过 |
| D4 全局监控页 | AI 智能体 | C4-07 C4-08 C6-03 C6-04 C6-05 | ✅ 连续通过（C6-04/05 另有单测 RECEPTION_REPLIES_OK） |
| D5 知识库页 | AI 智能体 | C5-01 | ✅ 连续通过 |

**结论**：26/26 连续多轮通过（10 轮 soak 的 1-6 轮 + 5 轮 + 3 轮复测），
`apiFailures=0`、`pageErrors=0`、账号库无异常变化。

自检脚本现在直接按三大核心汇总输出，便于对照本清单：

```
=== 按三大核心功能汇总 ===
  A 内容创作: 9/9 通过
  B 账号管理: 7/7 通过
  C AI 智能体: 9/9 通过
  D 公共底座: 1/1 通过
FULL_CHAIN PASS checks=26 blockers=0
```

> 反向验证：把自检指向**旧版安装包**（server 还是修复前的 lib）时，A3 与 C1 立刻报红
> （分组误删默认分组、AI 回复回落到模板），说明这套自检确实能拦住旧代码。

### E2 本轮修复的阻塞点（都有复现与复测）

| # | 阻塞点 | 归属 | 根因 | 修复 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 删除分组会误删默认分组、目标分组反而留下 | 账号管理 B2-02 | 前端 `{ids}` 放 JSON 体、服务端只读查询串，空 ids 退化成"保留全部非默认分组" | 两种来源都读、空 ids 不删、默认分组永不删、账号回落默认分组 | 自检 A3 由 FAIL 转 PASS + 直接 API 实测 |
| 2 | server 包 `tsc -b` 编译失败 | 公共底座 | `api.ts:500` 数组下标未收窄 | 先取局部变量再判空 | `pnpm --filter ...-server build` 通过 |
| 3 | 桌面端启动页永远停住 | 公共底座 D2-01 | 端口被占 → 内核 `EADDRINUSE` → 握手失败后不再加载产品页 | 先探测已服务则接入、握手失败仍尝试加载、失败给原因与重试 | 安装版两种场景截图 |
| 4 | 32 位系统无提示 | 公共底座 D2-04 | 无架构守卫 | 明确弹窗 + README 运行环境 | 代码路径（无 32 位机器） |
| 5 | 发布失败提示登录失效，界面仍显示"在线/正常" | 账号管理 B2-05 | 服务端写 `loginState`、前端从不读 | 账号页与频道行显示「需重新登录」+ 原因 | 标记 invalid 后两处实测 |
| 6 | 误删账号无回滚点 | 账号管理 B2-06 | 删除级联清记录/统计，无快照 | 删除前自动快照 + 空保存留痕 | 实测目录生成 |
| 7 | 「停止生成」点了没用：服务端继续跑完并把任务写成「已完成」 | AI 智能体 C2-03 | abort 只改落库状态，正在流式生成的那次随后用内存副本覆盖回 completed；内核轮询没有中断通道 | 生成期登记控制位：abort 置位后停止转发增量、内核提前收工并关闭；落库 aborted 且只保留已送达客户端的部分正文；侧栏「停止」先通知服务端 | 探针改前（中断后仍收到 done、状态 completed）→ 改后门禁 `AGENT_TASK_ACTIONS` 18/18，UI 点停止后状态 aborted |
| 8 | 分享链接打不开（HTTP 404） | AI 智能体 C2-04 | 前端拼 `origin + /chat?token=`：桌面端在 `/bosom-friend/` 下且是 hash 路由，基路径与 `#` 全丢；服务端忽略 ttlSeconds、令牌只在内存 | 链接由当前地址推导（`origin + 应用基路径 + #/chat?token=`）；服务端按请求 ttlSeconds 生效、令牌落盘 `share-tokens.json`、过期即失效、urlPath 指向只读页 | 探针改前链接 404 → 改后 HTTP 200 且「仅供查看」渲染；门禁断言有效期与落盘 |
| 9 | 不存在的任务也发分享链接，用户拿到打不开的地址 | AI 智能体 C2-04 | `agent/tasks/:taskId/share` 不校验任务是否存在就发 token | 先查任务，查不到返回 18100，不发链接 | 门禁 `分享·不存在的任务不发链接 code=18100 data=null` |
| 10 | 任务详情刷新后标题回落「新对话」、收藏与评分状态丢失 | AI 智能体 C2-02 / AC-014-2 | `useChatState` 里「store 已有消息就短路」把任务详情一起省掉（为流式缓存加的优化），刷新后 `task` 恒为 null | store 有消息时只省消息拉取与轮询，任务详情照拉 | 探针改前：同一 URL 刷新后 `headerFavorited=false`、标题「新对话」；改后 true + 原标题与实星 |

### E3 待办 / 阻塞（需要你或外部条件）

| # | 事项 | 归属 | 说明 |
| --- | --- | --- | --- |
| 1 | 抖音登录已失效 | 账号管理 B1-03 | 账号页已显示「需重新登录 / 重新授权」，**扫码一次即可**继续真实发布链路 |
| 2 | 小红书账号未连接 | 账号管理 B1-02 | 需扫码；未连接时小红书真实发布无法验证 |
| 3 | 上传素材按钮在 text2video 模型下隐藏 | 内容创作 A1-09 | 所选视频模型不支持参考素材时按钮按能力隐藏，建议改为"置灰 + 说明原因" |
| 4 | AC 验收准则覆盖率 | 公共底座 D4-04 | 门禁黄灯：45 条 AC 覆盖 32.2%（覆盖 8 / 部分 13），发布前需补齐证据 |
| 5 | 发布弹窗不响应 Esc | 内容创作 A4-02 | 全屏弹层只有右上角 X（含二次确认），影响很小 |
| 6 | 素材库/草稿箱部分操作未进自检 | 内容创作 A2/A3 | 编辑、删除、预览等仅人工抽查，建议补进自检项 |

### E4 账号管理专项 debug（2026-09-10 下午）

**修掉的产品缺陷（2 个，均已进自检）**

| # | 缺陷 | 归属 | 根因 | 修复 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 被拦下的删除请求也会写一份回滚快照 | B2-06 | `DELETE accounts/:id` 先 `backupBeforeDestructive` 再判 `confirm=1`，被拒绝的请求仍复制 4 个数据文件 | 快照移到二次确认之后，拒绝的请求不再有落盘副作用 | 改前同一轮 A12+A13 留下 3 份 `pre-delete-account-*`（17:54:19.899/.026/.134，多出的那份来自被拦下的请求）；改后同流程只留 2 份（18:02、18:06 各 2 份），自检 A12 直接断言"被拦下的删除不新增快照" |
| 2 | 登录失效账号照样参与刷新，最后还提示「粉丝数刷新完成」 | B2-04 | `useFansRefresh` 只按 `status` 过滤账号、不看 `loginState`，与按钮旁"登录过期账号会自动跳过"的说明不一致 | 失效账号不参与刷新；全库失效时如实提示「暂无可刷新的正常账号」 | 自检 A15。反向验证：把过滤退回"只看 status"重建前端后 A15 立刻报红（原文「账号登录已失效却提示刷新完成（应跳过并如实说明）：抖音粉丝数刷新完成」），恢复修复后转绿 |

**顺带修掉的自检"假 PASS"（否则上面两条根本拦不住）**

| 现象 | 后果 | 改法 |
| --- | --- | --- |
| 断言写在页面/弹窗文案上（`/抖音|小红书/`、`/粉丝/`） | 频道行没渲染、只剩侧栏平台名也 PASS | A1/A4/L2-B2 改成对落库账号逐条比对：行数、昵称、粉丝数、登录态徽标 |
| 用 `[role=dialog]` 取第一个弹窗 | 页面上还有隐藏的接待规则表单，读到的不是频道管理 | 统一用 `[data-testid=channel-manager-dialog]` |
| 刷新粉丝数只查"弹窗里没有报错字样" | 点了没反应也 PASS | 等应用内通知卡片（`role=status`）出现匹配文案，并核对 `lastStatsTime` 真的前进 |
| 新建分组只查界面有没有那行字 | 只活在前端状态、没落库也 PASS | 建/删都回读 `v2/channels/account-groups`，并校验分组数量回到初始值 |
| 弹窗关闭动画期间 `isVisible()` 仍为 true | 把"正在关闭"当"已打开"，下一步点到隐藏按钮上 30 秒超时 | 打开弹窗的辅助函数先等动画落定、必要时重试 |

**新增 / 加固的自检项**

| 编号 | 覆盖功能点 | 说明 |
| --- | --- | --- |
| A2 加固 | B2-04 | 刷新反馈必须是真实通知文案；说"完成"时 `lastStatsTime` 必须前进 |
| A3 加固 | B2-02 B2-03 | 建组要落库、删组要真的消失、分组数量回到初始 |
| A4 / A5 / A7 加固 | B2-01 B2-05 B2-08 | 账号页与频道行按落库账号逐条对；可连接平台卡片必须是 2 个 |
| A9 新增 | B2-02 | 分组改名 + 上移排序，全部回读落库 rank |
| A10 新增 | B2-04 | 同一平台二次刷新必须命中 1 小时冷却，且不再打平台接口 |
| A11 新增 | B2-05 | 响应改写为 `loginState=invalid` 后，账号页与频道行都必须显示「需重新登录」 |
| A12 新增 | B2-06 | 合成账号走完：未确认删除被拦（无副作用）→ 退出只清 cookie → 确认删除留快照 |
| A13 新增 | B2-03 | 合成账号进自定义分组 → 删组 → 账号回落 `grp-default` |
| A14 新增 | B3-01 B3-02 | 11 个平台元数据 + 抖音/小红书能力声明 + 频道行显示本地化名称 |
| A15 新增 | B2-04 | 全库登录失效时不刷新，提示「暂无可刷新的正常账号」 |
| A16 新增 | B4-01 B4-03 | 账号库↔发布记录↔互动指标↔数据中心接口四者对账（孤儿记录、重复作品、缺作品证据、engagement 与 metrics 逐字段比对） |
| A17 新增 | B4-01 B2-07 | 平台没给的指标必须如实标注：接口返回 `metricAvailability`，概览卡标注"平台未提供"、默认指标避开它、手动切回来时给出说明 |
| L2-B1 / L2-B2 / L3-B 加固 | B1/B2/B3/B4 | 建组落库、账号分组不变、数据中心渲染落库账号、主链路刷新有反馈且账号数据不变 |

**账号数据对账（同日）**

- 现状核对（1 个抖音账号 / 15 条记录 / 15 条指标）：无孤儿记录与指标、无重复平台作品、已发布记录都带
  `platformWorkId + workLink + publishTime`、记录的 `engagement` 与 `metrics.json` 逐字段一致、
  analytics 接口值与落库一致、数据中心作品数与已发布记录一致。
- **发现并修掉：`qa/acceptance/data-consistency-audit.mjs` 一直在空转** —— 它按裸数组解析落库文件，
  而落库是 `{schemaVersion, value:[...]}` 信封，于是账号/记录/指标全部报 0 却"通过"。已改为信封解析 +
  账号数据四者对账，失败即退出码 1（`DATA_CONSISTENCY PASS`）。
- **播放量恒 0 是平台真值，不是采集 bug**：直接抓抖音创作者页的 `work_list` 原始响应，
  `statistics.play_count=0` 而 `digg_count/comment_count/share_count` 有值。但数据中心默认按
  「浏览量」展示趋势与作品排行（`DataStatisticsClient` 默认 `viewGrowth`），用户首屏看到的是全 0。
  建议：平台未提供播放量时给出说明或把默认指标切到点赞（产品决策，待拍板）。
- **抖音昵称是平台占位值 → 已修**：`/web/api/media/user/info/` 返回 `nickname="No name"`，
  服务端原来只回退成平台显示名"抖音"，多账号无法区分；现在回退为 `平台名 · uid`（如 `抖音 · XchanM520`，
  uid 超 12 位截断），真实昵称不受影响。单点实测：`No name`+`XchanM520` → `抖音 · XchanM520`；
  真实昵称 → 原样保留；无 uid → 仍回退平台名。
- **播放量"平台没给"已诚实化**：数据中心接口新增 `metricAvailability`（无采样行时不下结论），
  概览卡在对应指标上标注"平台未提供"，默认增长/排行指标自动避开它（改按点赞），
  用户手动切回「播放/浏览」时给出"平台未提供「播放/浏览」数据（接口返回 0），这里不做估算"。
  实测：`metricAvailability={"views":false,"likes":true,...}`、概览仅播放卡带标注、默认无提示、
  切回后提示出现；可复跑脚本 `qa/acceptance/verify-dashboard-metric-honesty.mjs`
  （临时数据根 + 独立端口，不碰真实数据，输出 `DASHBOARD_HONESTY PASS`）。

**跑法**：`$env:BF_CHAIN_ONLY='A,L2-B,L3-B'; node qa/acceptance/full-chain-loop.mjs`
→ `LAYER_GATE PASS`、`FULL_CHAIN PASS checks=19 blockers=0`（L1 16 · L2 2 · L3 1）。

### E5 AI 智能体板块专项测试（2026-09-10 晚间）

**方法**：先写行为探针 `qa/probes/probe-agent-board.mjs`（接口段 + 浏览器段，只记录不判分），
把「点下去到底发生了什么」看清，再动代码；修完先用探针复测，最后把结论固化成门禁。

**修掉的产品缺陷（2 个，均已进门禁）**

| # | 缺陷 | 归属 | 根因 | 修复 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 「停止生成」点了没用，服务端继续跑完并把任务写成「已完成」 | C2-03 | `agent/tasks/:id/abort` 只改落库状态；正在流式生成的那次随后用自己内存里的任务副本覆盖回 `completed`，内核轮询也没有中断通道 | 生成期登记控制位（`inFlightAgentTasks`）：abort 置位后停止转发增量、内核轮询提前收工并关闭；落库 `aborted` 且只保留已送达客户端的部分正文并标注「已中断」；侧栏「停止」改为先通知服务端再断本地流 | 探针改前：中断后仍收到 `done`、落库 `completed`、正文 2095 字；改后：门禁 `AGENT_TASK_ACTIONS` 18/18，UI 点停止后 8 秒状态仍为 `aborted` |
| 2 | 分享链接打不开（HTTP 404） | C2-04 | 前端拿 `origin + /chat?token=` 拼链接，桌面端跑在 `/bosom-friend/` 下且是 hash 路由，基路径与 `#` 都丢了；服务端另有三处不实：忽略 `ttlSeconds`、`expiresAt` 固定写 7 天、令牌只在内存里（重启即失效），`urlPath` 还指向私有任务详情页 | 链接改由当前地址推导 `origin + 应用基路径 + #/chat?token=`（4 处调用统一走一个函数）；服务端按请求 `ttlSeconds` 生效（1 分钟~30 天，越界回落 7 天）、令牌落盘 `share-tokens.json`、过期即失效、`urlPath` 指向只读页 | 探针改前：生成的链接 404「找不到网页」；改后：同一链接 HTTP 200，页面渲染「仅供查看」+ 对话正文；门禁断言有效期、落盘、只读消息过滤、无效令牌 40404 |

**新增门禁**：`qa/verify-agent-task-actions.mjs`（18 项，接入 `run-gate.mjs` 快速门禁；模型不可用时判黄灯）。

**本轮覆盖**：C2-03 中断/继续、C2-04 分享链接、C1-07 收藏与评分从「🟡 已实现」转为「✅ 门禁覆盖」。

**第二轮（同日晚间续，按"还有什么问题没"的清单推进）**

- 门禁从 18 项扩到 **24 项**（`BF_AGENT_BOARD_SLOW=1` 时 **27 项**）：新增 AC-005 三态、AC-014-1/2、
  「不存在的任务不发链接」「关窗口（客户端断开）不算中断」等可证伪断言。
- 又修 2 个缺陷：E2 第 9 条（不存在的任务也发分享链接）、E2 第 10 条（详情页刷新丢标题与评分收藏）。
- RTM 同步：AC-005-1/2/3、AC-014-1/2 由「未覆盖」转「已覆盖」，AC 覆盖率 **21.1% → 32.2%**。
- 前端三处修复（分享链接、侧栏停止、详情页刷新）都在未入库的 Web 工程里，只在工作区与 `dist` 生效。

---

## F. 与既有 REQ 清单的对应关系

既有 `docs/requirements/前端功能需求盘点清单.md` 按 REQ 编号登记需求；本清单按三大核心功能组织。
两者一一对应，改动任一侧都要同步另一侧：

| REQ | 需求 | 本清单归属 |
| --- | --- | --- |
| REQ-001 | 启动与主界面 | D2-01 |
| REQ-002 | 账号体系 | D3-03 |
| REQ-003 | 平台账号绑定 | B1-01 ~ B1-05、B2-01 |
| REQ-004 | AI 对话任务 | C1-01、C2-01 ~ C2-04 |
| REQ-005 | AI 三态诚实 | C1-02、D1-01 |
| REQ-006 | 内容创作-草稿生成 | A1-01 ~ A1-11 |
| REQ-007 | 素材库 | A3-01 ~ A3-04 |
| REQ-008 | 一键发布流程 | A4-01 ~ A4-08 |
| REQ-009 | 发布回传真实作品 ID/链接 | A4-05 |
| REQ-010 | 数据中心 | A5-01 ~ A5-03、B4-01 ~ B4-04 |
| REQ-011 | 热点内容与评论搜索 | C3-01 ~ C3-03 |
| REQ-012 | 知识库 | C5-01 ~ C5-05 |
| REQ-013 | 全局监控与 7×24 接待 | C4-01 ~ C4-08、C6-01 ~ C6-05 |
| REQ-014 | 任务记录 | C2-01 ~ C2-04 |
| REQ-015 | 通知中心 | D3-04 |
| REQ-016 | 设置中心 | D1-01 ~ D1-04 |
| REQ-017 | 智能体每步截图+说明 | C1-03 |
| REQ-018 | 全功能对话自动化 | C1-03、C4-05 |
| REQ-019 | 反馈/联系我们 | D3-04 |
| REQ-020 | 桌面安装/升级/卸载/数据保留 | D2-01 ~ D2-04 |
| REQ-021 | 纯正 .exe 桌面形态与体验 | D2-01 ~ D2-04 |

## G. 代码位置速查

| 层 | 路径 |
| --- | --- |
| 前端页面 | `products/bosom-friend/project/bosom-friend-web/src/app/[lng]/*` |
| 前端组件 | `products/bosom-friend/project/bosom-friend-web/src/components/*` |
| 后端 API | `products/bosom-friend/server/src/{api,routes-channels,routes-content,routes-knowledge}.ts`（159 条路由） |
| 平台引擎 | `products/bosom-friend/engine/worker.py`（发布/登录/同步/接待的真实浏览器执行） |
| 内核工具 | `products/bosom-friend/kernel/src/index.ts`（10 个只读工具） |
| 桌面壳 | `products/bosom-friend/desktop/{electron,renderer}` |
| 门禁与自检 | `products/bosom-friend/qa/` |
