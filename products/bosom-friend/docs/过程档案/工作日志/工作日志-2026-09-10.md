# 工作日志 - 2026-09-10（第二轮：三大核心全量验收 → 合并重复 → 低配适配 → 打包 0.2.40）

## 0. 本轮基线（改动前的旧构建）

- `node products/bosom-friend/qa/acceptance/full-chain-loop.mjs`（旧构建、未配置模型）：
  L1 闸门 **67/67 通过**、L2 闸门 **8/8 通过**、L3-A / L3-B 通过；L3-C 之后长时间无输出（智能体步骤在等模型），
  取得基线后主动停止，改为"改完再全量复跑"。
- **发现自检假绿**：C1 / L2-C1 / L3-C / L4-4 四处"未配置大模型"守卫写的是
  `/未配置任何大模型|大模型调用失败|内置模板/`，而前端实际固定提示是
  「您好！当前还没有配置大模型 API…」、服务端是「未接入任何大模型…」——两者都不匹配，
  于是"请先配置大模型"的提示被当成**真实回复**判 PASS。已补全文案（4 处），修复后这类项会如实报红。

## 1. 合并重复功能（本轮已做，均有证据）

| 项 | 改动 | 证据 |
| --- | --- | --- |
| 接待入口收敛 | 账号页原本 4 个页签（接待规则 / 接待记录 / 评论接待 / 私信接待）与「全局监控」的评论/私信客户列表重复。现在账号页只留**接待规则**（配置面）；评论/私信的真实待办处理与人工回复搬到全局监控新增的「待办处理」区（`data-testid=reception-todo-board`，评论/私信分段切换），与「按客户收纳的回复记录」同一页 | `views/reception/index.tsx` 删除 3 个页签；`views/reception/components/GlobalMonitor.tsx` 新增待办处理卡片；自检 C11 断言旧页签不存在 |
| 删除死链路「接待记录」 | `GET/DELETE v2/customer-reception/logs`、`POST v2/customer-reception/handle`、`reception-logs.json`、前端 API 与页签一并删除。唯一写入方 `handle` 在整个前端没有任何调用方 → 页签恒为空；真实接待结果一直由 `reception-replies.json` 承载 | `server/src/routes-content.ts` / `store.ts` / `api.ts` / `types.ts`；`qa/golden/routes-master.txt` 与 `qa/reset-zero-state.mjs` 同步；`server tsc -b && tsdown` 通过 |
| 删除死 import | `accountCore.tsx` 引入 `GlobalMonitor` 但只渲染 `ReceptionPage` | 静态审计 #9 |

## 2. 低配电脑适配（本轮已做）

| 项 | 改动 | 为什么 |
| --- | --- | --- |
| 安装期解压内核运行时 | `desktop/build/after-install.nsh` → **`installer.nsh`**（electron-builder 只认默认名，自定义钩子此前被静默忽略），并在 `electron-builder.yml` 与 `build-win.sh` 显式写 `nsis.include`；`main.cjs` 的 `kernelRootForVersion()` 改为优先使用安装期解压的固定目录 `userData/kernel-runtime`，缺失才回退按版本自解压 | kernel-runtime.zip 是 417MB / 约 23 万个文件：安装期不解压就全部压到首次启动，低配机械盘上启动页要等十几分钟 |
| 随包引擎裁剪开发期 profile | `build-engine-portable.ps1` 排除 `cookies / logs / db` | 实测 `social-auto-upload/cookies` 一项 581MB / 4161 个文件，安装包与安装占用同步下降 |
| 小屏窗口被裁 | `createWindow` 用 `screen.getPrimaryDisplay().workAreaSize` 夹取初始尺寸，最小 1024×640 | 原 1440×900 / 最小 1100×700 在 1366×768 或 125% 缩放下会把底部按钮挤出屏幕 |
| 老显卡白屏无出路 | 新增 `BF_DISABLE_GPU=1` 退回软件渲染 + GPU 进程崩溃写日志 | 全目录此前没有任何 `disableHardwareAcceleration` / GPU 崩溃处理 |
| 启动不再预热 Python 守护 | `api.ts` 的 `ensureEngineDaemon(deps)` 改为 `BF_WARM_ENGINE=1` 才预热（该函数幂等，登录链路内多处会再调用） | 原逻辑启动即拉起 Python 守护 + 一个完整 Chrome，和内核冷启动抢内存/CPU |
| 没装 Chrome 的机器可用 | `worker.py`（3 处）与 `interactions.py`（1 处）改为 `if LOCAL_CHROME_PATH and Path(LOCAL_CHROME_PATH).exists()`，否则回退随包 Chromium | 低配机常只有 Edge：原判断只看变量是否非空（conf.py 写死 Chrome 路径），executable_path 会指向不存在的文件 |

## 3. 环境前置：真实模型

- 通过真实界面（设置 → 自定义大模型）写入 Agnes 钥匙，`.tmp-model-probe.mjs` 实测
  `MODEL_PROBE elapsed=4705ms deltas=13 model=agnes-2.5-flash`，真实流式正文。
- 基线里"生成任务立刻 success"其实是模板兜底；配置模型后 B2 状态真实经历 `generating`。

## 4. 已审计、本轮未做（需决策或有回归风险）

来自两份只读审计（重复项 22 条 / 低配项 11 条），未做的都有明确理由：

- **账号与用户 store 双栈**（electron `store/account.ts` + web `store/account.ts`，status 语义相反，靠 merge 去重）：动它等于换账号数据源，风险中高，需要单独一轮带回归。
- **模型配置 4 份真相源**（llm-user.json / settings.yaml / .credentials.yaml / localStorage 回灌）：`PUT ai/user-llm` 里约 140 行兼容分支；合并需同时改内核投影链路。
- **发布弹窗 4 处挂载**、**assets 公开/私有两套同构路由**、**cordis 双 server 补丁**：中风险，本轮不动。
- 低配的 P7（ffmpeg/发布并发串行队列）、P9（逐帧复制改 concat demuxer）、P11（启动加固延后）、P10（内核运行时裁剪）留待后续。
- `qa/evidence` 已累积 128,254 个文件 / 5.15GB，以及 `qa/` 根散落的一次性报告与事件流：建议下一轮归档（不影响打包）。

## 5. 复跑结果（合并后的新构建 + 真实模型）

三大核心全量自检（`full-chain-loop.mjs`）：

```
--- L1 闸门：67/67 通过 ---
--- L2 闸门：8/8 通过 ---
--- L3 闸门：3/3 通过 ---
--- L4 闸门：4/4 通过 ---
LAYER_GATE PASS（逐层通过）
A 内容创作 21/21 · B 账号管理 19/19 · C AI 智能体 25/25 · D 公共底座 2/2
BLOCKERS 0 · API_FAILURES 0 · PAGE_ERRORS 0 · FULL_CHAIN PASS checks=82 blockers=0
```

- 合并后的接待 IA 由新 C11 断言通过：「规则在账号页；待办处理与回复记录统一在全局监控；3 个重复页签已删除」。
- 真实模型贯穿：B2 生成任务真实经历 `generating → success`（约 3 分钟），B5 草稿正文是模型写的
  （「重庆30岁之后必去的夜景打卡地，太震撼了」），L3-C 智能体主链路真实跑完并给出动作卡。

系统级门禁与冒烟：

| 项 | 结果 |
| --- | --- |
| `run-gate.mjs --full`（含 S 级前端验收） | **退出码 0，0 红灯**；12 绿（内核组合/内核入口/禁直连模型/依赖边界/追溯结构/功能完整性/缺陷闭环/安装包只读保护/模型配置链路/业务域 CRUD 4 步/知识库蒸馏链路/AI 智能体任务动作/S 级前端验收），黄 1 项：AC 覆盖率 33.3% |
| S 级前端验收（`frontend-supreme-gate.mjs`） | `FRONTEND_SUPREME_GATE PASS files=7 violations=0 missing=0` |
| `smoke-all.mjs` + `smoke-edge.mjs` | 退出码 0（含上传签名/上传/确认/回读/静态资源 6/6） |
| 模型链路 | `MODEL_PROBE elapsed=4705ms deltas=13 model=agnes-2.5-flash` |

打包：

- `BosomFriend-Setup-0.2.40.exe`（`products/bosom-friend/desktop/release/0.2.40/`，并复制到桌面）。
- 内核运行时 zip 重打、随包引擎重新组装（已剔除开发期 profile）。

## 6. 仍未清零 / 需下一轮

1. **AC 覆盖率 33.3%**（门禁唯一黄灯）：82 项自检已全绿，但 RTM 里每条 AC 的 S 级证据回填没做完。
   可直接回填的至少有 6 条：AC-006-1（B2）、AC-006-2（B5+B13+B8）、AC-007-1（B11+B3+B15）、
   AC-010-1（A16）、AC-012-1（C12）、AC-012-2（C13+C19）；其余多为需要真实平台动作的场景
   （扫码、平台拒绝、定时到点、卸载残留）。
2. 重复项审计里的中高风险合并（账号/用户 store 双栈、模型配置 4 真相源、发布弹窗 4 挂载）需专门一轮带回归。
3. 低配适配的 P7/P9/P10/P11（并发串行、ffmpeg concat、内核运行时裁剪、启动加固延后）。
4. `qa/evidence` 体积（128,254 文件 / 5.15GB）与 `qa/` 根一次性产物归档。

## 10. 交付闭环（2026-09-11 凌晨 01:2x）——B 计划全部完成

### 10.1 打包（步骤 5）
- `rebuild-kernel-and-installer.ps1` → DONE：engine-portable **15,818 文件 / 1,315.7 MB**（已剔除开发期 cookies）、`kernel-runtime.zip` 417.3 MB、
  安装包 `products/bosom-friend/desktop/release/0.2.40/BosomFriend-Setup-0.2.40.exe` **771.3 MB @ 01:17:37**。

### 10.2 产物级复核（步骤 6，直接查 win-unpacked，216 个 js）
| 检查 | 结果 |
| --- | --- |
| 包内是否还调已删路由 `customer-reception/logs` / `handle` | **0 / 0** ✅ |
| 包内是否有合并后的待办处理区 | `reception-todo-board` = 1（GlobalMonitor-*.js） ✅ |
| 包内引擎是否夹带开发期 profile | `resources\engine\social-auto-upload\cookies` **不存在** ✅ |
| 包内内核运行时 | `kernel-runtime.zip` 417.3 MB ✅ |
| NSIS 钩子是否真被收录 | `release\0.2.40\builder-debug.yml:99` → `!include "...\build\installer.nsh"` ✅ |

### 10.3 DEF-011 真机静默安装验证（步骤 7）—— **通过**
- `BosomFriend-Setup-0.2.40.exe /S` → installer **exit 0**。
- **安装期完成解压**：`%APPDATA%\Bosom Friend\kernel-runtime` 于 01:21:40 重建，解出 **199,779 文件 + 30,010 目录 = 229,789 条**（与 zip 条目数一致）；`runtime\bin-desktop.mjs` 存在；
  INSTDIR 里的 zip 已被删除（= 解压成功分支）；解出的 server lib 含新代码（`not-collected-this-sync`、`lastStatsAttemptTime`、刷新失败文案）。
- 启动后窗口标题「Bosom Friend」+ 首屏产品页（`def011-app-window.png`），设置→系统与更新显示 **v0.2.40**；
  **首启未再自解压**（无 `kernel-runtime-0.2.40` 目录）；退出后无残留进程、31280 已释放。
- 结论：**首启要解 23 万文件的"十几分钟卡启动页"问题，在真机上被消除**（DEF-011 可从"待验证"转"已验证"）。

### 10.4 交付结论
B 计划六项全部达成：①播放量诚实化+A17 假绿 ②编码损坏修复 ③analytics 假新鲜度 ④粉丝刷新假成功 → 重打安装包 → 真机安装验证 → 门禁 **0 红**（绿 14 / 黄 1 / 红 0）。

### 10.5 仍不确定 / 仍开着的（如实，不许含糊）
1. **A17 首跑曾红**（00:16:51）：原因是**测试定位器范围过大**（产品行为正确）；作者 00:18:13 收敛、00:18:36 绿、00:19:30 集成方独立复跑绿、00:54:23 全量绿——红→绿过程可查。
2. **托盘「完全退出」路径本轮未验证**（只验证了启动与关窗=最小化到托盘的设计行为）。
3. **播放量仍为 `not-collected-this-sync`**：本轮只验证了"如实标注 + 可重新同步"，**没有做"重新同步后播放量恢复"的端到端验证**（需要平台侧真的恢复返回 play_count）。
4. AC 覆盖率 33.3% 黄灯（逐条证据回填的文档活，不影响功能）。
5. 集成期间在 `%TEMP%` 留下 4 个 `bf-stats-probe-*` 目录（含账号拷贝与**明文 cookie**）——**已由父 agent 全部清除**。
6. dev 实例（31280）已由集成代理停掉（DEF-011 首启需独占端口）；需要继续开发时重启即可。


## 9. B 计划（交付级修复）执行结果 —— 2026-09-10 深夜 / 09-11 凌晨

用户指令：「按 B 干」。范围为 ①播放量诚实化+A17 假绿 ②编码损坏 ③analytics 假新鲜度 ④粉丝刷新假成功 → 重打安装包 → 真机安装验证 → 门禁 0 红。

### 9.1 全量回归（新构建 + 真实模型）

```
--- L1 闸门：67/67 通过 ---   --- L2 闸门：8/8 通过 ---
--- L3 闸门：3/3 通过 ---      --- L4 闸门：4/4 通过 ---
LAYER_GATE PASS（逐层通过） · BLOCKERS 0 · API_FAILURES 0 · PAGE_ERRORS 0
FULL_CHAIN PASS checks=82 blockers=0      （report.json at=2026-09-10T16:54:23Z）
```

- L3-C（AI 智能体主链路）耗时约 15 分钟才出动作卡（模型侧 429 重试），**未超时、最终 PASS**；期间日志静默属预期，已用「任务列表 + agent-tasks.json mtime + chrome 进程」三重活性证据确认不是卡死。
- L3-B 走限频分支，返回「全部平台均在刷新间隔内，请 43 分钟后再试」——如实回报，**旁证 ④ 的诚实化**。

### 9.2 四条缺陷的修复与证据

| # | 修复 | 独立验证 |
| --- | --- | --- |
| ① 播放量 | 新增 `metricAvailabilityReason`（`available` / `not-collected-this-sync` / `no-signal`），前端按语义提示「本次未采到…可重新同步」，不再说成「平台未提供」；A17 重写为可判红断言并收敛到「增长趋势/作品排行」两个 section | 接口实测 `views=not-collected-this-sync`、`likes=available`；A17 负向对照证明断言不空转 |
| ② 编码损坏 | 12 个损坏串按会话日志证据还原（非占位文案），先备份、diff 恰好 12 条 | `check-data-integrity.mjs` → PASS/exit 0；**根因是测试工具**（PowerShell 5.1 的 `Invoke-WebRequest -Body` ASCII 回退）；已加 `assertNoAsciiFallback` 防复发 |
| ③ analytics 假新鲜度 | GET 改纯读（不再改写 `lastStatsTime`/不触发采集），新增 `POST analytics/refresh` 真实采集口 | 隔离实例（31281+临时数据根）：两次 GET 时间戳不变、accounts.json 哈希不变 |
| ④ 粉丝刷新假成功 | 采集失败不再被 `.catch(()=>{})` 吞掉（`code 50000`+真原因）；只有真实采集成功才推进 `lastStatsTime`；前端按 `refreshStatus` 如实提示 | 未接入/Cookie 损坏 → 50000；真实账号 → `started`/`in_flight`；23.3s 后才推进时间戳 |
| 附 | `abort`/`rating` 对不存在任务返回 `18100`（与 share 同规则） | 实测三个端点均 18100 |

### 9.3 独立复核抓出、我原判断错误的地方（如实记录）

1. **A17 红的真因不是"默认指标没避开"**（我最初判断错误）：集成代理实测默认已落在「点赞」，真因是 A17 的 locator 取了**全页**，而「平台效率」板块本就该显示「本次未采到」。修法采纳**测试侧收敛**，明确否决"为让测试过而改产品"。
2. 我曾误判前端改动了产品行为：核查后确认**改动已全部回退**，且**重建产物哈希完全不变**（`DataStatsPage-ore2zhBl.js` 同名同哈希）→ 无行为差异。
3. 我此前给出的"0 红灯 = 没问题"结论不成立：数据完整性门禁**长期没接进 run-gate**，已接线；台账为空导致门禁空过，已补登并加"空表→黄灯"。

### 9.4 台账

`qa/defects/缺陷台账.md` 现登记 **21 条**（DEF-001..021），每条含严重度/根因归类/关联 REQ/回归用例/重开次数；编码损坏（DEF-019）重开 2 次、analytics 假新鲜度（DEF-016）重开 1 次——**重开次数现在可查**，这是"老问题反复出现"的量化入口。

### 9.5 关机前的状态（未完成项，不许含糊）

- `run-gate --full` 正在跑（日志 `qa-gate-full.log`），**结果未出**。
- **安装包仍是 23:15:16 打的旧包**，不含 ①③④ 与 abort/rating 修复 → 不能交付。
- **DEF-011 真机静默安装验证未做**。
- 因此"确保整个项目没有任何问题"这一前提**尚未满足**：全量功能面已 82/82 通过，但门禁终值、打包、装机验证三项还在进行中。


### 打包最终结果

| 项 | 数值 |
| --- | --- |
| 安装包 | `BosomFriend-Setup-0.2.40.exe` **771.3 MB**（0.2.39 为 1,143.8 MB，**-372.5 MB / -33%**） |
| 位置 | `products/bosom-friend/desktop/release/0.2.40/`（同时复制到桌面） |
| 随包内核运行时 | `kernel-runtime.zip` 417.3 MB（已用本轮 server/kernel 重新打包） |
| 随包引擎 | 15,818 文件 / 1,315.7 MB（裁剪前 23,587 文件 / 2,202.4 MB）；`social-auto-upload/cookies` 已剔除，`cookiesFile` 保留 |

**未做（如实）**：0.2.40 安装包没有在本机静默安装实测。`installer.nsh` 是新增的安装期解压钩子，
首次真实安装才能验证它；失败时的兜底是 `main.cjs` 自己按版本目录解压（与原行为一致，不会更差）。
建议下一轮在干净机器/虚拟机装一次，确认 `%APPDATA%\Bosom Friend\kernel-runtime\runtime\bin-desktop.mjs` 存在且首启秒开。

## 7. 独立复核（反方）与它抓出的问题（2026-09-10 深夜）

用户质疑「既当选手又当裁判」——成立：本轮我改了自检（C11 断言、4 处守卫、门禁台账规则）又用它给自己打分。
于是另开两个**独立代理**做反方复核（不复用我的脚本、自写验证），结论如下。

**复核判定**：声称 1（4 页签→1）、2（待办搬到全局监控 + testid）、4（未动的重复项）**证实**；
声称 3（接待记录整条链路删除）**功能上证实**（handle/logs 实测 40400、server 源码 0 残留，
旧包 0.2.31 产物含「接待记录×5/评论接待×2/私信接待×2」而新包为 0），但**自检未覆盖的三处残留当时还在**。

**独立复核抓出、自检完全没覆盖的问题（已全部修掉）**

| # | 问题 | 为什么自检没发现 | 处理 |
| --- | --- | --- | --- |
| A | `qa/probes/verify-reception.mjs` 仍在 POST `/handle`、GET `/logs`，对现在的服务必然抛 TypeError | 它不在任何门禁里，是孤儿脚本；自检只跑 `qa/acceptance` 与 `qa/golden` | 删除该孤儿脚本 |
| B | `qa/acceptance/contract-audit-report.json` 仍把 3 条已删路由列为后端路由（生成于 2026-09-03，本轮没重跑） | 自检不校验"生成物是否陈旧" | 删除陈旧生成物 |
| C | 数据根仍有 `reception-logs.json`（40B，无写入方）及 7 份备份里的同名残留 | 自检只查接口与落库内容，不查物理残留 | 全部清除 |
| D | 前端产物里仍有 `getLogs/clearLogs` 调已删路由 | 我只删了页面用法，漏了 `api/reception.ts` 客户端方法 | 删方法 + 重建 dist（新产物 0 命中）与安装包 |

D 是最有代表性的一条：**产物层面的反查**（在 dist 里搜已删路由）才抓到它，功能自检永远抓不到。

**复核员的 D 项推断有误（如实记录）**：他说工作日志里"与 qa/reset-zero-state.mjs 同步"不成立，
依据是该文件 git 未修改、grep reception 为 0。实际是该文件**未被 git 跟踪**（`git ls-files` 报 False），
改动看不出来；grep 0 命中恰恰是"已经删掉那一行"的结果。此条日志无误。

**复核员的限制（它自己也声明了）**：renderer 源码不在版本控制内（`project/` 只有 9 个受跟踪文件），
所以"原有 4 页签"只能用 0.2.31 打包产物作基线；0.2.40 安装包内的 bundle 未验证（release 目录只有 exe）。

## 8. 台账空过的修补

`qa/defects/缺陷台账.md` 长期是「（暂无）」→ 门禁「缺陷闭环留痕」空过（无 DEF 行就无字段可校验）。
本轮补登 **14 条真实缺陷**（DEF-001..014，含根因归类/关联 REQ/回归用例；DEF-011 待干净机器安装验证），
并在 `run-gate.mjs` 增加：台账缺失→红灯、台账为空→黄灯，避免再次空过。
`DEF-014` 就是"台账长期为空"这件事本身。



