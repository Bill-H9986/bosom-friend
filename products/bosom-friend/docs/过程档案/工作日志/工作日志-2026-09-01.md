# Bosom Friend 工作日志 · 2026-09-01

## S 级前端监考全量复考（2026-09-01 11:50–11:57）

按 `EXAM_PROTOCOL.md` 以监考方式全量复考，结果 **SUPREME_ACCEPTANCE PASS（8/8）**。

执行：
1. `frontend-supreme-gate.mjs`：PASS，7 个 S 脚本零违规；
2. `run-supreme.mjs`：AUTO_AGENT / FRONTEND_FINAL / FRONTEND_EMPTY_REPRO /
   FRONTEND_SYNC / FRONTEND_ACCOUNTS / FOLLOW_TOGGLE / AGENT_MESSAGE_CLEAN 全 PASS。

三项专项要求：
- 智能体内容创作可视化：`auto-0-agent-content.png`（内容卡+「去发布」卡片）与
  `auto-1-publish-dialog.png`（发布弹窗），时间线含 agent-content-captured；
- 发布后实时平台数据：数据中心可见 13/13 作品、播放 498、评论 33；监控页/账号页取证；
- 全程留痕：`auto-events.jsonl`（37 条页面自身请求/响应/动作）、6 张自动截图、
  4 张终检截图、2 张同步截图、各题 report.json 与 `SUPREME_TEST_REPORT.md`。

本轮修复：
- 登录改用独立全新 Chrome 档案，避免共享档案“已登录”导致二维码闪断（已实测出码）；
- 登录失败报错前端化，不再泄漏 Playwright 定位器日志；
- S 级测试断言同步为“页面可见证据”，并删除对旧造假话术“内容已经准备完成”的依赖；
- 真实发布小红书笔记 2 篇（SKYC-重庆机长），发布后同步回真实作品 ID。

剩余边界（平台/环境，如实展示不伪造）：小红书消费者端会话缺失+IP 风控；
抖音私信发送待换 imapi protobuf 通道。

## 双平台定稿监考（2026-09-01 13:16 最终轮）

结果：**SUPREME_ACCEPTANCE PASS（9/9）**，小红书+抖音均真实发布成功并同步回真实作品。

新增修复：
- 自动发布钩子接通（此前弹窗内部 useAutoPublishOnReady 未接线，视频自动发布不触发）；
- 短视频预填挂起修复（seek(0) 不触发 seeked → 元数据就绪即出封面）；
- 静态文件补 HTTP Range（206），并修复注入 index.html 的 Content-Length 误截断白屏；
- 抖音 cookie 校验改用真实 Chrome（旧 Playwright Chromium 被风控误判 cookie 失效）；
- 发布后跳转数据中心改为“打开时为自动模式”判定，抖音场景不再丢失跳转；
- 用户重新扫码登录抖音（新账号 acc-6zvvlrix），cookie_auth=True。

双平台发布证据：小红书数据中心可见 16 作品/播放 517；抖音 18 作品/播放 3.9w、
点赞 199、评论 69，新视频回链 https://www.douyin.com/video/7680429462294400271。

归档：`SUPREME_TEST_REPORT.md`、`supreme-run-report.json`、双平台事件日志与全部截图。

## 目标：优化 AI 接待客服引擎

按 `项目铁律.md` 完成：先查开源方案与许可证 → 复用成熟页面驱动 → 不把 AGPL/无 LICENSE
代码搬进 MIT 产品 → 后端只读采集，平台发送由前端按钮/真实页面执行。

## 上游调研结果与取舍

- `git-rumengai/MediaMate`：MIT，作为思路参考。
- `puyujian/xhssx`：MIT，作为小红书私信页驱动参考。
- `joneqian/ChatGPT-On-CS`：AGPL-3.0，**不引入**。
- `AIjiaKeFu/AI-Customer-Service`：AGPL-3.0，**不引入**。
- `Rockedw/douyin-web-api-sdk`：未发现 LICENSE，**不引入**，只作为协议存在性参考。
- 详细记录见 `products/bosom-friend/engine/VENDORED_RECEPTION.md`。

## 本次落地改动

### 后端

1. 新增纯函数规则匹配器 `server/src/reception-rules.ts`
   - 账号专属规则优先，平台级规则回退，优先级升序；
   - 支持全半角/大小写/空白归一化；
   - 支持“任一命中 / 全部命中”、排除词黑名单；
   - 删除 `routes-content.ts` 中的重复匹配实现，避免两份规则漂移。
2. 重构 `server/src/reception-engine.ts`
   - `intervalMinutes`、`maxPendingPerRound`、`seenWindowMinutes`、
     `cooldownMinutes` 真正生效；
   - `receptionSeen` 落盘并按窗口去重，自动裁剪；
   - 每账号每轮最多登记 `maxPendingPerRound` 条，不再无上限重复堆积；
   - 待办增加 `pending/succeeded/failed/skipped` 生命周期和 `handledAt/error/sentText`；
   - 账号级监控显示真实待处理/已处理/失败计数。
3. 新增/调整 REST 契约
   - `POST /v2/customer-reception/config`：更新轮询间隔、去重窗口、冷却、每轮上限；
   - `POST /v2/customer-reception/suggest`：只生成规则/AI 建议草稿，不写日志、不发送；
   - `POST /v2/customer-reception/pending/:id/status`：前端显式发送后回写真实结果；
   - 规则创建/更新支持 `matchMode`、`excludeKeywords`、`cooldownMinutes`。

### 前端

1. 接待规则页支持“任一/全部命中”“排除词”“失败重试冷却”，并显示匹配方式/排除词列。
2. 文案统一为：引擎 7×24 只读采集并登记待办，命中后生成建议，回复由前端智能体自动执行。
3. 评论接待/私信接待增加“生成建议”按钮，可一键填入规则模板或 AI 草稿。
4. 待办显示“待确认 / 处理中 / 已处理 / 发送失败 / 已跳过”；发送成功/失败后回写待办状态。
5. 全局监控显示真实轮询间隔、每轮上限、待确认、已处理和失败计数；
   新增“引擎参数”编辑区，可保存轮询间隔、去重窗口、失败冷却和每账号每轮上限。
6. 新增“7×24 全自动接待”前端智能体开关：
   - `AIAssistantPanel` 全局运行时自动读取待办，生成缺失建议，调用平台真实发送任务；
   - 任务先标记 `processing`，避免手动/自动并发重复发送；
   - 成功后标记 `succeeded`，失败保留 `failed/error`，页面可看到真实结果；
   - 全局监控可一键“立即处理待办”，也能关闭开关回到手动确认模式。

## 实测证据

### 1. 后端严格编译

```text
pnpm exec tsc -p products/bosom-friend/server/tsconfig.json --pretty false
exit 0
```

### 2. 规则纯函数测试

```text
pnpm exec vitest run --config products/bosom-friend/project/bosom-friend-harness/vitest.config.ts reception-rules.test.ts --reporter=verbose
✓ 归一化全半角、大小写与空白
✓ 账号专属规则优先，未命中再回退平台级规则
✓ 支持全部关键词必须命中与排除词黑名单
✓ 平台范围限制按未选平台匹配全部
Test Files  1 passed (1)
Tests       4 passed (4)
```

### 3. REST API 黑盒

使用隔离数据根 `BOSOM_FRIEND_HOME=<%TEMP%>/bf-reception-qa-*`：

- 初始状态配置：`intervalMinutes=10, maxPendingPerRound=3, seenWindowMinutes=10080, cooldownMinutes=30`；
- 更新配置后：`intervalMinutes=7, maxPendingPerRound=5, seenWindowMinutes=60, cooldownMinutes=10`；
- 新建“全部命中 + 排除词”规则后：
  - `什么时候发货，物流多久` → 命中组合规则；
  - `什么时候发货` → 回退到默认发货规则（组合规则要求全部关键词，符合设计）；
  - `已经回复过了发货问题` → 排除组合规则后回退默认规则；
- `suggest` 返回规则名与模板草稿，不产生接待发送；
- `handle` 仅写入接待记录并返回建议文本，不执行平台动作。

待办生命周期单元运行证据：

```text
MARK={"id":"pend-1",...,"status":"succeeded","handledAt":"...","sentText":"你好，已收到"}
CONFIG={"intervalMinutes":3,"maxRepliesPerRound":2,"seenWindowMinutes":10080,"cooldownMinutes":30,"maxPendingPerRound":2}
SAVED={"id":"pend-1",...,"status":"succeeded","handledAt":"...","sentText":"你好，已收到"}
```

### 4. 前端构建与真实浏览器验收

```text
npx vite build --mode=test
✓ 7341 modules transformed（Vite 8 渲染器）
✓ 213 modules transformed（Electron main/preload）
```

新增隔离验收脚本：

```text
node products/bosom-friend/qa/acceptance/reception-optimization-verify.mjs
RECEPTION_OPTIMIZATION PASS
checks:
  ruleTab=true
  readOnlyCopy=true
  commentTab=true
  dmTab=true
  realChannelCopy=true
  noAutoReplyCopy=true
  engineParams=true
  autoMode=true
  autoRunner=true
pageErrors=[]
```

截图：`products/bosom-friend/qa/acceptance/reception-optimization.png`

## 诚实边界

- 本轮已接入“前端智能体自动发送”，但尚未执行真实平台外部副作用验收；
  S 级协议要求真实对外发送必须先获明确外部副作用授权，并保存前后截图与请求闭环。
- 在当前隔离数据根只要没有已登录平台账号，自动执行会如实显示“账号未登录/未确认”，
  不会伪造发送成功。
- MediaMate / xhssx 只作为参考；未整体引入，避免为了“开源”而增加未验收的大依赖。
- 抖音私信 protobuf、小红书消费者端 `web_session` 仍是平台侧能力边界，界面会如实显示原因。
