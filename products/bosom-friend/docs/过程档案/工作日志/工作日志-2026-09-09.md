# 工作日志 - 2026-09-09

## 主题：续接 9/7-9/8 未提交成品，全部落盘归档

### 背景
- 分支：`fix/ai-autopublish`（HEAD 原为 `b6502c5`）。
- 上一会话（9/7-9/8）三路并行修复已交付入库，但**品牌 LOGO / MCP 工具 / AC-018 导航动作 / 门禁迁移 / RTM 状态 / vibe-coding 规范**这批成品仍停留在工作树未提交。
- 本会话确认：本环境沙箱已切换为 danger-full-access，git 索引/ref 写入**不再被拦截**（上一会话「幽灵暂存 ~15000 条」已消失，工作树只剩真实改动 + 未跟踪文件）。

### 本会话提交（5 个原子提交，均过 lefthook 预提交钩子）
| 提交 | 内容 |
| --- | --- |
| `96cfd8d` | fix(bosom-friend): 品牌 LOGO 修复 + 0.2.33 版本（electron-builder 显式 icon/installer 三件套 + main.cjs 窗口图标存在才传） |
| `8232abb` | feat(bosom-friend): 内核接入 MCP computer-use/ssh，移除 dsh-web-app 依赖（bundle/launcher 增 dsh-mcp-client） |
| `9684af4` | feat(web): AC-018 通用导航动作执行器 executeAgentNavigateAction（7 条动作类型→hash 路由映射，lint 0 错误） |
| `ffce0b9` | chore(qa): 门禁迁移 bin-kernel + RTM 验收状态更新（AC-009/017/018/021 标记为部分） |
| `4d7b203` | docs: 纳入 Vibe Coding 避坑实战指南（技能 SKILL.md + AGENTS.md 约定 + 工程化规范统一索引） |

### 门禁复跑（run-gate.mjs，快速门禁）
- 结论：**黄灯，无红灯**。
- 6 项绿：DSH 内核组合 / 内核入口 / 禁直连模型 / 依赖边界 / 追溯结构 / 缺陷闭环。
- 验收准则覆盖率 **12.2%（覆盖 0 / 部分 11 / 总数 45）**——发布前必须 100%，黄灯属预期。
- S 级前端验收未执行（需 `--full` + 应用运行）。

### 剩余未完成（发布前清单）
1. **AC 覆盖率 12.2% → 100%**：34 条「未覆盖」实为「功能已实现但缺 S 级验收证据」，非功能缺口；补齐需启动应用 + 真实交互，其中真实平台发布/拒绝场景曾被 **429 API 额度耗尽**阻塞（用户已确认额度不限）。
2. **S 级前端验收**：**已跑通无副作用部分 5/5 PASS**（31280 实例）——静态门禁 / 账号页 / 数据中心同步 / 助手回复纯净度 / 跟随模式；发布类监考 2 项按 `EXAM_PROTOCOL.md` 需显式获批后执行（详见 `qa/reports/s-level-acceptance-2026-09-09.md`）。过程中修掉 2 个监考脚本缺陷：Playwright `waitForFunction` 超时参数位置错误（3 个脚本）、`verify-follow-toggle.mjs` 硬编码 3080 端口。
3. ~~**P2 fansCount**~~：**已完成（741e4c2）**——engine 新增 `fetch_xhs_profile`（创作者资料接口取 `fans_count`）+ 平台分发；`run_sync` 顺带采集资料写入 `state.profile`；server 侧 `applyAccountProfile` 统一回写。实测见下节与 `qa/reports/fix-progress-fansCount-2026-09-09.md`。
4. **P2 orphan 会话目录清理**：涉及删除，需用户拍板。
5. **661 个未跟踪文件**：主体为冻结 Web 前端工程 `project/bosom-friend-web`（含 AC-018 跟随模式接线 AIAssistantPanel/index.tsx）+ QA 证据/构建产物/日志，是否入库待用户决策。

### 环境铁律（沿用，勿忘）
- 服务启动必须带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`（WorkBuddy 删除护栏会误杀应用自身备份轮换）。
- 真实发布验收依赖 `DEEPSEEK_API_KEY` / 平台额度，额度耗尽时不可伪造成功证据。
- 桌面端（0.2.x）是唯一主线；Web 端（0.13.x）已冻结，改动需用户明确批准。

### 本会话续接（fansCount，2026-09-09 午后）

- **提交 `741e4c2`**：`fix(bosom-friend): 小红书粉丝数采集与回写（fansCount 实时化）`，
  5 文件（engine/worker.py + engine/.gitignore 首次入库、platform-login.ts、platform-sync.ts、
  server/test/account-profile.test.mjs），过 lefthook 预提交钩子。
- **应用实测**（31280，新 lib 重启）：xhs 账号 acc-ukzk41h7 同步后
  `fansCount 0→7`、`nickname 小红书→SKYC-重庆机长`、`avatar 空→头像代理`、`uid 不变`；
  前端「频道管理」显示「SKYC-重庆机长 / 小红书 / 在线 / 粉丝: 7」。
- **发现并修掉一个身份坑**：xhs 账号 uid 取自登录 cookie 的 `x-user-id-creator`，
  若资料接口回填小红书号会导致下次登录新建重复账号；`fetch_xhs_profile` 因此不回填 platformUid。
- 应用当前仍在运行（`127.0.0.1:31280`，job `pwsh-2`），可直接续跑 S 级前端验收。
- **S 级前端验收（无副作用部分）**：5/5 PASS，明细见 `qa/reports/s-level-acceptance-2026-09-09.md`；
  `verify-frontend-final` / `frontend-empty-repro` 因依赖发布监考产出的任务会话而 FAIL（非产品缺陷）。
- **待用户批准**：真实平台发布监考（xhs acc-ukzk41h7 / douyin acc-991g9pkm 各发 1 条公开内容），
  批准后按 `AUTO_AGENT_XHS → AUTO_AGENT_DOUYIN → FRONTEND_FINAL → FRONTEND_EMPTY_REPRO` 顺序补齐。

### 下一轮待办（fansCount 会话收尾，2026-09-09 晚）

**第 2 条 · promo/mat 双集合统一（纯后端，可立刻验证）**
- 现状：`POST material` 写入 `promotions.json`（`promo-*`）；`GET media/list` 只读 `media.json`（`mat-*`）→ 两套集合互不可见。
- 方案：`routes-content.ts` 的 `media/list` 读路径合并两类，条目带 `source: 'material' | 'promotion'`；`POST material` 同时保证同一实体只落一处（或改为写 materials 后由 promotions 引用其 id）。
- 验证：`POST material` → `GET media/list` 必须能查到；删除后列表消失（铁律 #9 第 2/4 条）。

**第 3 条 · 内容创作平台选择与「添加频道」对齐（需重建冻结前端）**
- 先跑通 `products/bosom-friend/project/bosom-friend-electron` 构建链（产出 dist），再让两处都读 `v2/channels/platforms` + 账号实际平台，去掉前端硬编码。
- 验证：内容创作平台下拉 == 添加频道可选平台（S 级截图 + 可访问性快照）。

**第 1 条遗留**：`loginState` 已落到 API，但冻结前端不会显示；需前端读取该字段显示「登录已失效，请重新扫码」。

### 在途/交接
- engineer-2 xhs 复跑 + douyin 监考：429 额度耗尽未完成，待用户补额度后重派。
- 下一优先建议：用户补额度后先跑 S 级前端验收补 AC-001~005 证据；纯代码项（fansCount）可并行推进。

### 文档更新：开发文档 v2.0（2026-09-09）

- 重写 `0.2.0统一开发方案与路线图.md` 为 v2.0，补入上网查证的权威依据与小白可读章节：
  Electron 安全清单、Microsoft SmartScreen 声誉与桌面应用认证、SemVer / Keep a Changelog、
  NN/g 可用性十条、W3C WCAG 2、Scrum.org 完成定义、《个人信息保护法》第 6/13/14/17/24 条。
- 新增第 4 章「合规红线」：小红书 2026-07-17 AI 托管治理公告（已处置 42 万账号）与抖音开放平台处罚案例，
  指出"自动发布/自动互动"与平台规则冲突，给出 A/B/C 三方案待用户拍板；`项目铁律.md` 铁律 #6 已加风险提示。
- 新增第 2 章「小白自查」（能用 5 条 + 好用 10 问 + 反馈三件套）、第 6.3 节中文可执行 DoD、第 7 章真实状态表。
- 实测核实并写入文档：0.2.33 安装包 431,851,967 字节、SHA256 BDC33525…、
  `Get-AuthenticodeSignature` = **NotSigned**（未签名，用户安装会遇 SmartScreen 警告）；
  门禁 `latest.md` = 8 绿 2 黄，覆盖率 12.2%（0/11/45）；S 级无副作用 5/5 PASS。

### 产品定位拍板（2026-09-09，用户原话）

> 「我们的驱动核心是 DSH，我们的理念是 AI Agent 全自动一站式服务平台！面向普通用户使用场景开发！
> 由 AI 驱动全自动是我们的核心卖点！这点在任何情况下都不能改！」

- `项目铁律.md` 新增**铁律 #10**：DSH 唯一运行时 / 全自动一站式 / 面向普通用户 / 全自动不可改 /
  风险用缓解措施解决而非降级 / 验收口径 = "一句话全自动完成"的 S 级证据。
- `0.2.0统一开发方案与路线图.md` 升到 **v2.1**：第 4 章由"三选一待拍板"改为"**产品铁律：全自动不可妥协 + 风险缓解**"，
  v2.0 初稿的"真人确认发布"方案**作废**；新增 4.2 九条缓解措施、4.3 仅剩"自动互动是否全自动"待拍板、4.4 用户告知文案；
  8.1 待拍板事项由 4 件减为 3 件。
- **二次拍板（同日，用户追问后）**：全自动范围 = **全链路且包含互动**（点赞/评论/私信/关注/浏览），
  无人工兜底节点；4.3 由"待拍板"改为"已拍板：同样全自动"，新增 6 条互动落地要求；
  8.1 只剩 2 件与自动化范围无关的运营批准（真实账号监考、未跟踪文件清理）。

### 真实平台全自动监考 + P0 安装验收（2026-09-09 下午，用户批准）

用户批准：「真实平台发布/互动监考必须执行，否则无法证明业务链路是否打通」+「安装包签名与 0.2.33 干净机验收可以进行」。

**修掉两个阻断全自动发布的真实缺陷（提交 `8145a6c`）**

1. `engine/worker.py`：`run_publish` 未解包 `_sync_*_works` 的 `(works, complete)` 元组 →
   `max(works, key=lambda w: w.get(...))` 对 list 调 `.get()` →
   `平台发布失败: 'list' object has no attribute 'get'`，真实发布 100% 失败。
2. `server/src/api.ts`：`generateAgentVideo` 等待上限 180s < 实测出片 4m07s →
   超时返回 null → `publishReady=false` → 抖音发布动作卡永不出现。

**S 级前端监考（一次用户动作 → AI 全自动 → 真实发布 → 自动同步）**

| 平台 | 结果 | 真实作品 | 链接 |
| --- | --- | --- | --- |
| 小红书 | **PASS** | `6aa0fce70000000026015874` | https://www.xiaohongshu.com/explore/6aa0fce70000000026015874 |
| 抖音 | **PASS** | `7683421642705489186` | https://www.douyin.com/video/7683421642705489186 |

依赖脚本：`verify-frontend-final.mjs` **PASS**、`frontend-empty-repro.mjs` **PASS**。
明细：`products/bosom-friend/qa/reports/s-level-acceptance-2026-09-09-publish.md`。

**0.2.33 安装包验收：FAIL（发现致命问题）**

- 安装/启动正常（退出码 0、窗口在、端口 200），但**窗口白屏**：
  打包内 `frontend-dist/index.html` 引用 `/assets/*`，服务器只在 `/bosom-friend/` 下提供前端 → 全部 404。
- 卸载干净：目录/快捷方式/进程清除，数据根 14415 个文件前后一致；仅遗留历史注册表项 `Bosom Friend 0.13.5`。
- 处理：版本升 `0.2.34`，用当前 dist（`base: '/bosom-friend/'`）重建安装包，待复验。

**签名**：本机无代码签名证书、`electron-builder.yml` 未配置签名 → 无法签名；
购买方案（IV 个人 / OV 3,588–4,888 元/年 / EV 6,888 元/年 / Artifact Signing 9.99 美元/月起）
与落地配置已写入《0.2.0统一开发方案与路线图.md》§3.2。

**RTM 更新**：AC-008-1、AC-009-1 → 已覆盖；AC-021-3 → 部分（0.2.33 白屏，0.2.34 待复验）。

### 用户五条要求与处理（2026-09-09 傍晚）

1. **平台风控限频**：新增**铁律 #11**——同账号 24h 内 ≤1 次真实发布 / ≤1 次互动监考、间隔 ≥6h、
   逐次获批、禁止批量补测、连续失败 2 次即停、产品侧必须实现账号级限频+随机间隔。本会话后续不再发起真实发布。
2. **不签名**：产品决策——**不做代码签名**；开发文档 §3.2 记录决策与取舍，8.2 P0 移除签名项。
3. **前端显示旧版本**：定位到两个根因并修复——
   - `server/src/api.ts` 写死 `appVersion: '0.13.9'`（前端 `__APP_VERSION__` 来源）→ 改为
     `BOSOM_FRIEND_VERSION`（桌面壳注入 `app.getVersion()`）→ 回退 desktop/package.json → 空串交给前端构建期版本；
   - `desktop/electron/main.cjs` 固定解压 `kernel-runtime-v3` 且以 marker 判断"已解压"→ **升级后仍跑旧运行时**；
     改为按版本分目录 `kernel-runtime-<version>`。
4. **未做完的继续做**：注册表残留 `Bosom Friend 0.13.5` 已备份后清除（仅剩 0.2.34）；
   真实发布/互动监考按铁律 #11 限频，待批准后逐次执行。
5. **跟随模式与前端动手铁律**：开发文档新增 §6.5（跟随模式到底是什么）与 §6.6（铁律已写在
   `server/src/api.ts` 的 `AGENT_OPERATING_RULES`，但内核 12 个工具中仍有 4 个写工具越权，列为 P0）。

**新发现（重大）**：0.2.34 安装包内**没有 Python 平台引擎**（无 `worker.py`/patchright/social-auto-upload），
主进程也未注入 `BF_ENGINE_ROOT` → **安装版无法真实登录/发布/同步**，只有源码态可用。已记入开发文档 §7.4 / §8.2。

**进行中**：0.2.35 重建（server lib 同步进内核运行时 → 重打 kernel-runtime.zip → 重建安装包），
让上述修复真正随包生效。

### 智能体规则专业化 + 依赖补齐 + 工具只读化（2026-09-09 深夜）

**用户要求**：① 限频太离谱，应按平台规则优化、用浏览器拟人化抗风控；② 缺失依赖全部补齐；
③ 登录了哪个账号就用哪个账号真实发布/私信/评论；④ 四个写工具按推荐办法走；
⑤ 给 APP 智能体写一份像 DSH/豆包/Codex 那样详细专业的规则。

**已完成**

1. **铁律 #11 重写**：拟人化为主手段（真实浏览器 + 随机节奏 + 非固定时间点）+ 逐平台规则适配表 +
   合理限频（默认发布 ≤3 条/天/账号、互动 ≤20 次/天，间隔随机 5–25 分钟，可配置）+ 不并发轰炸。
2. **内核工具只读化 12 → 10**：删除 `bosom_content_save_draft`、`bosom_content_delete_draft`；
   `bosom_content_generate_script` 去掉 save 与落库分支。冒烟：`KERNEL_TOOLS_OK 10 tools`。
3. **依赖补齐**：新增 `desktop/build-engine-portable.ps1`（engine + .venv + social-auto-upload +
   便携 Python 3.12 + patchright chromium-1208 = 19,993 文件 / 1,896.9 MB），
   electron-builder extraResources → `resources/engine`；main.cjs 注入 `BF_ENGINE_ROOT` /
   `BF_ENGINE_VENDOR_ROOT` / `PLAYWRIGHT_BROWSERS_PATH` 并改写 `.venv/pyvenv.cfg`。
   实测随包 python 可导入 patchright/playwright。
4. **智能体规则专业化**：新增 `server/src/agent-rules.ts`（十节、3,863 字），
   api.ts 改为引用；新增门禁 `qa/verify-agent-rules.mjs` → `AGENT_RULES_OK sections=10`。
5. **变更日志补齐** 0.2.34 / 0.2.35 / 0.2.36 三条（此前 0.2.x 全断档）。

**进行中**：0.2.36 重建（引擎随包 + 规则 + 只读工具），完成后需安装复验：
引擎可被服务端找到、设置页不再报"Python 环境缺失"、私信/评论可真实发送。

### 随包引擎实测与 BOM 修复（2026-09-09 深夜续）

- 0.2.36 安装包（1,090.8 MB）已含 `resources/engine`（1,896.2 MB / 19,993 文件）：
  worker.py、interactions.py、social-auto-upload/uploader、.venv、python-base、browsers/chromium-1208 全部就位。
- 发现并修复：`build-engine-portable.ps1` 用 `Set-Content -Encoding UTF8` 写出带 BOM 的 pyvenv.cfg，
  行首 BOM 让 main.cjs 的 `^home\s*=.*$` 匹配失败 → 安装版引擎指向占位符。
  已改为无 BOM 写入 + 主进程正则加 `\uFEFF?` 容错（提交 `c4dfc21`）。
- 实测（手工修正 pyvenv.cfg 后）：随包 python 3.12.10、base=resources/engine/python-base、
  `import patchright/playwright` OK、`worker.py --help` 正常 → **随包引擎可运行**。
- 平台接口 `/api/v2/channels/platforms` 返回 code=0，小红书等平台 status=available。
- 0.2.37 重建中（含 BOM 修复），待复验"全新安装即可自动改写 pyvenv.cfg"。

### 用户反馈修复：单工作组 + 目标平台对齐（0.2.39）

- **图2「10 个平台」乱成一团**：根因是 `resolveAsset()` 对已带部署基址的根绝对路径又拼一次基址
  （`/bosom-friend/bosom-friend/assets/logo.png`）→ 图片加载失败 → 浏览器渲染 alt 文本（平台名竖排）。
  修复后实测 10 个图标全部加载（broken=0），胶囊高度 124px → 28px（提交 `7952a9d`，0.2.38）。
- **目标平台与「我的频道」对齐**：`useTaskPlatforms()` 改为只列已连接账号的平台。
  0.2.39 实测：内容创作页目标平台胶囊由「10 个平台」变为 **「2 个平台」（小红书 + 抖音）**。
- **素材组只留一个**：`PlanTabBar` 在方案数 ≤1 时返回 null（不再渲染标签栏）；
  数据层把 2 个素材组合并为 1 个（`mg-persist` → 「默认素材组」），
  draft-generations(11)/media(62)/promotions(11) 共 84 处引用重指，
  合并前备份在 `backups/manual-20260909-before-group-merge`。
  实测：页面不再出现组标签（groupTabs=[]），草稿标题里的「人社局无人机装调检修」是内容标题，非分组。

### 用户反馈：前端设计回到 0.2.31 + 内容创作/添加频道平台对齐 + 设置视觉优化（2026-09-09 夜）

**用户原话**：「前端设计按照 0.2.31 做！那个是最新版！你现在做的越做越回去了！！！将内容创作平台和添加频道平台对齐！然后设置模块再视觉优化优化！」

**判定基线（先取证，再动手）**
- 把开发版 dist 临时换成 0.2.31 安装包内前端（`release/0.2.31/win-unpacked/resources/frontend-dist`，235 资源），
  逐页截图：首页 / 设置三页签 / 内容创作 + 目标平台弹层 / 频道管理 —— 与当前源码构建逐页对照。
- 两份 CSS 规则集比对（`index-*.css`）：0.2.31 有、当前没有的只有 15 条，
  且全部是设置页用到的 Tailwind 工具类（`p-7`/`gap-7`/`pt-10`/`leading-7`/`border-brand-cyan`/`bg-brand-purple/10` …）；
  当前多出的 109 条全是 DSH 模型模块版式（`bf-*` 与 `_*_cg7n0_*`）。
  → 结论：**唯一的设计回退点是「设置 → 自定义大模型」**，其余页面与 0.2.31 一致。
- 项目自己的验收门禁也写着 0.2.31 文案：`qa/acceptance/critical.mjs` C07 要求
  「AI 模型服务」+「自动获取可用模型」，`qa/probes/probe-llm.mjs` 要求「使用你自己的 AI 钥匙」
  「去 Agnes AI 国内站领取钥匙」「保存并使用我的钥匙」「注册 Agnes 账号」。

**改动（源码）**
1. `SettingsModal/tabs/CustomLlmTab.tsx` 重写为 0.2.31 版式：AI 模型服务 + 状态徽标 + 说明、
   单张「使用你自己的 AI 钥匙」卡片（三步引导 → Agnes 领取 → 接口地址 / API Key / 模型名 +
   自动获取可用模型 → 图片模型 / 视频模型（选填）→ 清除 / 保存并使用我的钥匙）、
   钥匙安全说明、两条常见问题；删除 `tabs/llm-models/` 三份 DSH 样式表。
   数据层仍是 `ai/user-llm` 的 `providers[] + activeProviderId`：表单映射成当前生效提供方，
   其它提供方原样回传（密钥留空由服务端按 id 保留），保存后仍写 DSH `settings.yaml` / `.credentials.yaml`；
   「清除」改为提交空提供方列表，内核路由与凭据一并清空。
2. 视觉打磨：图标块 + 带图标徽标、字段提示、API Key 显隐、选填分组折叠卡片（已配置标签 + 箭头）、
   操作行分隔线、内联错误/成功提示条、设置页签品牌色选中态。
3. 平台对齐：`store/platformMetadata/utils.ts` 新增 `CHANNEL_PLATFORMS`（小红书 / 抖音）+
   `isChannelPlatform()` + `getTaskChannelPlatformInfos()`；`useTaskPlatforms()`、
   `ChannelSidebar`、`ConnectChannelList` 共用这一份白名单（去掉两处重复的本地 allowlist）。
   内容创作目标平台由「只有已连接账号的平台」改为「与添加频道一致的白名单」，已连接排前。

**证据（铁律 #13）**
- 新增 `qa/acceptance/verify-design-0231.mjs`（functional 层）：`DESIGN_0231_GATE PASS checks=5 failed=0`
  - D01 创作=小红书/抖音 频道=小红书/抖音；D02 胶囊计数=2=列表数；
    D03 设置 9 项 0.2.31 文案齐全；D04 无 DSH 版式残留；D05 无未捕获异常。
  - 截图：`qa/acceptance/design-0231/01..04-*.png`。
- `node products/bosom-friend/qa/verify-llm-config.mjs` → `LLM_CONFIG_GATE PASS`（四环节全 PASS）。
- `node products/bosom-friend/qa/run-gate.mjs` → 黄灯无红灯（模型配置链路绿；AC 覆盖率 17.8% 黄，历史项）。
- 界面实测：设置页保存 → 三个文件（`llm-user.json` / `settings.yaml` / `.credentials.yaml`）同秒改写；
  深浅两套主题各截图一次（暗色下卡片/徽标/输入框正常）。

**数据事件（已处理）**
- 本次会话期间发现：`~/.bosom-friend/bosom-friend/accounts.json` 在 20:28:39 被清空
  （连同该账号的发布记录与统计，符合 `DELETE v2/channels/accounts/:id?confirm=1` 的级联行为；
  开发版运行日志无请求记录，无法定位触发者——本会话的脚本与浏览器操作均未点击删除按钮）。
- 用户确认后从 20:03:19 自动备份恢复：`accounts.json` 12322 字节写回，
  空文件另存为 `accounts.json.deleted-20260909-2028.bak`。
  验证：`GET v2/channels/accounts` → count=1（抖音 / 粉丝 36）；频道管理侧栏「我的频道 1 / 抖音」恢复。


### 全链路自检循环（用户要求：三大核心功能反复跑、发现即修，跑到无阻塞）

**做法**：新增 `qa/acceptance/full-chain-loop.mjs`——只驱动真实界面（不直连后端），
把「内容创作 / 账号管理 / AI 智能体」三大核心功能逐项真点真跑，每步独立断言 + 截图，
最后输出 BLOCKERS 清单；`--real-publish` 时额外真实提交一次发布。

**每轮覆盖（22 项）**
- 账号管理：频道管理列表、刷新全部平台粉丝数、新建分组→删除分组、账号页昵称/状态/粉丝。
- 内容创作：目标平台=添加频道白名单、AI 生成草稿、素材库页签、工具栏一键发布弹窗、
  草稿卡片→详情→发布预填（标题/正文/账号）、真实发布并拿到作品链接。
- AI 智能体：助手真实回复（非模板）。
- 其余页面：AI 互动、发布日历点日期、任务详情、数据中心查询、知识库新建、设置通用/系统与更新。

**循环中命中并修复的阻塞点**
1. **删除分组会误删默认分组**（严重）：前端 `http.delete` 把 `{ids}` 放在 JSON 体，
   服务端只读查询串 → `ids` 为空时过滤退化成"保留全部非默认分组"，
   默认分组被删、目标分组留下，账号 `groupId` 变成孤儿。已修（两种来源都读、空 ids 不删、
   默认分组永不删、账号回落默认分组）→ 提交 `ed6ee55`；实测 A3 由 FAIL 转 PASS。
2. **server 包编译失败**：`api.ts:500` 数组下标未收窄（noUncheckedIndexedAccess），
   `pnpm --filter ...-server build` 直接失败、后端改动落不到 lib。已修（先取局部变量再判空）。
3. 测试脚本自身的问题（不属产品缺陷，已修正脚本）：同 URL 的 `goto` 只做同文档 hash 导航
   → 加 `_r` 参数强制重载；发布弹窗不吃 Escape → 走 X + 二次确认；「全部」页签混排生成任务卡
   → 切「草稿」页签取 `draftbox-draft-card`；日历默认周视图 → 取 `calendar-week-view` 里的日期格。

**实测结果**
- `node qa/acceptance/full-chain-loop.mjs` → `FULL_CHAIN PASS checks=26 blockers=0`（扩到 26 项，
  新增：连接新频道入口可进可退、提示词编辑器+重置配置、草稿列表批量模式、跟随模式开关、新对话）。
- 加 `--real-publish` → `FULL_CHAIN PASS checks=22 blockers=0`，真实发布拿到作品链接
  `https://www.douyin.com/video/7683531929395809536`（本地记录 status=1）。
- 多轮 soak（每轮 26 项、无真发）连续通过，用于观察偶发；脚本登记进 `TEST_TIERS.json` functional 层。
- AI 智能体全链路监考 `frontend-auto-agent-exam.mjs`（douyin）→ PASS，
  作品 `https://www.douyin.com/video/7683514851985018175`，数据中心/监控/账号页均有回读证据。
- 三连跑 soak（无 --real-publish）用于观察偶发：见下方结论。

**模型配置**：用 `.env` 里的真实 Agnes 钥匙经设置页写入（对话 agnes-2.5-flash /
图片 agnes-image-2.5-flash / 视频 agnes-video-2.5-flash），AI 链路这才真正可用。

**今日真实发布记录**（铁律 #11 限频：≤3 条/天/账号，今天用了 3 条）
- 12:41 AI 智能体全链路：`https://www.douyin.com/video/7683514851985018175`
- 13:47 内容创作草稿链路（--real-publish）：`https://www.douyin.com/video/7683531929395809536`
- 21:5x AI 智能体复跑：见 `qa/acceptance/frontend-auto-agent-douyin-report.json`

**观察到的非阻塞项（待用户拍板再动）**
1. 上传素材按钮在「视频模型=agnes-video-2.5-flash」时消失——该模型 modes 只有 `text2video`，
   `maxImages/maxVideos/maxAudios` 全为 0，按现有逻辑 `showAddButton=false`。
   换成内置 `zy-template-video`（含 multi-ref/video2video）即恢复。建议后续改成「按钮保留但置灰 + 提示原因」。
2. 发布弹窗不响应 Escape（只有右上角 X + 二次确认）；全屏弹层，影响很小。
3. 小红书真实发布未测：本机只连了抖音账号，连接小红书需要用户扫码授权。


### 桌面端启动页卡死 + 32 位系统 + 发布登录失效（用户实测截图反馈）

**用户反馈**：截图显示安装版停在启动页「启动时间较长，请再稍等…」；并反馈 32 位系统用不了。

**根因与修复（均已实测）**
1. **启动页卡死**：复现出内核报错原文
   `failed to apply loader entry bosom-webserver (@deepseek-ai/dsh-host-webserver): listen EADDRINUSE: address already in use 127.0.0.1:31280`——
   另一个实例（或开发版）占着 31280 时，新实例握手必然失败，而旧 `main.cjs` 握手失败后
   不再尝试加载产品页，用户只能一直看启动页。
   现在：启动前先探测产品页是否已在服务 → 已服务就**直接接入既有实例**、不再起内核；
   握手失败仍尝试加载产品页；两条路都失败时启动页显示真实原因 + 「重试启动 / 完全退出」。
2. **桌面壳回退到 0.2.31 完整能力**：托盘菜单、退出清理整棵后代进程树、错误脱敏、
   快捷方式图标校正、随包 ffmpeg、开发态引擎路径、preload 的 `__APP_VERSION__` 与 `quit()`；
   同时保留按版本分目录的 `kernel-runtime-<版本>` 与 `pyvenv.cfg` 自动改写。
3. **32 位 Windows**：`process.arch === 'ia32'` 时弹窗明确说明"需要 64 位 Windows 10/11"；
   README 增加「运行环境」（64 位要求 + 磁盘/内存建议）。安装包按 x64 构建，
   32 位系统上双击安装包会被 Windows 直接拒绝，属预期。
4. **发布失败标记登录失效**：平台返回"抖音登录已失效，请重新扫码登录"时，
   账号立刻置 `loginState=invalid` + `loginNote`（与同步路径同字段），账号页不再假"在线"。

**取证**：把安装版 `resources/app.asar` 换成新壳后实测两个场景并截图：
端口被占 → 启动后直接进产品界面（`app-launch-1.png`）；
端口空闲 → 内核约 25s 拉起、31280 就绪后进产品界面（`app-launch-2.png`）。
提交 `8284e73`。

**待办**：抖音账号登录态已失效（今天 3 次真实发布后平台要求重扫），
需要用户扫码重新授权后才能继续做真实发布链路；其余功能不受影响。


### 10 轮全量复测 + 桌面端修复验证（2026-09-09 深夜 ~ 09-10 凌晨）

**用户要求**：解决截图里的启动页问题与 32 位系统问题，然后"将整个系统的每一个功能测试 10 遍"。

**桌面端（提交 8284e73）**
- 启动页卡死根因已复现：端口被占时内核 `listen EADDRINUSE` → 握手失败 → 旧 main.cjs 不再加载产品页。
  修复后两个场景都实测通过（安装版 asar 换成新壳，桌面截图取证）：
  端口被占 → 直接接入既有服务进产品界面；端口空闲 → 内核约 25s 拉起后进产品界面。
- 桌面壳回退到 0.2.31 完整能力（托盘/退出清理进程树/错误脱敏/快捷方式图标/ffmpeg/开发态引擎路径），
  保留按版本分目录的 `kernel-runtime-<版本>` 与 `pyvenv.cfg` 自动改写。
- 32 位：`process.arch === 'ia32'` 弹窗明确说明需要 64 位 Windows；README 增加「运行环境」。

**登录态可见性 + 误删兜底（提交 e596507）**
- 账号页徽标在 `loginState=invalid` 时显示「需重新登录」（title 显示平台原因），
  频道管理行（AccountStatusView）同样显示；实测把抖音账号标记为 invalid 后两处都生效。
- 删除账号前自动快照到 `backups/pre-delete-account-<ts>/`；账号库写成空数组时记调用栈到 `account-save-diag.log`。

**10 轮复测结果**
- 全链路自检（26 项/轮）：1-6 轮全绿；第 7-10 轮出现「账号库被清空」导致 A4/B4 失败。
  排查：账号删除只可能来自"删除频道 + 确认"（`DELETE accounts/:id?confirm=1` 会级联清记录/统计）。
  当时同时跑了两份自检脚本（前一份被 kill 但子进程可能仍在跑），两边的"删除确认"按钮可能互相点串，
  属测试编排问题；产品侧已加删除前快照与留痕兜底。
- 随后 5 轮 + 3 轮复测：`checks=26 blockers=0`，`accountChanges=0`，无空保存留痕。
- 安装版已同步为当前源码前端（备份 `frontend-dist.pkg-20260910-004305`），启动实测正常。

**仍需用户操作**：抖音登录已失效（今天 3 次真实发布后平台要求重扫），
账号页现在会显示「需重新登录 / 重新授权」，扫码后即可继续真实发布链路。


### 项目按三大核心功能分区 + 功能清单（2026-09-10）

**用户要求**：「从现在开始！整个项目以三大核心功能进行区分各小功能区！然后你拉一个功能列表详细介绍与执行的列表出来！」

**落地**
1. 新增唯一功能清单 `products/bosom-friend/docs/功能清单-三大核心功能.md`：
   - **A 内容创作**（创作工作台 / 草稿箱 / 素材库 / 发布 / 数据回流）34 项
   - **B 账号管理**（频道连接 / 账号库 / 平台目录 / 账号数据）20 项
   - **C AI 智能体**（对话与动作 / 我的任务 / 热点互动 / 7×24 接待 / 知识库 / 全局监控）30 项
   - **D 公共底座**（模型配置 / 桌面壳 / 数据与安全 / QA 门禁 / 用户体系）17 项
   - 合计 **101 个功能点**：✅ 自检连续通过 62、🟡 已实现待补自检 39、⛔ 未实现 0。
   - 每条含编号/入口/说明/实现位置/状态；E 节为执行清单（26 项自检矩阵、6 个阻塞点修复证据、6 条待办），
     F 节为 REQ↔A/B/C/D 双向映射，G 节为代码位置速查。
2. `项目铁律.md` 增补**铁律 #14**：功能必须归属三大核心之一（或公共底座）、必须登记进该清单、
   状态只能按证据升级（✅ 需自检连续通过）、REQ 清单双向同步、新能力按"服务哪个核心"归位。
3. 自检脚本 `full-chain-loop.mjs` 增加按核心汇总输出，直接对照清单：
   A 9/9、B 7/7、C 9/9、D 1/1，`FULL_CHAIN PASS checks=26 blockers=0`。
4. `README.md` 与 `docs/requirements/前端功能需求盘点清单.md` 顶部互相指向，避免两边漂移。

**反向验证（顺带证明自检有效）**：把自检指向**旧版安装包**（server 还是修复前 lib）时，
A3（分组误删默认分组）与 C1（AI 回复回落到模板）立刻报红；切回源码开发版后 26/26 全绿。

提交：`148bec4`（清单 + 铁律 #14）、`a2af5a4`（总览计数表）、`e315308`（自检按核心汇总 + 脚本入库）。


### 补齐 39 个 🟡 功能点的自检（2026-09-10 凌晨，用户要求"其他先做完、真实发布不着急"）

**做法**：`qa/acceptance/full-chain-loop.mjs` 从 26 项扩到 **57 项**，新增 31 项，覆盖原 🟡 中的 28 项：
- 内容创作（+10）：参数限制、文案要求、本地素材上传（切到支持参考素材的模型后**真上传**）、
  纯文生视频模型下上传按钮禁用且给出原因、生成记录、草稿编辑+删除二次确认、
  生成中任务卡、素材预览（灯箱计数）、发布日历建排期、数据中心排行/空态。
- 账号管理（+4）：账号资料完整性、频道行操作入口、授权页取消、平台同步能力声明。
- AI 智能体（+16）：对话附件上传、任务收藏筛选、任务分享弹窗/评分、评论搜索、热点平台导航、
  接待推荐模板、接待规则测试、全自动接待开关（aria-checked 回读）、接待记录、
  知识库新建→编辑→保存→回读（用完自动清理）、搜索、反向链接、挂载外部库、
  监控参数保存、立即处理待办、生成记录自动沉淀。
- 公共底座（+1）：通知与联系我们。

**自检命中的真实缺陷（已修）**：频道行的「刷新粉丝数」按钮**永远不会出现**——
前端判 `capabilities.analytics.account === true`，服务端只发 `analytics.supported`；
服务端补发 `account: true`（提交 68677d9）。

**结果**：`FULL_CHAIN PASS checks=57 blockers=0`
（A 内容创作 20/20 · B 账号管理 10/10 · C AI 智能体 25/25 · D 公共底座 2/2；
apiFailures=0、pageErrors=0）。功能清单同步：**108 个功能点 → ✅ 95 / 🟡 13 / ⛔ 0**。

**剩余 🟡 的 13 项**（均为外部条件或流程证据，非代码缺陷）：
需扫码（小红书/抖音授权、扫码发布）、需真实发布额度（任务重试/取消）、
未做破坏性操作（批量移动/删除素材、作品删除对账）、流程性证据（AC 覆盖率、S 级监考、32 位机器实测）。

**并发协作**：另一会话同时在做「知识库蒸馏」（已提交 32c4a09 / 4769e91，并往功能清单补了 C5-06~C5-10 等 7 条）。
铁律编号冲突已修：知识库铁律保留 #14，本次的分区铁律改为 #15，#13 第 5 条归位。

提交：`68677d9`（57 项自检 + analytics 修复）、`f540598`（清单状态同步）。


### 分层递进测试体系（2026-09-10 凌晨，用户要求"层层递进保证整个 APP 的正常运行"）

**用户要求**：按三大核心功能的小功能挨个测 → 各自无问题后再合并测大功能板块 → 三大板块单独过了再整体合并测。

**落地**：`qa/acceptance/full-chain-loop.mjs` 改为四层结构，层间有硬闸门（本层失败即停止向上）：

| 层 | 内容 | 项数 | 结果 |
| --- | --- | --- | --- |
| L1 小功能点 | 每个功能点单独验（页面每步重载） | 57 | 57/57 |
| L2 小功能区 | 区内功能串起来跑（创作台/草稿箱+素材库/发布/频道+账号/平台+数据/对话+任务/热点+接待/知识库+监控） | 8 | 8/8 |
| L3 核心板块 | 内容创作 / 账号管理 / AI 智能体 各一条主链路 | 3 | 3/3 |
| L4 整体集成 | 智能体→发布、知识库→生成注入、账号状态→创作、设置→对话 | 4 | 4/4 |

**一次完整运行**：`LAYER_GATE PASS（逐层通过）`、`FULL_CHAIN PASS checks=72 blockers=0`。

**分层测试暴露并修复的真缺陷（2 个）**
1. **智能体谎报登录态**：内核工具 `bosom_platform_list_accounts` 只返回 status，模型据此说"抖音账号已登录"，
   而实际登录已失效。现在返回 `loginState/loginNote`，回复变为"抖音账号登录已失效，需要重新扫码登录才能发布"。
2. **动作卡被掐掉、正文仍引导点按钮**：视频素材生成失败会阻止动作卡，但正文仍写"点击「去发布」"，
   用户找不到按钮。现在正文追加如实说明。

**测试自身的安全修正**：跟随模式会自动点动作卡并**真实提交发布**（本轮误触发 9 次，均因账号登录失效在平台侧失败，
没有产生真实作品）。现在自检在涉及动作卡的步骤（L3-C/L4-1）先关闭跟随模式，只验证"动作卡→发布弹窗预填"，
并新增 `dismissOverlays` 清理弹层，杜绝误触。

提交：`2db2f6f`（分层改造 + 两个产品修复）、`e2998ae`（跟随模式安全化）。

**未做 / 待办**
- `ai/models/fetch` 仍报 HTTP 404（现存配置里的密钥是 `sk-verify-placeholder` 占位值，
  且该接口地址没有 /models 列表）——这是既有数据问题，不是本次改动引入；真实模型调用因此无法验证。
- 多提供方在界面上只剩「当前生效提供方」可编辑（其它提供方保留在配置里不再展示），
  与 0.2.31 单表单版式一致；若需要多提供方编辑，需另立设计。
- Web 前端工程仍未入库（661 个未跟踪文件，等用户拍板），本次只提交已跟踪文件的改动。

\n### 知识库放大为「蒸馏引擎」+ CRUD 门禁化（2026-09-10 凌晨，用户要求）\n\n**用户原话**：「按推荐方案优化吧！同时知识库目前在APP中的作用还是太小了！你看怎么让知识库的作用发挥到最大！这个是大模型做蒸馏的关键！」\n\n**1. CRUD 门禁化（推荐方案第 1 项）**\n- 新增 qa/crud-matrix.mjs：五域（账号分组/素材组/素材/知识库笔记/接待规则）各验\n  增→列表出现→改→删后消失且不可再读，每步同时校验接口与落盘文件，共 34 项；\n  应用未运行时退出码 2，门禁判黄灯而非红灯。\n- 接入 qa/run-gate.mjs → 门禁新增 [绿] 业务域 CRUD 四步（CRUD_MATRIX PASS checks=34）。\n- 提交 72eab8f。\n\n**2. 知识库注入：从「摆设」到「模型的可检索记忆」**\n- 新增 server/src/knowledge-distill.ts：按指令检索知识笔记（拉丁词 + 中文 2 字滑窗，无需分词器），\n  命中片段拼成 [已知知识] 块注入模型上下文；对话与内容生成共用同一条检索链路。\n- 自动沉淀/ 目录是审计日志，**明确排除**不参与注入（它们含大量业务关键词，会稀释真知识）。\n- 新增 POST knowledge/inject-preview：输入一句话即看命中哪几篇、多少字、排除了什么。\n\n**3. 知识蒸馏：可微调 / 可少样本 / 可复盘**\n- 每次生成与对话留一条结构化样本（指令、输出、模型、注入来源、注入字数、状态、评分）→ knowledge.json.distillations。\n- 新增 GET knowledge/distill/stats、/dataset、/export（JSONL，OpenAI messages 结构 + 元数据）、\n  /fewshot、POST knowledge/distill/rate。\n- 前端 KnowledgePage 新增「蒸馏」页签（KnowledgeDistillPanel.tsx）：统计卡、注入预览、样本列表、1–5 星评分、导出 JSONL。\n  截图：qa/acceptance/knowledge-distill-tab.png。\n\n**4. 端到端实测（证据）**\n- 写入专属知识「重庆夜景内容必须带口号：山城夜话，灯火为证」→ 预览命中该笔记、无关指令不命中、审计笔记被排除。\n- 真实调用 i/chat 提问「用一句话说明重庆夜景口播的要求」→ 模型回答原文：\n  「重庆夜景口播只需记住一句话：**

### 账号管理板块专项 debug（2026-09-10 下午）

**用户要求**：「咱们继续账号管理板块的debug测试！」

**做法**：先用探针脚本看清真实行为（新增 `qa/probes/probe-account-module.mjs`、
`qa/probes/probe-account-notice.mjs`：刷新粉丝发了哪些请求、提示走哪个组件、文案是什么），
再把自检断言从"页面文案"改成"落库回读 + 真实提示"，最后按"跑红 → 修 → 跑绿"闭环。

**修掉的产品缺陷（2 个）**

1. **被拦下的删除请求也会写一份回滚快照**（`server/src/routes-channels.ts`）
   `DELETE v2/channels/accounts/:id` 先 `backupBeforeDestructive` 再判 `confirm=1`：
   没有二次确认的请求被拒绝，却已经把 accounts.json / publish-records.json / metrics.json /
   account-groups.json 各复制了一份。修复：快照移到二次确认之后——拒绝的请求不留落盘副作用。
   证据：改前同一轮 A12+A13 留下 3 份 `pre-delete-account-*`（17:54:19.899/.026/.134），
   改后同流程只留 2 份（18:02、18:06）；自检 A12 现在直接断言"被拦下的删除不新增快照"。
2. **登录失效账号照样参与刷新，最后还提示「粉丝数刷新完成」**（`useFansRefresh.ts`）
   按钮旁写着"登录过期账号会自动跳过"，实现只按 `status` 过滤、不看 `loginState`，
   失效账号照样打平台、失败后仍弹"完成"。修复：失效账号不参与刷新；
   全库失效时如实提示「暂无可刷新的正常账号」。
   反向验证（铁律 #16）：把过滤退回"只看 status"重建前端后，A15 立刻报红——
   「账号登录已失效却提示刷新完成（应跳过并如实说明）：抖音粉丝数刷新完成」；恢复修复后转绿。

**自检自身的"假 PASS"（5 类，全部改掉）**

| 现象 | 后果 | 改法 |
| --- | --- | --- |
| 断言写在页面/弹窗文案上（`/抖音|小红书/`、`/粉丝/`） | 频道行没渲染、只剩侧栏平台名也 PASS | 对落库账号逐条比对：行数、昵称、粉丝数、登录态徽标 |
| 用 `[role=dialog]` 取第一个弹窗 | 页面上还有隐藏的接待规则表单，读到的不是频道管理 | 统一用 `[data-testid=channel-manager-dialog]` |
| 刷新粉丝数只查"弹窗里没有报错字样" | 点了没反应也 PASS | 等 `role=status` 通知卡片出现匹配文案，并核对 `lastStatsTime` 前进 |
| 新建分组只查界面有没有那行字 | 只活在前端状态、没落库也 PASS | 建/删都回读 `v2/channels/account-groups`，并校验数量回到初始 |
| 弹窗关闭动画期间 `isVisible()` 仍为 true | 把"正在关闭"当"已打开"，下一步点隐藏按钮 30 秒超时 | `openChannelManager()` 先等动画落定、必要时重试 |

**新增 / 加固的自检项（账号管理专项 18 项全绿）**

- 新增 A9（分组改名+排序，落库回读 rank）、A10（1 小时限频：二次刷新必须命中冷却且不再打平台）、
  A11（`loginState=invalid` 时账号页与频道行都必须显示「需重新登录」）、
  A12（合成账号：未确认删除被拦且无副作用、退出只清 cookie、确认删除留快照）、
  A13（合成账号进自定义分组 → 删组 → 回落 `grp-default`）、
  A14（11 个平台元数据 + 本地化名称 + 抖音/小红书能力声明）、
  A15（全库失效时不刷新并如实提示「暂无可刷新的正常账号」）。
- 加固 A1–A8 与 L2-B1/L2-B2/L3-B：落库回读、真实通知、账号数据不被链路改坏。
- 跑法：`$env:BF_CHAIN_ONLY='A,L2-B,L3-B'; node qa/acceptance/full-chain-loop.mjs`
  → `LAYER_GATE PASS`、`FULL_CHAIN PASS checks=18 blockers=0`（L1 15 · L2 2 · L3 1）。
- 全量四层复跑（同日晚间）：`LAYER_GATE PASS`、`FULL_CHAIN PASS checks=80 blockers=0`
  （L1 65/65 · L2 8/8 · L3 3/3 · L4 4/4；A 21/21 · B 17/17 · C 25/25 · D 2/2；apiFailures=0、pageErrors=0）；
  门禁 `qa/run-gate.mjs` 全绿（[黄] 两项仍是既有的 AC 覆盖率与 S 级监考）。
- 顺带修掉一处自检自身缺陷：L2-C3 直接点「保存参数」，而该按钮在折叠面板里（折叠时不挂载）
  → 改为先展开再点（L1 C15 一直就是这么做的），否则 L2 闸门一红、L3/L4 全都不跑。

**账号数据专项（同日追问"账号数据方面呢？"后的对账）**

- **现状核对通过**（1 个抖音账号 / 15 条发布记录 / 15 条互动指标）：无孤儿记录与指标、无重复平台作品、
  已发布记录都带 `platformWorkId + workLink + publishTime`、记录的 `engagement` 与 `metrics.json`
  逐字段一致、`analytics` 接口值与账号库一致、数据中心作品数 = 已发布记录数。
- **修掉一个"空转的审计"**：`qa/acceptance/data-consistency-audit.mjs` 按裸数组解析落库文件，
  而落库是 `{schemaVersion, value:[...]}` 信封 → 账号/记录/指标全报 0 却"通过"。
  已改为信封解析 + 账号数据四者对账 + 失败退出码 1，跑出 `DATA_CONSISTENCY PASS`。
  新增自检 **A16**（同样的对账进分层自检，账号板块 19 项）。
- **播放量恒 0 = 平台真值**（不是采集 bug）：直接抓抖音创作者页 `work_list` 原始响应，
  `statistics.play_count=0`，而 `digg_count/comment_count/share_count` 都有值。
  但数据中心默认指标就是"浏览量"（`DataStatisticsClient` 默认 `viewGrowth`），首屏因此是一片 0，
  点赞/评论/分享要点一下切换才看得到 → 建议平台未提供播放量时明确说明或改默认指标（待用户拍板）。
- **抖音昵称是平台占位值**：`/web/api/media/user/info/` 返回 `nickname="No name"`，
  服务端回退成平台显示名"抖音"，多账号无法区分 → 建议兜底带平台 uid 尾号（待用户拍板）。
- 探针脚本（均为只读）：`qa/probes/probe-account-data.mjs`（落库↔接口对账）、
  `qa/probes/probe-douyin-raw.mjs`（原始 user/info 与 item/list）、
  `qa/probes/probe-douyin-worklist.mjs`（抓 work_list 原始统计字段）。

**"平台没给却说成 0"的两处诚实化修复（同日，用户要求"遇到问题就解决"）**

1. **播放量**：数据中心接口新增 `metricAvailability`（views/likes/comments/shares/favorites 各一个布尔）；
   一行采样都没有时不下"平台未提供"的结论（返回 undefined，避免把"没采到"说成"平台没给"）。
   前端：概览卡在平台未提供的指标上标注"平台未提供"；默认增长/排行指标自动避开不可用项（改按点赞）；
   用户手动切回「播放/浏览」时显示"平台未提供「播放/浏览」数据（接口返回 0），这里不做估算"。
   实测（共享实例）：`metricAvailability={"views":false,"likes":true,...}`、概览仅播放卡带标注、
   默认无提示、切回后提示出现；可复跑脚本 `qa/acceptance/verify-dashboard-metric-honesty.mjs`
   （临时数据根 + 独立端口，不碰真实数据）→ `DASHBOARD_HONESTY PASS`。
   踩坑：隔离实例是全新数据根，首屏有免责声明弹层，不先关掉它点击会被挡住（脚本已处理）。
2. **抖音昵称占位**：`cleanPlatformNickname` 回退时带上平台 uid（`抖音 · XchanM520`，uid 超 12 位截断）；
   真实昵称不受影响，无 uid 时保持原行为。单点实测四种输入（占位+uid / 真实昵称 / 占位无 uid / 超长 uid）。

新增自检 **A17**（平台没给的指标要如实标注），账号板块因此到 20 项
（`FULL_CHAIN PASS checks=20 blockers=0`，L1 17 · L2 2 · L3 1）。

**重启纪律**：服务端修复要重建 lib 并重启运行时——重启前先确认另一路会话的自检已结束，
否则就是把对方打断（本轮已按此执行）。

**工具改进**：`full-chain-loop.mjs` 新增 `BF_CHAIN_ONLY` 前缀过滤（按板块单跑，层闸门只统计跑过的项）。

**踩到的坑（避免再踩）**

- **同一套自检不能并发跑，更不能在跑的中途重启 dev 运行时**：19:32 的全量复跑跑到内容创作板块时，
  另一会话在 19:37:53 重启了 dev 运行时（进程号变更），自检立刻 `B9 page.goto ERR_CONNECTION_REFUSED`、
  B10/B11 随之失败——这是环境打断，不是产品回归（同轮账号板块 18/18 全绿）。
  另外实测本机同时存在另一路在跑 `full-chain-loop.mjs` 的会话（`c-board-verify.log`，C 板块复测）
  以及上一轮留下的存活 node 进程（各自带一整棵 playwright Chrome）；双方都在建/删分组、切页面，
  本次全量自检的 A3 假红（"分组数量 1 -> 2"）就出现在这种环境里——清掉残留、确认没有别的自检在跑后重跑，A3 立即转绿。
  **跑全量自检前先确认没有第二路自检（含残留 node 与 playwright chrome）。**
- **开发版运行时用后台作业启动时会被作业生命周期带走**：先用 `dev-web.ps1` 起、作业一结束端口就没了；
  改成常驻后台作业启动，并在开跑前显式确认 31280 已就绪。

**文档同步**：`docs/功能清单-三大核心功能.md`（自检 80 项、账号专项 18 项、E4 专项记录）、
`项目铁律.md` 增补 **#16 自检断言必须落到"落库/请求"证据**。

### AI 智能体板块专项测试（2026-09-10 晚间）

**用户要求**：「咱们继续AI智能体板块功能测试！」

**做法**：沿用账号板块那套——先写行为探针 `qa/probes/probe-agent-board.mjs`（接口段 + 浏览器段，
只记录不判分），把「点下去到底发生了什么」看清；再修；修完先用同一个探针复测，最后把结论固化成门禁。

**C 板块剩下的两项 🟡 都是"页面看着能用、其实没生效"**

1. **「停止生成」点了没用**（`server/src/api.ts` + `kernel-client.ts` + 右侧 AI 助手）
   探针证据：生成中调用 `agent/tasks/:id/abort` 后，SSE 仍一路跑到 `done`，任务落库 `completed`、正文 2095 字。
   根因：abort 只改落库状态；正在流式生成的那次随后用自己内存里的任务副本覆盖回 `completed`；
   内核轮询（`kernel.prompt`）也没有中断通道。
   修复：生成期登记控制位 `inFlightAgentTasks`——abort 置位后停止转发增量、内核轮询提前收工并关闭；
   落库 `aborted`，只保留已送达客户端的部分正文并追加「（已中断，以上为已生成的部分。）」；
   右侧助手的「停止」原来只断本地流，现在先通知服务端。
2. **分享链接打不开（HTTP 404）**（前端 `ShareModal.tsx` + `server/src/api.ts` + 存储）
   探针证据：弹窗生成的是 `http://127.0.0.1:31280/chat?token=…`，浏览器打开 404；
   换成应用内 hash 路由 `http://127.0.0.1:31280/bosom-friend/#/chat?token=…` 就正常渲染「仅供查看」。
   根因：桌面端跑在 `/bosom-friend/` 下且是 hash 路由，前端用 `origin + /chat?token=` 拼链接，基路径与 `#` 全丢。
   顺带修掉服务端三处不实：忽略 `ttlSeconds`、`expiresAt` 固定写 7 天、令牌只在内存（重启即失效），
   以及指向私有任务详情页的 `urlPath`；现在有效期按请求生效（1 分钟~30 天，越界回落 7 天）、
   令牌落盘 `share-tokens.json`、过期即失效、`urlPath` 指向只读页。

**新增门禁**：`qa/verify-agent-task-actions.mjs`（18 项）→ 接入 `qa/run-gate.mjs`：
`AGENT_TASK_ACTIONS PASS checks=18 pass=18 fail=0`（应用未运行或模型不可用时退出码 2，判黄灯）。

**证据**：`qa/evidence/REQ-004-分享链接只读页-20260910.png`、
`REQ-004-停止生成后任务维持已中断-20260910.png`、`REQ-004-中断与分享探针-20260910.json`（同一探针的改前/改后记录）。

**文档同步**：`docs/功能清单-三大核心功能.md`（C2-03/C2-04 转 ✅、E2 第 7/8 条、E5 专项）、
`docs/trace/RTM.md`（AC-004-1 → 部分、AC-004-2 → 已覆盖）、`qa/README.md`（新脚本登记）。

**未做 / 待办**
- 前端工程仍未入库：`ShareModal.tsx` / `AIAssistantPanel/index.tsx` 的改动只在工作区与 `dist` 里
  （与 Web 工程 661 个未跟踪文件是同一个问题），需要用户拍板是否入库。
- 分享链接目前只在本机的产品服务上可打开；跨机器分享要公网域名/端口映射，属产品决策。
- 两个会话共用同一工作区：server 源码与功能清单/工作日志同时被两边修改，提交需要先协调顺序。
**复测结果（2026-09-10 19:11，`BF_CHAIN_ONLY='C,L2-C,L3-C,L4-1,L4-2,L4-4'`）**

```
--- L1 闸门：19/19 通过 ---   （C1–C19 全绿）
--- L2 闸门：3/3 通过 ---     （L2-C1 对话+任务 / L2-C2 热点+接待 / L2-C3 知识库+监控）
--- L3 闸门：1/1 通过 ---     （L3-C 对话→动作卡→任务记录）
--- L4 闸门：3/3 通过 ---     （L4-1 智能体→发布弹窗 / L4-2 知识库→注入 / L4-4 设置→对话）
LAYER_GATE PASS（逐层通过） · FULL_CHAIN PASS checks=26 blockers=0 · C AI 智能体 19/19
```

慢检查（`BF_AGENT_BOARD_SLOW=1`，多等 60 秒等令牌真过期）：`AGENT_TASK_ACTIONS PASS checks=20 pass=20 fail=0`——
「分享·1 分钟有效期生效」60s、「分享·过期令牌被拒（区别于不存在）」HTTP 410、
「中断·生成随即停止」中断后 125ms 结束流、「中断·只保留已送达客户端的部分正文」落库 212 字 = 客户端 194 字 + 中断标注。

一键门禁：`[绿] AI 智能体任务动作 AGENT_TASK_ACTIONS PASS checks=18 pass=18 fail=0`（CRUD 34/34、蒸馏 12/12 同绿）；
整份门禁仍是黄灯（AC 覆盖率 21.1% + S 级监考未跑），红灯 0。

**并发踩坑（记下来）**：另一会话同一天重启了 4 次开发版运行时、跑了 3 轮全链路自检，
我的 C 板块复测被打断 2 次（13 项 `ERR_CONNECTION_REFUSED` 假红）；最终等它的自检结束（1815 秒）后一次跑绿。
两个会话共用一个 31280 实例时，跑前端自检前必须先确认没有另一轮在跑。

### AI 智能体板块第二轮：把"还有什么问题"逐条清掉（2026-09-10 晚）

**1. 又修掉两个缺陷（都有探针复现）**

| # | 缺陷 | 归属 | 根因 | 修复 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 不存在的任务也发分享链接 | C2-04 | `agent/tasks/:taskId/share` 不校验任务是否存在就发 token，用户拿到一条永远打不开的地址 | 先查任务，查不到返回 18100，不发链接 | 门禁 `分享·不存在的任务不发链接 code=18100 data=null` |
| 2 | 任务详情刷新后标题回落「新对话」、收藏与评分状态丢失 | C2-02 / AC-014-2 | `useChatState` 里「store 已有消息就短路」把任务详情一起省掉（为流式缓存加的优化），刷新后 `task` 恒为 null | store 有消息时只省消息拉取与轮询，任务详情照拉 | 探针 `probe-task-detail-reload.mjs`：改前同一 URL 刷新 `headerFavorited=false`、标题「新对话」；改后 true + 原标题与实星 |

**2. 门禁扩容：18 项 → 24 项（`BF_AGENT_BOARD_SLOW=1` 时 27 项）**

- AC-005-1 未配置：临时移开 `llm-user.json` 后真实对话，必须回固定提示，且 finally 还原配置并断言已还原；
- AC-005-2 已配置：`result` 事件必须带模型名与真实正文；
- AC-005-3 失败：错误密钥 → 「大模型调用失败」且**不得**回落模板文案；
- AC-014-1 列表/详情的状态与时间字段；AC-014-2 评分/收藏刷新后仍保持（回读 + 落盘双证）；
- SLOW：60 秒真过期 → HTTP 410；关窗口（客户端断开）**不算**中断，答案照样跑完落库。

`AGENT_TASK_ACTIONS PASS checks=24 pass=24 fail=0`（SLOW 模式 `checks=27 pass=27 fail=0`）。

**3. 前端证据补进 `qa/evidence/`**

`REQ-005-未配置固定提示`、`REQ-005-调用失败如实报错`、`REQ-005-已配置真实模型回复`、
`REQ-014-任务记录列表`、`REQ-014-任务详情含评分收藏`、`REQ-014-刷新后标题与评分收藏保持`。
RTM 里 AC-005-1/2/3、AC-014-1/2 由「未覆盖」转「已覆盖」，AC 覆盖率 21.1% → 32.2%。

**4. 还没解决的（如实列出）**

- **AC-005-1 文案不一致**：验收准则写的是「未接入任何大模型」（服务端固定文案），前端未配置时走自己的短路提示
  「您好！当前还没有配置大模型 API…」（`agent.methods.ts:35`）——两者都如实、都不编内容，但字面不同，要不要统一属产品决策。
- **AC-005-3 错误原因丢失**：失败时只显示「大模型调用失败：未知错误」，内核返回的具体原因（如 401）没透出来，正文诚实但不够可诊断。
- **断开语义**已按「跑完并落库」固化并加了断言；若要改成「人走了就停」，在 `res.on('close')` 里也置中断位即可（一行）。
- **前端三处修复仍未入库**：`ShareModal.tsx`、`AIAssistantPanel/index.tsx`、`useChatState.ts` 只在工作区与 `dist`。
- **分享链接只在本机可打开**；跨机器分享要公网域名/端口映射。
- **懒加载 chunk 失效无自愈**：并发期间出现过一次 `Failed to fetch dynamically imported module`（另一会话重建 dist 后旧 chunk 消失），
  ErrorBoundary 如实报错但不会自动重载；打包版更新后也可能遇到，建议加「chunk 加载失败 → 自动刷新一次」。