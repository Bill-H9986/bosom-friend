# Bosom Friend 全量功能测试报告

> 日期：2026-09-04
> 依据：`products/bosom-friend/qa/功能全量测试清单.md`（A~L 共 12 组 + M 最终验收）
> 门禁：`qa/run-gate.mjs` + `qa/acceptance/frontend-supreme-gate.mjs` + `run-supreme.mjs`

## 一、测试结果总表

| 测试层 | 入口 | 结果 |
|---|---|---|
| 静态门禁 | `qa/run-gate.mjs` | 🔴 **红灯**（7 条 AC 未实现 + 覆盖率 6.7%） |
| S 级静态监考 | `frontend-supreme-gate.mjs` | ✅ **PASS**（7 文件 0 违规） |
| 后端 API 冒烟 | `qa/smoke-all.mjs` | 🟡 **20/26 PASS** |
| 大模型文本生成 | agnes-2.5-flash | ✅ 真实调用成功 |
| 大模型图片生成 | agnes-image-2.5-flash | ✅ 真实生成（ai-img-*.png） |
| 大模型视频生成 | agnes-video-2.5-flash | ✅ 真实生成（ai-vid-*.mp4） |
| S 级前端考试·抖音 | `frontend-auto-agent-exam.mjs` | ❌ 卡在自动跳转发布 |
| S 级前端考试·小红书 | 同上（xhs） | ❌ 卡在发布提交 |

## 二、核心发现

### 1. 内容创作链路全通（AI 对话 → 文本/图片/视频生成）
- 文本：`agnes-2.5-flash` 生成完整口播脚本（标题 + 正文 + 话题标签）
- 图片：`agnes-image-2.5-flash` 生成封面图（`ai-img-fe3aeb91.png`）
- 视频：`agnes-video-2.5-flash` 生成视频（`ai-vid-857eff1a.mp4`），任务 `gen-829b6dbe` status=success

### 2. AI 自动发布链路断点（精确对应静态门禁红灯）
- **抖音**：AI 生成内容 + 动作卡后，**停在对话层**，未自动跳转草稿箱 → `AC-018-1`（全对话自动化未实现）
- **小红书**：AI 成功自动跳转草稿箱（`#/draft-box?aiPublish=1`），但**无 `POST publish` 提交**，持续轮询 `customer-reception/pending` → `AC-009`（真实发布自动化未打通）
- **历史对照**：9-02 日志显示小红书曾真实发布成功（作品链接 `xiaohongshu.com/explore/6a96a355...`，数据中心 workCount 28 / viewGrowth 52134），说明发布功能本身可用，是**内核迁移后 AI 自动化断点**

### 3. 账号已登录（非测试阻塞项）
真实数据根 `~/.bosom-friend/bosom-friend/accounts.json`：抖音 `acc-nnpqqueo`、小红书 `acc-ukzk41h7` 均 `status=1` 已登录（含登录 cookie）。

## 三、红灯项清单（发布前必须清零）

| AC | 关联 REQ | 状态 |
|---|---|---|
| AC-009-1 / AC-009-2 | REQ-009 抖音/小红书真实发布 | 未实现 |
| AC-017-1 / AC-017-2 | REQ-017 AI 每步截图入会话 | 未实现 |
| AC-018-1 | REQ-018 全对话自动化 | 未实现 |
| AC-021-2 / AC-021-4 | REQ-021 | 未实现 |

验收准则覆盖率：**6.7%**（45 条 AC 中覆盖 1 / 部分 4）。

## 四、smoke-all 的 6 个 FAIL 归因（非产品 bug）

| 项 | 真实情况 | 归因 |
|---|---|---|
| note agent-collect | 产品返回 12 条，脚本断言 `=== 6` | 测试脚本 bug |
| hot feed | 请求 `itemLimit=5`，脚本断言 `=== 10` | 测试脚本 bug |
| miniapp auth | 脚本传假数据 `loginCode:'x'` | 测试假数据 |
| douyin searchTopic | 产品 `code=0` 正常，话题返回空 | 平台数据 |
| miniapp fans | 断言写死 `=== 7` | 待核 |

## 五、环境修复记录（本轮排查）

1. 依赖未链接 → `pnpm install --no-frozen-lockfile`（lockfile 因内核迁移过期）
2. 旧启动器 `bin.js` 废弃 → 改用 `bin-desktop.mjs`（挂 dsh-base + kernel + desktop-bundle）
3. 源码态半构建 → 改用打包产物 `desktop/dist/kernel-runtime-unpacked` 启动
4. 环境透明代理（`127.0.0.1:61687`）致假 502 → 测试须 `--noproxy` / `NO_PROXY`
5. 大模型配置 → 复用 `~/.bosom-friend/bosom-friend/llm-user.json`（`api.agnes-ai.cn/v1` + agnes 系列模型）
6. 内核子进程路径 → 补齐 `BF_KERNEL_ROOT` 后大模型真实跑通
7. 启动必设：`BF_KERNEL_ROOT`、`BF_FRONTEND_DIST`、`BF_DESKTOP_PORT=31280`；`stdin` 须 `< /dev/zero` 保持

## 六、下一步建议

1. **P0**：修复 AI 自动发布链路（AC-009 + AC-018-1）——这是发布前的硬阻塞，抖音/小红书均断在此处。
2. **P0**：补 AC-017（AI 每步截图入会话）。
3. **P1**：修正 `smoke-all.mjs` 的硬编码断言（agent-collect `===6`、hot feed `===10`、miniapp 假数据），避免误报。
4. **P1**：补 45 条 AC 的覆盖率（当前仅 6.7%）。
5. **P2**：视频生成时长/稳定性调优（agnes-video-2.5-flash 生成耗时长）。
