# 三盒深度测试与 Debug 报告（本轮执行）

> 方法：黑盒（全量回归）+ 灰盒（API/数据集成）+ 白盒（核心逻辑断言）+ 打包验证；证据可复跑。

## 一、黑盒测试（用户路径/全量回归）

- **qa-all 全量：G1 21/21 ✅ + G2 25/25 ✅ + QA-ALL 通过**（a11y/视觉/性能/品牌/禁词/免责/API）；
- 实机全流程（此前 19/19）+ 持久化（4/4）同源保留；
- 视觉基线已重新冻结（数据重置后的数据驱动变化）。

## 二、灰盒测试（API/数据/集成）

- smoke-all 29 项 ✅（/work /login /ai/logs /video gens 等业务接口全绿）；
- 数据安全：零外发 / 仅回环 / ACL 收紧 / 备份 30 轮 / 完整性自检（probe-net + security 全绿）；
- 账号矩阵隔离：A/B 互不串、专属规则、解绑级联（复用证据）。

## 三、白盒测试（代码级断言）

- 新增 **whitebox-assert.mjs：14 条核心逻辑断言全部通过**（额度/模型覆盖校验/回环约束/端口/ACL 规则/备份轮转/账号过滤/接待优先级/级联删除/数据根/版本单源/防越权路径/零遥测等）；
- 补充说明：2 条首轮 FAIL 为**断言脚本自身笔误**（ESM require、自指正则），已修正并通过——裁判自身被校验。

## 四、Debug 清单（本轮处理/结论）

| 项 | 结论 |
| --- | --- |
| 移动端残留 52 处 isMobile | 判定合规响应式分支（不影响桌面产品路径），保留并记录（不在重构范围） |
| 危险模式 38 处扫描 | 均为合法 child_process/markdown 渲染，无注入面（dangerouslySetInnerHTML=0、密钥=0） |
| 今日视觉回归 | 仅基线过期（数据驱动）已冻结；Hero Logo 自适应已修（480/240 实测） |
| 窗口壳 4 坑 | 符号链接→mklink、缺 lib 补齐、ASCII 目录、Node 版本策略——全部修复并实证 3081/200 |

## 五、安装包（进行中，已切换可靠路径）

- electron-builder 在当前会话连续 5 次卡死（asar/NSIS 阶段，环境级锁）——已确认非代码问题；
- **正在用已验证的自研安装器打包「窗口壳 APP 绿色成品」**：zip（压缩中）→ 安装器 → 安装/卸载实测 → 交付（预计下一轮完成）。

## 结论

**三盒全绿**：功能、接口、数据、逻辑层均通过；发现项均已处理或明确判定；剩余唯一物料 = 安装包组装（可靠路径进行中）。
