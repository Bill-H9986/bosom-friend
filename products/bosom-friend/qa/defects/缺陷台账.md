# 缺陷台账（每周更新）

> 这是产品负责人每周必看的三份报告之一。数据来自各 DEF 文件，由登记人同步维护。
>
> **2026-09-10 补登说明**：本轮独立复核时发现本台账长期是空的「（暂无）」——门禁 `run-gate.mjs` 的
> 「缺陷闭环留痕」因此**空过**（没有 DEF 行就没有可校验字段）。这正是"选手兼裁判"最容易漏掉的地方：
> 门禁绿不代表没缺陷，只代表没人登记。下面按工作日志与修复报告把已修缺陷补登入库。
>
> **2026-09-11 补登说明**：全量门禁复跑时「中断」两条断言假红，定向探针查出两件事——
> 门禁断言时序有缺陷（DEF-023），产品在收尾窗口里确实会把 `aborted` 覆盖回 `completed`（DEF-022）。
> 两条当场修掉，并用「还原旧写法必判红」的负向对照证明回归断言真的能判红。

## 当前快照

| DEF编号 | 标题 | 严重度 | 状态 | 缺陷类型 | 根因归类 | 关联REQ | 回归用例 | 责任人 | 计划版本 | 重开次数 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DEF-001 | 「停止生成」点了没用：abort 只改落库状态，流式生成继续跑完并覆盖为 completed | P1 | Fixed | 功能 | 并发/中断控制位缺失 | REQ-004 | `qa/verify-agent-task-actions.mjs`（中断真的停 + 只留已送达正文） | Jay | 0.2.40 | 0 |
| DEF-002 | 分享链接在浏览器打开 404（origin+/chat 丢掉 /bosom-friend 基址与 # 路由） | P1 | Fixed | 功能 | 前端路由基址拼接错误 | REQ-004 | `qa/verify-agent-task-actions.mjs`（链接指向应用内只读页） | Jay | 0.2.40 | 0 |
| DEF-003 | 不存在的任务也能生成分享链接（拿到永远打不开的地址） | P2 | Fixed | 功能 | 缺少任务存在性校验 | REQ-004 | `qa/verify-agent-task-actions.mjs`（不存在任务返回 18100） | Jay | 0.2.40 | 0 |
| DEF-004 | 任务详情刷新后标题回落「新对话」、收藏与评分丢失 | P1 | Fixed | 功能 | store 短路优化连带跳过任务详情拉取 | REQ-014 | `qa/probes/probe-task-detail-reload.mjs` + 门禁 AGENT_TASK_ACTIONS | Jay | 0.2.40 | 0 |
| DEF-005 | 登录失效账号照样参与刷新，最后仍提示「粉丝数刷新完成」 | P2 | Fixed | 功能 | 过滤只看 status 不看 loginState | REQ-003 | 分层自检 A15（全库失效时如实提示并跳过） | Jay | 0.2.40 | 0 |
| DEF-006 | 被拦下的删除请求（未二次确认）也写了一份回滚快照 | P2 | Fixed | 数据 | 快照早于确认判断执行 | REQ-003 | 分层自检 A12（被拦下的删除不新增快照） | Jay | 0.2.40 | 0 |
| DEF-007 | 删除分组把默认分组删了、被删分组反而留下，账号变孤儿 | P1 | Fixed | 数据 | DELETE 只读查询串，ids 为空导致过滤条件反转 | REQ-003 | 分层自检 A3 + A13（删组后账号回落 grp-default） | Jay | 0.2.40 | 0 |
| DEF-008 | 平台没给播放量却显示 0（首屏一片 0，用户以为数据没采到） | P2 | Fixed | 诚实性 | 缺少指标可用性声明，默认指标选到不可用项 | REQ-010 | `server/test/functional/dashboard-honesty.spec.ts`（三态 + `zeroOnlyCount` 口径）+ `qa/e2e/specs/dashboard-honesty.spec.ts`（页面必须显示原因）+ 分层自检 A17 | Jay | 0.2.40 | 0 |
| DEF-009 | 抖音占位昵称「No name」被回退成平台名，多账号无法区分 | P3 | Fixed | 功能 | 未用平台 uid 兜底 | REQ-003 | 分层自检 A6 + 单点实测（占位/真实/无 uid/超长） | Jay | 0.2.40 | 0 |
| DEF-010 | 桌面端启动页可能永远停住（端口被占时 EADDRINUSE，握手失败后不再加载产品页） | P1 | Fixed | 功能 | 启动分支缺少接入既有实例与失败兜底 | REQ-020 | 实测截图（两种占用场景）+ main.cjs isProductServing 分支 | Jay | 0.2.40 | 0 |
| DEF-011 | 内核运行时解压压在首次启动（417MB/23 万文件），自定义 NSIS 钩子文件名不被 electron-builder 识别而从未生效 | P1 | Verified | 打包/低配 | 资源钩子命名不符合 electron-builder 约定 + 目录约定不一致 | REQ-021 | 静默安装实测：`BosomFriend-Setup-0.2.40.exe /S` exit 0，安装期解出 `%APPDATA%\Bosom Friend\kernel-runtime` **199,779 文件**且 `runtime\bin-desktop.mjs` 存在，首启不再自解压、退出无残留进程（工作日志-2026-09-10 §10.3 + `def011-app-window.png`）；2026-09-11 复核该目录仍在 | Jay | 0.2.40 | 0 |
| DEF-012 | 自检假绿：把「还没有配置大模型 API」的固定提示当成真实模型回复判 PASS | P1 | Fixed | 测试有效性 | 守卫正则与实际提示文案不一致 | REQ-005 | 分层自检 C1 / L2-C1 / L3-C / L4-4（补全 4 处守卫） | Jay | 0.2.40 | 0 |
| DEF-013 | 「接待记录」页签恒为空：唯一写入方 POST customer-reception/handle 没有任何调用方 | P3 | Fixed | 重复功能 | 同一业务两套记录存储（logs 与 replies） | REQ-013 | 分层自检 C11（旧页签不存在 + 待办处理在全局监控） | Jay | 0.2.40 | 0 |
| DEF-014 | 缺陷台账长期为空，门禁「缺陷闭环留痕」空过 | P2 | Fixed | 流程 | 登记纪律未执行，门禁对空表无感知 | REQ-021 | 本表补登 + 门禁解析 DEF 行（本轮独立复核发现） | Jay | 0.2.40 | 0 |
| DEF-015 | 播放量采集丢失（同批 15 条作品 09-02 有 13016/10329/…，09-10 全 0）前端却标「平台未提供」 | P1 | Fixed | 诚实性 | 平台本次 work_list 返回 play_count=0；服务端用「全 0」倒推平台未提供，缺语义分级 | REQ-010 | A17（重写为会判红的断言）+ `metricAvailabilityReason` 三态；实测 views=not-collected-this-sync | Jay | 0.2.40 | 1 |
| DEF-016 | analytics 读接口改写 lastStatsTime 造成「假新鲜度」（09-07 曾修，本轮复发） | P2 | Fixed | 诚实性 | GET 内无条件写 lastStatsTime 并触发采集，读接口产生落盘副作用 | REQ-003 | 隔离实例 31281：两次 GET 时间戳不变、accounts.json 哈希不变 | Jay | 0.2.40 | 1 |
| DEF-017 | 「粉丝数刷新完成」不代表真的刷新过（失败被 `.catch(()=>{})` 静默吞掉） | P1 | Fixed | 诚实性 | 前端按 `code===0` 判成功；服务端吞异常并先回缓存值 | REQ-003 | 新 POST `analytics/refresh`：未接入/Cookie 损坏 → code 50000 真原因；真实采集才推进 lastStatsTime | Jay | 0.2.40 | 0 |
| DEF-018 | GET `/agent/tasks/:id/rating` 返回 `data.data` 双重包装 | P3 | Fixed | 契约 | 服务端多包一层；前端已用任务详情兜底 | REQ-014 | `qa/verify-agent-task-actions.mjs` 28 项：评分回读走单层 `data.rating` 且显式断言 `data.data === undefined`；不存在的任务如实回 18100 | Jay | 0.2.40 | 0 |
| DEF-021 | A17 自检定位器范围过大：全页取值把「平台效率」板块（硬编码 viewGrowth、如实提示「本次未采到」）也算进来，导致断言必红 | P3 | Fixed | 测试有效性 | 测试侧缺陷，非产品缺陷——集成代理实测：增长趋势/作品排行默认已避开（选中「点赞」，`DataStatisticsClient.tsx:745-753`） | REQ-010 | 收敛 A17 locator 到「增长趋势/作品排行」两个板块，保持断言强度；`BF_CHAIN_ONLY=A17` 必须 PASS | Jay | 0.2.40 | 0 |
| DEF-020 | 自检 A16 的「已更新 N/M 条作品数据」把全 0 占位行也算作已更新（GA-17） | P3 | Fixed | 诚实性 | 计数未区分「真值」与「全 0 占位」：只按 workId 命中就计入已更新 | REQ-010 | `qa/acceptance/verify-dashboard-metric-honesty.mjs`：从隔离数据根现算期望值并断言 `updatedCount=12 / zeroOnlyCount=3 / totalCount=15`；已接入 `run-gate.mjs`「数据中心指标诚实化」 | Jay | 0.2.40 | 0 |
| DEF-022 | 「停止生成」在收尾阶段被覆盖回「已完成」：正文流完、封面/动作卡还在生成时点停止，abort 先落库 `aborted`，收尾代码又用生成开始时的状态快照把终态写回 `completed`，并照旧挂上发布动作卡 | P1 | Fixed | 功能 | 终态取自生成开始时的快照，收尾阶段到达的中断不参与判定（DEF-001 同族窄窗） | REQ-004 | `qa/verify-agent-task-actions.mjs` 收尾窗口三项（落库 aborted / 正文标注已中断 / 不挂动作卡）；负向对照：把快照式写法还原后这三项判红（checks=27 pass=24 fail=3）；另加 `qa/acceptance/verify-abort-timing-matrix.mjs` 五个时间点扫描（终态序列不得出现 aborted→completed），旧写法下第 3 个时间点判红（checks=24 pass=20 fail=4） | Jay | 0.2.40 | 0 |
| DEF-023 | 门禁「中断」断言时序假红：等首个增量 + 600ms 才停止，模型一次吐完整段时停止落在生成结束之后，被误判成产品没中断 | P2 | Fixed | 测试有效性 | 断言假设「600ms 后必然还在生成」，且没有校验这次停止是否真的落到生成中 | REQ-004 | `qa/verify-agent-task-actions.mjs`：三次机会（600/0/0ms）重试，以 `abort` 回报的 `data.interrupted` 判定，停晚了不判红、三次都没赶上如实 SKIP；修复后复跑 27/27 绿 | Jay | 0.2.40 | 0 |
| DEF-024 | 内容创作首屏被测试素材组顶成空组：验收脚本建的「持久化素材组」被插到队首，App 默认选中列表第一项 → 用户打开内容创作「什么都没有」，增删改查（含批量删除）都无从下手 | P1 | Fixed | 数据/测试污染 | 默认素材组既没有排序也没有标记；`POST contents/groups` 新建组插队首，前端在无效选中时取 `list[0]`；`qa/smoke-all.mjs` 建的临时组从不清理，直接落在真实数据根 | REQ-007 | `qa/acceptance/verify-batch-delete.mjs` 前两条（默认素材组排第一 + 首屏落在它上面）；负向对照：去掉排序修复后首屏落到空组（cards=0） | Jay | 0.2.40 | 0 |
| DEF-025 | 内容创作「批量删除」没有端到端验收：09-02 的标签页脚本断言绑死旧按钮名（批量移除）与旧路由（media/ids），界面改名后脚本早已腐烂且无人重跑，坏了没人发现 | P2 | Fixed | 测试有效性 | 断言绑定文案/路由等易变细节，腐烂后不进任何门禁 | REQ-007 | `qa/e2e/specs/content-crud.spec.ts`（自建 1×1 PNG 靶子 → 页面发起 kind=asset 的 DELETE → 服务端确认删的正是靶子 → 服务端总数 -1 → 重新进入不复活）+ `server/test/functional/api-contract.spec.ts`（删除语义/路由表）；原先的 `qa/acceptance/verify-batch-delete.mjs` 因写死 playwright-core 安装路径腐烂，已删除 | Jay | 0.2.40 | 0 |
| DEF-026 | 全局监控右上角两个按钮外观一模一样，且主按钮可能「静默无效」：「刷新」与「立即轮询」用同一个图标紧挨着；引擎正在跑一轮时 poll-now 直接早退，接口回 code 0 却什么都没发生、页面零提示；真触发后一轮真实采集要 40~90 秒，期间页面毫无变化 | P2 | Fixed | 交互/诚实性 | 两个动作视觉不可区分；触发接口不回报是否真的触发；触发后没有盯盘闭环，用户只能靠猜 | REQ-013 | 实测：修正前 rounds 停在 403 不动且无提示；修正后 `POST poll-now` 回报 `triggered`，进行中再点提示「接待引擎正在跑这一轮」，触发后 84 秒自动刷新为「已完成轮次 406」并提示「本轮轮询已完成，结果已刷新」；截图 `qa/evidence/全局监控-两个按钮已区分.png`、`全局监控-轮询完成自动刷新.png` | Jay | 0.2.40 | 0 |
| DEF-027 | 在内容创作按刷新（或重开窗口）会回到首页：URL 被改写成只剩 `?planId=`，hash 路由整段丢掉 | P2 | Fixed | 功能 | `buildPlanUrl` 用 `history.replaceState` 写 `pathname?query` 时没有带上当前 hash；路由只活在内存里 | REQ-001 | `qa/e2e/specs/content-crud.spec.ts`：刷新后断言 URL 仍含 `#/draft-box` 且「批量管理」可见 | Jay | 0.2.40 | 0 |
| DEF-028 | 每次启动把浏览器里的模型配置副本整份回灌服务端：一次局部同步就把设置页配好的多服务配置冲掉（此前只在服务端加兼容分支兜住，根因没除）；每次 AI 请求还额外带上这份副本，服务端两套配置相争 | P1 | Fixed | 设计 | 模型配置有 4 份副本（llm-user.json / settings.yaml+.credentials.yaml / localStorage / 桌面主进程 electron-store），前端把它那份当权威、启动即整份 PUT | REQ-005 | `qa/e2e/specs/llm-config.spec.ts`：种一份"老用户本地副本"再启动，断言**不发 PUT**、服务端 provider 数量/生效服务/接口地址均不变；负向对照：还原旧写法后该用例判红（`带着本地副本启动也不得回灌服务端`） | Jay | 0.2.40 | 0 |
| DEF-019 | 落盘数据编码损坏第 3 次复发：中文被写成 `?` | P1 | Fixed | 数据/工具 | **根因是测试工具**：会话内 `tools.pwsh` 为 Windows PowerShell 5.1，`Invoke-WebRequest -Body <字符串>` 用 ASCII 编码请求体；产品写入端无缺陷 | REQ-021 | `assertNoAsciiFallback` 接入 A12/A13/L2-C3/L4-2 四处中文往返断言；落库 `??` 扫描 0 处 | Jay | 0.2.40 | 2 |
| DEF-029 | 图片分辨率回退成正方形像素档：与视频档位不一致，用户选的比例作用不到成品（P-012/P-013 回退） | P1 | Fixed | 代码（回归） | 档位表 RATIO_TIERS 无调用方、生成写死 size=1024x1024、resolution 不读 imageSize；无门禁断言"图片档位=视频档位" | REQ-001 | `server/test/functional/content-params.spec.ts`（档位一致 / 唯一成品像素 / 1080p+9:16=1080x1920）+ `qa/e2e/specs/content-params.spec.ts`（门槛 10/11/12）+ 真实生成实测 ai-img-ddcae155.png=1080x1920 | Jay | 0.2.41 | 0 |
| DEF-030 | 源图不可解码时裁剪谎报成功，成品指向一个不存在的文件 | P1 | Fixed | 代码（诚实性） | 成功判定只看 stdout 含 OK；PowerShell 无 ErrorActionPreference/try-catch；未校验产物是否落盘 | REQ-001 | `server/test/functional/content-params.spec.ts`：坏源图必须返回空串（该用例即发现者） | Jay | 0.2.41 | 0 |
| DEF-031 | 频道白名单只有小红书/抖音：引擎已落地的快手进不来，闲鱼在产品里完全没有定义 | P2 | Fixed | 需求遗漏 | 前端手写白名单与引擎能力清单两份数据无一致性来源；缺"已列入但未落地"的中间状态 | REQ-003 | `server/test/functional/content-params.spec.ts`（快手 available / 闲鱼 coming_soon）+ `qa/e2e/specs/content-params.spec.ts`（连接列表四张卡 + 目标平台不含闲鱼）+ 直接请求闲鱼授权如实回"暂未开放"且不拉引擎+ `qa/acceptance/full-chain-loop.mjs` A5 白名单断言 | Jay | 0.2.41 | 0 |
| DEF-032 | 备份轮转删不掉旧备份时异常冒到插件 `apply()`，开发实例启动直接失败（源码注释却承诺"失败只记录不阻断"） | P1 | Fixed | 代码（回归） | 轮转 `rmSync` 无护栏，也无成功/失败留痕；本机数据根刚好攒到 30 份上限，等于每次启动都要删一份 | REQ-021 | `server/test/functional/security-backup.spec.ts`：真实子进程占用最老备份目录 → 删除被拦下、启动不抛异常、`security.json` 如实记 `backupPruned=0`/`backupPruneError`；负向对照还原无护栏写法必判红（`EPERM ... backups\<ts>`） | Jay | 0.2.41 | 0 |
| DEF-033 | 频道里的快手点了没反应：产品加了白名单，但 worker 固定传参与引擎 `*_setup` 的 `handle=False` 默认值冲突，扫码流程根本不会启动 | P1 | Fixed | 代码 | worker 对 11 个平台固定传同一套登录参数：只导出 `*_setup` 的平台默认 `handle=False` → 没 cookie 就直接返回「cookie 文件不存在或已失效」；而 alipay/weibo 的 `*_cookie_gen` 又没有 cdp_url 参数（固定传会 TypeError）。能力清单靠源码正则扫描，扫得出"有登录函数"，扫不出"参数对不对" | REQ-003 | `qa/probes/verify-channel-login-start.mjs`：对 KWAI / wxSph / xianyu 各走一遍真实产品链路（auth → qr），三张二维码都是真图（125x125 / 470x470 / 148x148），证据落 `qa/evidence/二维码-*.png` | Jay | 0.2.41 | 0 |
| DEF-034 | 模型配置被整份清空（providers 变 []，只剩图片/视频残留），用户配好的服务与密钥从界面消失 | P0 | Fixed | 数据/设计 | 说得出的一环：`PUT ai/user-llm` 的两条路径（多服务 providers 分支、旧式单配置分支）都会**无条件接受清空**——任何脚本探针、自动化、旧客户端一次空提交就能把用户配置带走；说不清的一环：18:10 那次写入的确切发起方无法从现有日志确定（前端打开设置页只发 GET，已用探针证明） | REQ-005 | 服务端两条路径都加硬护栏：已有服务时清空必须显式 `allowEmptyProviders` / `clear`，否则报错且不落盘；`server/test/functional/content-params.spec.ts` 两条用例（静默清空被拒 + 显式确认放行） | Jay | 0.2.41 | 0 |
| DEF-035 | qa-all 的门禁链整条失效：qa-guard / qa-guard2 把 playwright 安装路径写死，依赖树一变就 `MODULE_NOT_FOUND`，而且失效方式看起来像"没跑"——品牌/无障碍/视觉/性能四道防线实际已黑了不知多久（`last-guard1.json` 停在 8/31） | P2 | Fixed | 环境集成/测试有效性 | 脚本硬编码 `node_modules/.pnpm/playwright-core@1.61.1/...`；门禁报告对"脚本跑不起来"与"检查没做"不加区分 | REQ-021 | 新增 `qa/lib/browser.mjs` 按"谁装了就用谁"解析（qa/e2e → 仓库根 → 前端依赖），三个门禁脚本改用它；修后 qa-guard **23/23 PASS**（修前 20/21 BLOCKED），qa-guard2 **22/25**（三条红已逐条定位：monitor 对比度+label / accounts 规则表 ant-tag 对比度 3.39 / knowledge 新增 page-tabs 导致布局指纹变化，均非本轮引入） | Jay | 0.2.41 | 0 |
| DEF-036 | 内核握手没有超时：内核子进程活着但不回应时，启动页永远停在「等待初始化握手」，用户既进不去也看不到原因（实测等 150 秒仍无任何提示，也没有失败出口） | P1 | Fixed | 功能/可靠性 | 未加界的等待：`client.initialize()` 既没有超时也没有取消；子进程存活但协议无响应时 Promise 永不 settle，catch 分支永远到不了，navigateToProduct 的兜底也轮不到执行 | REQ-021 | 门禁「内核握手超时」`qa/probes/verify-kernel-handshake-timeout.mjs`：预算 8000ms 时实测 8041ms 抛出「内核初始化握手超时（8 秒内没有收到内核响应）」；启动页场景 B（`verify-splash-progress.mjs --cold`）断言失败页出现可读原因且「重试启动 / 完全退出」可见 | Jay | 0.2.42 | 0 |
| DEF-037 | 清理旧版内核运行时残留会卡死启动页：pruneStaleKernelRuntimes 用同步 fs.rmSync 递归删除，一份运行时 417MB / 约 23 万个文件，实测堆到 8 份共 8.9GB，机械盘上删除期间主进程事件循环被占满，窗口显示与 IPC 全部停摆 | P1 | Fixed | 性能/低配 | 同步递归 IO 放在启动关键路径上：该函数不是 async，调用点写 await 也不会让出事件循环，而注释却承诺「不该拦住启动」 | REQ-021 | `qa/probes/verify-perf-profile.mjs` 结构断言「启动链路没有同步递归删除目录」（扫描 main.cjs 里 rmSync(..., recursive: true)，为 0 才通过）；删除改为 fs.promises.rm 并按「第几份 / 共几份」上报真实进度 | Jay | 0.2.42 | 0 |
| DEF-038 | 不确定态进度显示自相矛盾：不确定态把进度条填充拉满、却把百分比文字冻结在旧值，界面上出现「6%」配一条满格条纹——正是本轮要消灭的「看起来像满进度」 | P2 | Fixed | 诚实性 | 渲染层把「不确定」实现成样式替换（width:100% !important + 条纹），并顺手跳过了百分比文字更新，UI 的两个信号各说各话 | REQ-001 | `qa/probes/verify-splash-progress.mjs` 场景 B 两条显示诚实性断言：百分比文字必须等于 aria-valuenow、填充长度不得超过真实进度（本轮自测截图发现，修复前必判红） | Jay | 0.2.42 | 0 |
| DEF-039 | 门禁「专业 E2E 套件」假红：整套连跑时 AC-018-1 用例超时判红，单跑同一条却 5/5 绿；同时门禁给套件的整体预算是 15 分钟，而套件实测 11~15 分钟，模型稍慢就被整体杀掉并报成「退出码 null：存在失败用例」——超时被写成了产品红 | P2 | Fixed | 测试有效性 | 用例超时写死 120 秒，而 AI 对话用例要发 1~3 句真实对话（单条实测 25 秒 ~ 1.5 分钟，整套连跑时机器已热、延迟更高）；门禁侧又把「进程被超时杀掉」（status=null）与「用例失败」（status=1）混为一谈 | REQ-018 | agent-conversation.spec.ts 按真实调用耗时设 `test.describe.configure({ timeout: 300_000 })`（不放松断言）；run-gate.mjs 的 E2E 预算 15 → 30 分钟并写明超时与失败的区分。复跑：该 spec 单跑 5/5 绿（含原判红那条 1.3 分钟），整套复跑见本轮门禁报告 | Jay | 0.2.42 | 0 |
| DEF-040 | 主进程承诺的「内核运行时自解压兜底」永远不可能生效：安装器解压完会删掉 resources/kernel-runtime.zip（省 417MB），而 main.cjs 的注释与错误文案都写着「缺入口就从 zip 自解压」。用户端表现：%APPDATA%\Bosom Friend\kernel-runtime 一旦丢失（清理软件、手工删、安装期解压中断），应用永远打不开且只有一句「内核运行时缺失」，唯一出路是重装 | P2 | Fixed | 功能/诚实性 | 安装器与主进程对「zip 是否随包留存」的假设不一致，且兜底分支永远走不到、也没人验证过它 | REQ-021 | 注释改为真实契约（兜底仅在 zip 仍存在时可用）；缺 zip 时抛出可执行动作「请重新运行安装包完成修复安装（BosomFriend-Setup-*.exe），再打开本程序」。验收：安装版场景 `verify-splash-progress.mjs --packaged` 8/8（真实安装包冷启动走完进度并进产品页）；探针同时修掉自己引入的隔离错误（改 --user-data-dir 会让应用找不到安装期解压目录） | Jay | 0.2.42 | 0 |
| DEF-041 | 门禁「专业 E2E 套件」第 2 次假红：AC-011-2 断言「页面上已有搜索结果」，依赖上一条用例残留的搜索状态——整套连跑时前面有清理动作、页面回到空态就必红，而单跑（残留状态还在）恒绿。本轮整套连跑红、单跑 23.4 秒绿，两次结果都记在案 | P2 | Fixed | 测试有效性 | 用例复用了「上一次搜索留下的结果」，而不是自己走「输入关键词 → 搜索」的用户流程；外部全网搜索服务不可用时也没有如实的出口 | REQ-011 | ac-verification.spec.ts AC-011-2 改为自己输入关键词并点搜索；外部服务不可用时 test.skip(true, 原因) 如实跳过（不冒充通过）；结果统计 / 原文链接 / 导出下载三段断言保持不变。复跑：单条 31.5 秒绿；整套结果见最终门禁报告 | Jay | 0.2.42 | 0 |
| DEF-042 | 卸载不是真的无残留：卸载钩子只删 kernel-runtime* 与 Cache/Code Cache/GPUCache/Dawn* 几个具名目录，应用跑过之后 `%APPDATA%\Bosom Friend` 仍留 127 个文件（Local Storage / IndexedDB / Service Worker / Network / Preferences / perf-profile.json / gpu-crash.json），而验收准则 AC-021-2 写的是「卸载无残留」 | P3 | Fixed | 交付/诚实性 | 卸载清理按「我记得的那几个目录」罗列，而不是按「这个目录整体属于可重建产物」判断；加上应用的 Chromium profile 是运行后才生成的，安装即卸载的测法测不出来 | REQ-021 | installer.nsh 的 customUnInstall 补 `rd /s /q "$APPDATA\Bosom Friend"`（用户数据 `%USERPROFILE%\.bosom-friend` 明确保留）。复测：安装 → 启动并使用（userData 200,182 文件）→ 卸载 → 安装目录/桌面/开始菜单/注册表清零、`%APPDATA%\Bosom Friend` 目录不存在（0 文件）、进程 0、数据根 8,075 文件且 accounts.json 哈希不变 | Jay | 0.2.42 | 0 |
| DEF-043 | 用图片生成视频不管用：用户加的参考图从头到尾没进媒体生成链路，成片是模型按文案另画的图拼的，跟用户那张图没有任何关系 | P1 | Fixed | 功能 | `request.imageUrls` 只落盘留痕：`attachGenerationMedia` 的 request 参数类型里根本没有它，`generateAiVideoDirect` 也只发 `mode:'text'` 的纯文生视频请求 | REQ-001 | `server/test/functional/video-reference.spec.ts`：截下真实厂商请求，断言 `mode=keyframe` 且 `first_frame` 是用户那张图的 Data URI（字节逐位相等）、成片落盘可播；无参考图时必须不带任何图片字段。厂商契约用真实探测确认（`POST /v1/videos` + Data URI 首帧 → HTTP 200） | Jay | 0.2.43 | 0 |
| DEF-044 | AI 智能体没有发布指令也会发布：用户只说「写一篇小红书笔记」，智能体照样产出「去发布」动作卡，跟随模式自动点掉 → 内容被直接发到平台 | P0 | Fixed | 功能/诚实性 | 动作卡闸门用的是 `/(小红书\|抖音\|发布\|创作\|生成\|种草\|笔记)/` ——「创作/生成/笔记」全在里面，发布与创作被当成同一件事；且只有"模型拒答/调用失败"两道护栏，没有"用户有没有要求发布"这一道 | REQ-004 | `server/test/functional/agent-publish-intent.spec.ts`：走真实 `POST agent/tasks` SSE 通道（内核桩），断言「写一篇笔记」不产出 `navigateToPublish` 且正文如实标注"尚未发布"、明确说「发布」时必须产出该卡；`qa/verify-agent-rules.mjs` 新增"闸门必须接在 api.ts 上"两条结构断言 | Jay | 0.2.43 | 0 |
| DEF-045 | 装好的应用完全打不开：启动页停在 80%「加载产品界面」，错误框只有一句 `spawn tar.exe ENOENT`，重试无效、只有「完全退出」能退出（现场 4 核 / 7.9GB 机器，v0.2.42） | P1 | Fixed | 功能/可靠性 | 兜底解压直接 `execFile('tar.exe')` 靠 PATH 解析，且从不判断 tar 到底在不在；在 Windows 上「系统没有 tar.exe」「tar.exe 不在进程 PATH 上」「进程当前目录不存在」三种情况报的是同一句 ENOENT，现场无法区分 | REQ-021 | 新增 `desktop/electron/kernel-extract.cjs`：解压阶梯＝系统 tar 绝对路径 → PATH 上的 tar → 随包便携 Python（`python -m zipfile -e`）；每次 spawn 显式给 cwd；解压成功后才删 zip（原来成功后 417MB 常驻）；失败文案不再出现 libuv 原文。`qa/probes/verify-kernel-extract.mjs` 22 项全绿（含真进程正负对照：真 zip 解开 / 假工具名必须抛人话），已挂门禁检查 5o「内核运行时兜底解压」。随包 Python 深路径实测：352 / 459 字符的条目都能解开（真实运行时最深相对路径 233 字符）。现场恢复工具 `qa/probes/repair-kernel-runtime.ps1` 三分支实测（无 zip 如实报错 / 真解压 PASS / 重跑幂等） | Jay | 0.2.43 | 0 |
| DEF-046 | 安装期内核运行时解压失败是静默的：安装器只打一行 DetailPrint 就继续，用户装完以为成功，几分钟后第一次启动才发现打不开 | P2 | Fixed | 交付/诚实性 | customInstall 把解压失败当可忽略事件；也没有「这台机器到底有没有 tar.exe」的前置判断，失败原因要等用户首次启动才暴露，而那时用户面对的是一句看不懂的英文错误 | REQ-021 | `installer.nsh` 改成与主进程同一阶梯：先 `IfFileExists "$SYSDIR\tar.exe"`，缺失或失败改用随包 Python，两条都失败才 `MessageBox` 明确告知（静默安装不弹窗、只留日志）并说明 zip 会保留到首次启动。探针断言安装器阶梯与可见提示两处结构；**0.2.48 装包实测已补**：新增 `qa/probes/verify-installed-package.ps1 -Install` **13/13 PASS**（包内解压器解包内 zip 200,537 文件 3.6 分钟 / 静默安装 exit 0 / 注册表 `Bosom Friend 0.2.48` / 安装器把内核运行时解到位 / 无 `kernel-extract.log`），见 `qa/reports/release-0.2.48-20260915.md` | Jay | 0.2.43 | 0 |
| DEF-047 | 门禁「内核工具可执行」把"仓库产物未构建"直接判红：任何人 clean 一次工作区（dist/lib 都是可清理产物）门禁就满屏红，几次之后红灯没人看；而同一份工作区里「内核握手超时」遇到"缺少内核入口产物"判的是黄——同一个原因两种颜色 | P2 | Fixed | 测试有效性 | 门禁把"环境未就绪"与"代码有缺陷"混成同一个颜色；产物依赖型检查没有区分"产物不在"与"产物在但坏了" | REQ-021 | run-gate 检查 5j 改为：产物缺失 → 黄并给出构建命令，产物在但工具 execute 抛错 → 红。负向对照：临时把 kernel/lib 移走跑门禁，「内核工具可执行」由红转黄（gate-20260914-153806.md）；恢复后转绿（gate-20260914-154*.md） | Jay | 0.2.43 | 0 |
| DEF-048 | 门禁「数据编码完整性」把文档里的举例当成真损坏：知识库收录 项目铁律.md 后（#9.5 用 \`?\`、\`??????\` 举例说明损坏长什么样）判定 DATA_INTEGRITY FAIL，1 处误报 | P3 | Fixed | 测试有效性 | 判据 /\\?{3,}/ 直接打在整串上，没有区分"正文里的长问号串"与"代码片段里的示例" | REQ-021 | check-data-integrity.mjs 判定前先剥掉行内代码与围栏代码块。正负对照：只放文档举例 → PASS；放回真损坏（正文里的 `你好??????世界`）→ 仍 FAIL hits=1；真实数据根 → PASS | Jay | 0.2.43 | 0 |
| DEF-049 | 回滚安全网整条丢失：基线钉住的 5 个回滚安装包（0.13.5~0.2.0）连同上版的 0.2.41 一起被仓库整理清掉（原本放在 <仓库根>/release/），门禁「安装包只读保护」转红，本机一度没有任何可回滚的安装包 | P2 | Fixed | 交付/流程 | 安全资产存放在"每次整理都会被清扫"的目录里；删除没有走既定的"先改基线、明确放弃回滚能力"流程；.exe 未进 git，删除在历史里无痕迹 | REQ-021 | 回滚网口径改为"当前出货版本 + 上一版本"，存放在仓库外 C:\\Users\\Jay\\BosomFriend-Releases（0.2.42 已入库、复制后 SHA256 复算一致）；基线新增 dir 字段、只钉现存版本，5 个包移入 retired（保留指纹与退役原因）；门禁支持 dir 且目录不在位判黄；R8 定义同步。决策留痕 qa/reports/cleanup-2026-09-14-retired-rollback-net.md | Jay | 0.2.43 | 0 |
| DEF-050 | 厂商视频轮询预算比厂商真实耗时还短：`generateAiVideoDirect` 写死 180 秒，而厂商一条 4~7 秒成片实测要 33s / 125s / 160s（工作日志另记过 4m07s）——超时即返回 null，调用方静默改用本地幻灯片兜底，用户拿到的"AI 视频"其实是图片拼的 | P1 | Fixed | 功能/诚实性 | 轮询预算照"感觉够快"写死，没有按厂商实测耗时标定；超时路径与"厂商失败"共用同一个 null 返回值，"等不及"于是被当成了"生成不了" | REQ-001 | 提升为命名常量 `VIDEO_POLL_BUDGET_MS=600000` 并注明实测依据；`server/test/functional/digital-human-real.spec.ts`（真配音 + 真 Agnes + 真 ffmpeg）在 180 秒预算下第 2 段必红（`[AI_VID_ERR] poll timeout`），提到 10 分钟后同一条用例产出 8.99 秒成片（1,705,270 字节） | Jay | 0.2.43 | 0 |
| DEF-051 | 打包链的 repack 脚本随构建产物一起被清掉：`repack_kernel.py` 原先住在 `desktop/dist/`，而 `dist/` 会被整理动作整目录删除，2026-09-14 出 0.2.43 时它已不存在——第 4 步「重打内核 zip」必然失败，且失败点是"脚本找不到"而不是"打包出错"，不查日志只会以为压缩坏了 | P2 | Fixed | 交付/流程 | 打包脚本与构建产物放在同一个目录：产物目录天生会被清理（知识库 01 §46 已经写过这条教训，但脚本自己踩了同一个坑） | REQ-021 | 重写到源码侧 `desktop/repack_kernel.py`（带长路径 \\?\ 前缀处理与逐文件失败即中止），`rebuild-kernel-and-installer.ps1` 第 4 步改为引用该常驻路径；本轮 0.2.43 用它产出 366.1MB / 200,064 条目的 zip 并验证包内命中 | Jay | 0.2.43 | 0 |
| DEF-052 | 装好的应用完全打不开（0.2.43 与 0.2.44 两个包都受影响）：内核运行时里缺 `msedge-tts`，内核加载抛 `ERR_MODULE_NOT_FOUND`，启动页停在 80%「等待产品服务就绪」直到超时。用户侧表现与 DEF-045 一模一样，根因完全不同 | P0 | Fixed | 交付/流程 | 出包流程第 2 步只把**代码**（lib）同步进内核运行时，**依赖**不会跟着走——运行时是更早一次 `pnpm deploy` 的产物。给服务端新增运行时依赖后不重新物化运行时，装出来的包必然打不开，而第 2 步看上去一切正常 | REQ-021 | 新增 `desktop/verify-runtime-deps.mjs` 并接进 `rebuild-kernel-and-installer.ps1` 第 2b 步：按 server 的 dependencies 逐条核对运行时 node_modules，缺一个就在出包前 throw；本次缺的 32 个包已补进运行时。装机实测：0.2.45 服务 **34 秒**就绪（0.2.44 等 653 秒仍失败），导航出现「长视频创作」「我的数字人」，更新日志 4 条齐全 | Jay | 0.2.45 | 0 |
| DEF-053 | 启动器入口声明指向已被删除的源文件：`launcher/package.json` 的 `bin`/`files` 写 `lib/types/bin.js`，而 `src/bin.ts` 早已随重构改为 `bin-desktop.ts`，且没有任何构建步骤产出旧路径 | P2 | Fixed | 交付/流程 | 重构改名后只改了源码，没同步 package.json 与依赖该路径的 QA 脚本；这类失效没有任何编译期信号，只在跑开发态门禁时才暴露 | REQ-021 | 补 `build:desktop` 脚本（esbuild 产出 `lib/bin-desktop.mjs`）；`bin`/`files` 与 `qa/run-isolated.mjs`、`qa/probes/verify-splash-progress.mjs` 一并改到当前入口 | Jay | 0.2.47 | 0 |
| DEF-054 | 删除接口成功返回值被改成对象，破坏既有契约：0.2.46 把 `DELETE contents/:id` 的成功载荷从 `true` 改成 `{deleted:1}`，E2E 按 `data === true` 判定，AC-006-2/AC-007-2 因此变红 | P1 | Fixed | 交付/流程 | 为满足"不许假成功"而顺带改了成功载荷的形状——诚实性只需要失败分支说真话，成功分支的形状是别人依赖的契约 | REQ-008 | 改回 `writeOk(res, true)`，失败分支保留非零码与原因；`delete-honesty.spec.ts` 增加"成功仍回 true"断言把契约钉死 | Jay | 0.2.47 | 0 |

| DEF-055 | 装好的应用**第二次**打不开（0.2.47，同一台 4 核 / 7.9GB 机器）：错误框从一句 `spawn tar.exe ENOENT` 变成「内核运行时解压失败（PATH 上的 tar.exe 失败…；随包 Python 失败（Command failed: …））」，重试无效 | P1 | Fixed | 功能/可靠性 | 解压链路的"保底"（随包 Python）同样过不了 MAX_PATH 260：这份 zip 最深相对路径 232 字符，解到 `%APPDATA%\Bosom Friend\kernel-runtime` 之后最深 289 字符，而 Windows 长路径支持默认关闭（`LongPathsEnabled=0`，且该值按进程缓存），Python 的 zipfile 与 libuv 都只在"进程 longPathAware **且** 开关为 1"时才越过 260。上一轮"随包 Python 深路径实测 352/459 字符通过"是在开发机上做的，而那台机器 `LongPathsEnabled=1`——把环境特性当成了产品能力 | REQ-021 | 新增 `desktop/electron/kernel-unzip.cjs`（纯 Node：ZIP64 + 中央目录 + 逐条 CRC + zip-slip 防护 + 显式 `\\?\` 前缀，CLI 供安装器与现场修复）；`kernel-extract.cjs` 阶梯加入该保底，并用 `probeLongPaths()`（在目标目录实写一条 >260 的路径）决定顺序；每一步以 `runtime/bin-desktop.mjs` 是否在位判定成败；`installer.nsh` 在 Python 之前调用同一份解压器（`electron-builder.yml` 用 extraResources 放到 resources 根）。验收：`verify-kernel-extract.mjs` 39 项全绿（含 233 字符条目 / 290 字符绝对路径的真解与回读、退出码 0 但入口缺失不算成功、损坏 zip 可读原因、CLI 契约）；真 zip 200537 条目解压约 3 分钟且最深条目可读 | Jay | 0.2.48 | 0 |
| DEF-056 | 解压失败的原因在错误框里被丢掉了：现场只看到「随包 Python 失败（Command failed: <一整行命令行>）」，真正的 Python 异常（`FileNotFoundError` / `WinError 3`）从未出现在用户面前，也没有任何落盘留痕，于是既看不懂也查不了 | P2 | Fixed | 诊断/诚实性 | `execFile` 的失败消息**首行是完整命令行**、原因在后续 stderr 行里，而 `kernel-extract.cjs` 的取首行与 `main.cjs` 的 `sanitizeKernelError` 两处都只保留首行——叠加起来把唯一有用的一行扔掉；失败事实也没有任何本地日志 | REQ-021 | `describeExtractFailure` 改用 stderr **末行**（异常落点）并截断 200 字符，`ENOENT` 仍统一表述为"缺少解压组件"；主进程把解压计划、每一步的来源与真实原因、长路径探测结果写进 `%APPDATA%\Bosom Friend\kernel-extract.log`，错误框保持脱敏（AC-021-3）。验收：探针断言 Python 风格 stderr → 文案里出现 `FileNotFoundError` 且不含命令行首行；主进程结构断言含 `writeExtractLog`/`failureReason` | Jay | 0.2.48 | 0 |

| DEF-057 | "装完就能用"里还留了一步必须用户自己做的事：0.2.48 解决了内核运行时解压，但 Windows 长路径支持默认是关的，得让用户去跑修复包或手动 `reg add` —— 按项目铁律 #3（需要命令行/改配置才能完成的步骤一律按"未完成"算），这等于产品没交付完 | P2 | Fixed | 交付/诚实性 | 需要管理员权限的动作被留给了用户：安装器是免管理员的 per-user 安装（`perMachine: false` + `allowElevation: false`），于是"开长路径"这件事没有任何产品内的落点 | REQ-021 | `installer.nsh` 在解压前检测 `LongPathsEnabled`，为 0 时 `ExecShell "runas"` 提权执行 `reg add`，**并轮询注册表直到真读到 1 才算成功**（命令跑过不算），30 秒未生效则写日志 + 如实提示；静默安装不弹 UAC。三层验证：makensis 编译通过、NSIS 逻辑副本实测（拒绝分支 30 秒终止不挂死）、真注册表 0→1 实测（`branch=enabled seconds=1`）；0.2.49 装包实测 `verify-installed-package.ps1 -Install` 13/13 PASS | Jay | 0.2.49 | 0 |
| DEF-058 | 内核 zip 里的依赖「在包里却解析不到」：pnpm 11 的部署用 Windows 目录联接点挂依赖，zip 存不了联接点（解压后顶层包下只剩 `.bin`），而**展开成真实目录也不够**——Node 按真实路径解析，展开后 `.pnpm/<hash>/node_modules` 这个兄弟层不在祖先链上，内核启动报 `Cannot find package 'js-yaml'` / `'@deepseek-ai/cosmokit'`，装出来的包完全打不开 | P0 | Fixed | 交付/可靠性 | 出包链每一环都假设「运行时是一棵普通目录树」（0.1.1 时代的 pnpm 部署确实如此），pnpm 11 换成联接点+真实路径解析后没有任何一步验证「解压出来能不能跑」，只有「条目数/字节数对得上」这种弱证据 | REQ-021 | `qa/probes/handshake-kernel.mjs`（对解压结果发真实 initialize 握手，已接进出包流程第 6 步）+ `repack_kernel.py` 的 `dsh-app-boot` 依赖解析自检 + `verify-runtime-deps.mjs`（按 Node 规则从服务端每份物理副本逐级解析）。负向对照：未补齐扁平层的展开树握手必红（`Cannot find package '@deepseek-ai/cosmokit'`），补齐后 PASS。同轮还查出出包第 2 步在 pnpm 11 下必然失败：顶层没有 server 包会直接 throw，且部署产物与工作区构建输出是**硬链接**（实测 nlink=10），就地 `Copy-Item -Force` 报 `being used by another process`——改成只写真实副本 + 先删后拷 | Jay | 0.2.50 | 0 |
| DEF-059 | 0.1.5 换基座后**桌面主进程的内核启动链整条停在 0.1.1 契约上**：开发态启动页停在 80% 并弹「Cannot find package '@deepseek-ai/dsh-sdk-client'」，而安装版跑的是旧运行时（0.1.1）反而 8/8 PASS，把问题掩盖了过去 | P0 | Fixed | 交付/兼容性 | 换基座只改了 server 的内核客户端（`kernel-client.ts`），Electron 主进程的 `kernel-host.cjs` 仍在用旧契约（`command`/`args`/`cwd`）；同时两条解析规则没跟着改——**裸包名按配置文件所在目录解析**、**pnpm 11 部署产物顶层只挂部署根的直接依赖** | REQ-021 | `desktop/electron/smoke-host.cjs` 实测 `M2_KERNEL_HOST_OK {"serverInfo":{"name":"deepseek-harness-sdk-runtime","version":"0.0.1"}}`（修复前是 `M2_KERNEL_HOST_FAIL … Cannot find package`）；开发态入口与随包入口统一为 `bundle/kernel/runtime/bin-*.mjs`；`bundle/kernel` 补 `dsh-sdk-client` 与 `dsh-bosom-friend-server` 两条直接依赖 | Jay | 0.2.50 | 0 |
| DEF-060 | 「端口隔离」类验收假红：桌面 bundle 补丁把 webServer 端口**写死 31280**，而主进程、并行 QA 与 `qa/probes/verify-splash-progress.mjs` 都用 `BF_DESKTOP_PORT` 做隔离（场景 A 默认等 31299）——服务其实起来了，探针却永远等不到那个端口 | P2 | Fixed | 测试有效性 | 端口这个「部署可变量」写死在补丁里：env 只被主进程用于**连接**，没被服务端用于**监听**。把补丁里的端口改成 `!!js Number(process.env.BF_DESKTOP_PORT ?? 31280)` 后实测服务**仍绑 31280**（补丁 config 的端口没生效，原因待查），故先还原并如实记开 | REQ-021 | 待补：查明补丁 config 未生效的原因后，断言「BF_DESKTOP_PORT=31299 时只有 31299 在听」。另记一条观察：用全新 `BOSOM_FRIEND_HOME` 启动时 75 秒内没有监听，真实数据根下 60 秒内已监听 31280——首次运行初始化可能吃掉探针的等待预算。同一根因还牵出「内核握手超时」这盏红灯：该探针要求应用连 31297（`BF_HANDSHAKE_PORT`），而服务端仍在 31280 上，应用于是**快速失败**而不是按预期走到超时分支（2026-09-16 门禁实测 `KERNEL_HANDSHAKE FAIL checks=3 fail=3`，实际 1368ms 返回）。**真因**（第二轮查明）：不是「补丁 config 没生效」，而是**注入副本按 (名字, 版本, peer 集合) 缓存**——改了 `cordis.patch.yml` 却没提版本号，`pnpm install` 不会刷新 `.pnpm/<id>/` 里的注入副本，运行时加载的仍是旧配置（实测：改文件后 install，注入副本原封不动；desktop-bundle 0.2.0→0.2.1 再 install，副本立刻变成新内容）。修法：端口改成 `!!js Number(process.env.BF_DESKTOP_PORT ?? 31280)` + 提版本号刷新副本；并把这条隐式规则变成红线——新增 `desktop/verify-runtime-config-drift.mjs`（逐字节比对三个补丁层与部署根入口，接进出包链第 3 步之后），另加端口隔离实测（`BF_DESKTOP_PORT=31299` 时只有 31299 在听：`PORT_ISOLATION PASS`） | Jay | 0.2.51 | 0 |
| DEF-061 | 出包第 2 步把 `kernel/lib` 拷成一个**文件**而不是目录：先 `Remove-Item $destLib`、再 `Copy-Item -Path <src>\* -Destination $destLib`，而 `kernel/lib` 里只有一个 `index.js`——PowerShell 把不存在的目标当成**新文件名**，于是运行时里出现 `node_modules/@deepseek-ai/dsh-bosom-friend-kernel/lib`（33899 字节的文件），内核启动报 `Cannot find module '...\lib\index.js'` | P1 | Fixed | 交付/工具 | `Copy-Item -Destination <不存在路径>` 在「单个源项」时是改名语义；这个坑只在源目录恰好一个文件时出现（`server/lib` 有 124 个文件，所以一直都正常，只有 kernel 踩中） | REQ-021 | 修法：拷贝前 `New-Item -ItemType Directory -Path $destLib` 建目录再拷；**抓到它的正是本轮新加的第 6 步**（解压后真握手：zip 195,471 条目解压后握手报 `Cannot find module`，而第 2 步自己报的是 synced） | Jay | 0.2.50 | 0 |
| DEF-062 | 批量删除确认框显示英文键名（task.batchDeleteConfirmTitle / common.cancel / common.delete） | P2 | Fixed | 代码（国际化） | 键在 `chat` 命名空间里不存在，i18next 缺键返回键名本身，`|| 兜底文案` 永远不执行；`common.*` 前缀写法在 `chat` 命名空间下两级都落空 | REQ-014 | 语言文件补 10 个键；确认框按钮改用 `t('actions.delete')`/`t('actions.cancel')`（经 fallbackNS 解析）；浏览器实测截图（新增键在 zh-CN 与 en 两侧同名存在，逐键核对） | Jay | 待定 | 0 |
| DEF-063 | 音色只能看名字不能听："女声·温柔"这类标签无法作为选择依据 | P2 | Fixed | 需求遗漏 / 设计 | 能力出口按调用方切分：`synthesizeSpeech` 把"合成"与"读时长（要 ffmpeg）"绑成不可分动作，纯播放场景被迫背上 ffmpeg 依赖，于是没人做试听 | 长视频创作 | 新路由 `GET digital-humans/voices/:voiceId/preview`（按需合成 + 按音色/文本缓存 + 返回 audio/mpeg）；`probeDuration` 选项让试听不依赖 ffmpeg；`server/test/functional/long-video.spec.ts` 四条（content-type=audio/mpeg / 同音色复用缓存 / 未知音色当场拒绝 / 全部音色可试听）+ 截图 `qa/evidence/lv-fix-2026-09-16/02-*.png` | Jay | 待定 | 0 |
| DEF-064 | 长视频语速太慢、讲解内容太少：稿件按目标秒数要求，模型写不出那么长 | P1 | Fixed | 设计 / 代码 | 缺"目标 → 可执行要求 → 结果校验"闭环：只给模型"约 N 秒"（模型对秒数没有尺度感），写完也不校验长度；字数不够配音只能拖长音 | 长视频创作 | 目标折算字数下限并入提示词 + 短稿补写一次（不循环）+ 稿件字数落盘；`long-video.spec.ts` 三条（12/30/120 秒的段数与字数下限、短稿必补写、够长不补写） | Jay | 待定 | 0 |
| DEF-065 | 长视频画面与内容不搭且偏少、生成记录找不到、等待过程完全看不到 | P1 | Fixed | 设计 / 功能 | 把"生成"实现成前端会话内的一次动作：段数在写稿之后才定（画面数量没人负责）、每段固定要 12 秒画面（短文案被拉成慢镜）、页面从不读后端任务列表（列表接口无调用方） | 长视频创作 | 段数前移进体裁要求（"刚好 N 句/一句一个镜头/不许重复场景"）+ 每段画面按文案体量给时长 + 进页面即读「生成记录」并可回填继续看；截图 `qa/evidence/lv-fix-2026-09-16/01-*.png` | Jay | 待定 | 0 |
| DEF-066 | 产品与卖点只有一句大白话可用，没有专业提示词增强 | P3 | Fixed | 需求遗漏 | 只给了输入，没给输入的辅助：需求只写"填产品与卖点"，没有写"用户写不清楚时怎么办" | 长视频创作 | 新路由 `POST long-videos/enhance-topic`（保留原意 + 不许编造 + 只出文本）+ 前端增强按钮（只回填、不自动生成）；`long-video.spec.ts` 两条 | Jay | 待定 | 0 |
| DEF-067 | 设置里存的图片模型名无权限，图片/封面生成每次都 403 静默失败 | P1 | Fixed | 代码（配置校验缺失）/ 环境集成 | 厂商的 `/v1/models` 是**全平台清单**，可用性由 **key 的团队白名单**决定；产品把两者当同一件事，保存配置时不做权限校验 | REQ-001 | `qa/probes/agnes-probe.mjs` 实测 403 `team_model_access_denied` 且错误文案含可用模型列表；修法：保存/测试连接时按 key 校验 + 错误信息带厂商原文。**已修**：用户配置 `imageModel` → `agnes-image-2.1-flash`（改前备份 `llm-user.json.before-image-model-fix-20260916-143421`），并用真调用验证 `qa/probes/verify-image-model.mjs` → HTTP 200 + 图片落盘 988,625 B | Jay | 待定 | 0 |
| DEF-068 | 图像端点 `image` 字段返回 200 却静默忽略参考图（图生图不可用） | P1 | Fixed | 环境集成 / 设计（能力假设未验证） | 厂商把"参数合法"与"参数生效"分开：非法字段明确 400，`image` 被静默吞掉；而我们的校验只看状态码，照着开源方案（关键帧链式）设计时把"字段被接受"当成了"能力可用" | REQ-022 | 对照实验固化进探针（参考图带 `NOTHING`/`4821`，带图 vs 不带图），证据 `qa/evidence/agnes-probe-2026-09-16/01~03`；一致性改走视频侧多参考图（实测锁人成功，证据 `04~08`） | Jay | 待定 | 0 |

## 每周指标（每周复盘时填写）

| 周 | 新增 | 关闭 | 存量 | P0/P1存量 | 重开率 | 线上泄漏 | 同根因重复 | 趋势判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-W36 | 0 | 0 | 0 | 0 | — | — | — | 初始 |
| 2026-W37 | 28 | 28 | 0 | 0 | 0%（均为首次登记） | 0 | 1（DEF-013 与 DEF-002 同属"两处真相源"） | 台账填实；DEF-022/023 本轮发现并当场修复；DEF-011 转 Verified |
| 2026-W37（补） | 5 | 5 | 0 | 0 | 0%（均为首次登记） | 0 | 2（DEF-063/065 同属"能力出口按调用方切分"；DEF-064/066 同属"目标没有翻译成可执行要求"） | 用户 2026-09-16 看图报障（4 张截图 6 个问题）当场查出 5 条并修复，均已补回归用例；同根因重复在第 2 次，未达升级线（第 3 次触发架构整改） |

## 待办

- ~~DEF-018 / DEF-020~~ 已在本轮真修（2026-09-11 下午）：rating 读接口改单层 data 并对不存在任务回 18100；「已更新 N/M 条」只算采到真值的作品，另出 `zeroOnlyCount` 如实报出全 0 占位。两条都已进 `run-gate.mjs`。
- 门禁「验收准则覆盖率」：09-13 已到 87.8% 以上，发布前必须补到 100%（剩余是「需要真实模型额度 / 干净机器 / 上游闲鱼发布能力」的条目）。
- DEF-036 / DEF-037 / DEF-038 是 2026-09-13 低配适配 + 启动页进度条这一轮查出并修复的，均已接入门禁。
- 台账为空→黄灯的兜底已实现在 `qa/run-gate.mjs`（本轮复核生效），原待办关闭。
- **国际化缺键没有门禁**（DEF-062 的根因）：本轮只补了键，门禁还没加。建议在 `qa/run-gate.mjs` 增加一条静态检查——扫描前端源码里 `t('a.b')` 的字面键，逐个到语言文件里查是否真的存在（`common` 命名空间按 fallbackNS 规则查），缺失即判红。
