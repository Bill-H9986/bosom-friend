# Bosom Friend 全量前端审计（2026-09-01）

> **2026-09-02 修正**：本文件 2026-09-01 的“✅ 可正常生成”只证明
> `gen-e74020a7` 一次成功，不能代表全部生成场景。2026-09-02 深度审计发现：
> 9 条生成记录中有 1 条 success 但无媒体、3 条 success 但正文为“调用失败”，
> 且 28 个资源引用未进入 `media.json`。因此旧结论不再作为交付判据，
> 最新结论以 `release/深度隐形BUG审计-2026-09-02.md` 为准。

执行方式：全部操作在 Web 前端页面完成；每次操作后截图，文件位于
`tools/live-flow/shots/`。判据：必须有页面可见结果 + 页面自身发出的请求 +
平台真实返回值，禁止后端口径代替。

## A. 内容创作：图片/视频生成（✅ 可正常生成，已修正判读）

- 正确操作：草稿箱 → 输入提示词 → 点工具栏「打开提示词编辑器」保存 →
  点工具栏右侧圆形箭头提交按钮（`draftbox-ai-submit-btn`）。
  注意：文字「生成草稿(视频)」只是模式选择，不是提交按钮；此前审计点错按钮导致误判。
- 实测记录 `gen-e74020a7`：status=success，generatedBy=ai，
  model=`video:agnes-video-2.5-flash`，2 张真实 AI 图 +
  真实视频 `ai-vid-87476c9a.mp4`（提交后约 58 秒完成）。
- 截图：`probe-composer.png`、`gen-success-toolbar.png`。
- 结论：内容创作 UI 可以生成真实图片/视频，后端能力已接通。

## A2. 创作参数与模型选择（✅ 本轮修复）

- 实测：分辨率/风格/比例/时长/数量/平台 控件都能点开并修改；
  改 1080p/16:9/30 秒后提交请求真实携带
  `resolution=1080p, aspectRatio=16:9, duration=30`（参数生效）。
- 修复：模型选择器此前只有“本地视频脚本模板”，现在把用户配置的
  `agnes-image-2.5-flash`/`agnes-video-2.5-flash` 前置为可选模型，
  名称带“（用户模型）”。证据：`shots/toolbar-user-models.png`。
- 开发规则：`项目铁律.md` 新增铁律 #3 普通人视角、#4 说到做到禁止阳奉阴违、
  #5 智能体每步截图会话反馈、#6 发布走平台接口 + 平台存在感。

## B. 账号登录/退出（部分无法自动化）

- 账号读取正常：小红书 SKYC-重庆机长、抖音两个账号均可在账号页展示（cookie 有效）。
- 抖音同步实测成功：返回 15 条真实作品。
- 全新扫码登录需要用户手机扫码，无法自动化；「退出/解绑」需要破坏临时账号，
  未在本轮执行，避免影响后续测试（可单独安排）。

## C. 数据中心实时数据（部分通过）

- 截图：`06-c1-datacenter-before.png`、`07-c2-datacenter-after.png`。
- 同步请求已发出；页面计数没有新增变化，因为当前没有新的真实发布成功
  （发布链路未闭环），历史作品数据会在同步后更新。
- 结论：数据读取/同步能工作；“数据中心不更新”的表象来自没有新发布，
  不是数据表损坏。

## D. AI 智能体操作 + 发布（部分通过，发布 FAIL）

- 严格版考试（每步截图 + 必须有发布请求 + 必须有真实作品 ID）：
  `auto-xhs-step-00..08-*.png`、`auto-douyin-step-00..08-*.png`。
- 已有改善（本轮已修复并截图验证）：
  - 「去发布」不再因为路由参数丢失而弹窗永不出现；
  - 发布请求真实发出：`POST /v2/channels/publish/flows` 返回 code=0；
  - 不再“没发布也跳到数据中心显示成功”；
  - 小红书上传引擎真实执行约 77 秒。
- 仍 FAIL 的硬伤：
  - 发布引擎（sau_cli）上传后不回传作品 ID/链接，`state.json` 只有 done，
    系统无法确认成功（小红书本次诚实标为失败，绝不假成功）；
  - 抖音用户确认通道接口未接入（现明确报 50100 错误）；
  - 抖音需用户本人扫码一次才能验证端到端。

## 本轮修复归档（已写入代码/协议）

1. `EXAM_PROTOCOL.md`：操作后截图铁律，缺少截图判 FAIL。
2. `frontend-auto-agent-exam.mjs`：每步截图 + 发布请求硬校验 + snapshots 清单。
3. `DraftBoxCore.tsx`：发布交接改为 sessionStorage，与路由参数解耦。
4. `DraftContentModule/index.tsx`：只有真实发布成功才跳数据中心。
5. `usePublishActions.ts`：等待平台返回真实作品 ID 才判成功。
6. `routes-channels.ts`：抖音确认通道未接入时显式报错。

## 铁律 #7：AI 实事求是（✅ 已实现并实测）

- 未配置 API：固定回答“未接入任何大模型。请先在「设置 → 配置大模型」中填写你的
  API 地址和密钥……”，不再编造内容。证据：`shots/factual-noapi.png`。
- 已配置 API：真实调用大模型并如实回答。证据：`shots/factual-withapi.png`。
- 调用失败：如实返回“大模型调用失败：<原因>”，不再用模板文案冒充成功。

## 平台发布引擎（小红书）联调进展

- 已验证平台发布链路整体可通：APP 真实发布过一篇笔记，平台返回真实作品 ID
  `6a96a355000000000d0248e0`，链接可打开，数据同步正常。
- 已修复引擎三处：新页面需先点「上传图文」才出现上传控件；
  发布按钮在新版页面是 Shadow DOM 的 `<xhs-publish-btn submit-text="发布">`；
  补上穿 Shadow DOM 点击与成功响应信号，并修复 expect_response 异常泄漏。
- 剩余问题：点击后“发布成功”确认仍不稳定（发布按钮内部点击未触发提交），
  下一步需 dump shadowRoot 内部按钮结构后确定触发方式。

## 待修复（平台对接级，非界面补丁）

1. ✅ 生成草稿弹窗：已找到正确提交入口并实测通过（圆形箭头提交按钮）。
2. ⚠️ 发布引擎回传作品 ID：已实现（发布后立即回读最新作品写回 state.json）；
   小红书本次实跑返回真实错误“发布按钮已尝试 3 次仍未确认成功”，
   说明平台页面/风控仍在上传器上阻拦，属于平台适配问题，非假成功。
3. ❌ 抖音 H5 确认通道：尚未实现，需用户扫码 + 平台方案接入。
