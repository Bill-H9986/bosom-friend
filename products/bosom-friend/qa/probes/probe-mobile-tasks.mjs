import { createRequire } from 'node:module'
const require = createRequire(import.meta.url)
const { chromium } = require('C:/Users/Jay/Desktop/Bosom friend APP/node_modules/.pnpm/playwright-core@1.61.1/node_modules/playwright-core')
const browser = await chromium.launch({ channel: 'chrome', headless: true })
const page = await (await browser.newContext({ viewport: { width: 390, height: 844 } })).newPage()
await page.addInitScript(() => { localStorage.setItem('bosom-friend-disclaimer-accepted-v1', 'true') })
await page.goto('http://127.0.0.1:3081/bosom-friend/#/tasks-history', { waitUntil: 'networkidle', timeout: 180000 })
await page.waitForTimeout(9000)
const info = await page.evaluate(() => ({ text: document.body.innerText.slice(0, 200), dialogs: document.querySelectorAll('[role=dialog]').length }))
console.log('MOBILE-TEXT:', JSON.stringify(info))
await browser.close()
