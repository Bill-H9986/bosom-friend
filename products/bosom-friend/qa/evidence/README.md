# 前端验收证据库

## 规则

1. 只放只读证据（截图、报告、日志、页面状态导出），不手改。
2. 命名：`REQ-xxx-描述-YYYYMMDD.png/json/log` 或 `DEF-xxx-描述-YYYYMMDD.png`。
3. 每份证据必须在 RTM.md 或缺陷台账里被引用；孤立的证据文件无效。
4. S 级验收要求：业务动作必须由前端页面发起、结果在页面可见；后端 code=0、日志、数据库写入不能单独作为通过证据。
