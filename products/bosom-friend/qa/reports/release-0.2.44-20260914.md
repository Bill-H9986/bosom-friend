# 发布记录 · Bosom Friend 0.2.44（2026-09-14）

> 0.2.43 出包后用户回报三条问题，本版把它们一并修掉再出包，供外部试用。

## 安装包

| 项 | 值 |
| --- | --- |
| 文件 | `products/bosom-friend/desktop/release/0.2.44/BosomFriend-Setup-0.2.44.exe` |
| 桌面副本 | `C:\Users\Jay\Desktop\BosomFriend-Setup-0.2.44.exe` |
| 大小 | 976,512,202 字节（931.3 MB） |
| SHA256 | `931C0B00EB85C460F1A7059B83E42D1041010E18BEE68596E988234959324510` |
| 构建时间 | 2026-09-14 19:24（electron-builder exit 0） |

## 本版内容（相对 0.2.43）

| 用户回报 | 处理 |
| --- | --- |
| 每次更新没有记录 | **新增「更新日志」**：`notification/list` 现在带出 `server/src/changelog.ts` 里的真实发布记录。此前通知中心的「更新日志」页签结构完整、却永远空着——没有任何代码往 `notifications.json` 写过东西。 |
| 任务记录不能批量删除 | **新增批量删除**：`DELETE agent/tasks`（按 taskIds 批量、回真实条数）+ 列表页「批量管理 → 全选 → 删除选中」；删任务时一并回收它的分享链接，不留打不开的只读页。 |
| 删除草稿要切换页面才成功 | **修掉一族的诚实性缺陷**：三条删除路由原先一律回 `code: 0`（`writeOk(res, idx >= 0)`），删没删到都说成功——用户看到"已删除"而列表没变，分不清是没删掉还是没刷新（历史 P-005/P-006 同族）。现在删不到就回非零业务码并说明原因，批量删除回真实条数。**"必须切换页面"的确切机制仍未复现，见下方未闭环项。** |

## 包内实测（拆包验证）

| 检查 | 结果 |
| --- | --- |
| 内核运行时 | `kernel-runtime.zip` 366.2 MB / **200,080 个条目** |
| 更新日志模块 | `changelog.js` 在包内；`0.2.44`、`0.2.43` 两版记录与正文文案全部命中 |
| 删除诚实性 | `routes-content.js` 含「草稿不存在或已被删除」 |
| 任务批量删除 | `api.js` 含「这些任务不存在或已被删除」 |
| 前端批量管理界面 | 构建产物中含「批量管理 / 删除选中 / 退出批量管理」文案 |
| 随包 ffmpeg | 61.5 MB，含 libass/freetype/libx264/aac（字幕前提） |

## 未闭环（如实记录）

- **「删除草稿要切换页面才成功」没有复现**：这台机器上应用未运行，无法跑真实页面；
  本次修的是同一族的"假成功"缺陷（删不到却回成功），但用户描述的确切触发路径
  （点的是哪个删除入口、有没有弹提示、提示写的什么）尚未确认。**需要用户补齐复现信息。**
- **仍未做装机实测**：本机装的是 0.2.42，直接安装会覆盖用户当前可用环境。
- **仍未跑 `run-gate.mjs`**：门禁多项检查要求开发版在 31280 运行，且构建与门禁不能并发。

## 本轮新增的回归用例

`server/test/functional/delete-honesty.spec.ts`（9 例）：删除不存在的草稿/素材组/任务必须回非零码、
批量删除回报真实条数、重复删除如实报不存在、删任务级联清分享链接、更新日志随包且最新一版在最前。
服务端全量：**14 文件 / 99 例通过**（真机 e2e 1 例默认自跳过）。
