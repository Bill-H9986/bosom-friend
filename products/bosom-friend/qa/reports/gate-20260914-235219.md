# 门禁红绿灯报告

- 时间：2026-09-14T15:52:19.044Z
- 执行方式：--full（含 S 级前端验收）
- Git 提交：3cf1ac0c1171e2919cb734fedf29469d5741ef2c（工作区有未提交改动，本报告只对这份工作区有效）
- 执行人：Jay
- 主机：DESKTOP-2OHKO6T
- 结论：红灯：禁止合并 / 发布

## 检查结果

### [绿] DSH 内核组合（启动器）

dsh-base 已挂载，未发现 dsh-web-app / dsh-client-*。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] DSH 内核入口（并行新路径）

内核入口组合正确：dsh-base + dsh-bosom-friend-kernel，无 Web UI。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] 禁止直连模型接口

未发现直连 chat/completions。

### [绿] 依赖边界（禁 DSH Web UI 包）

未发现 dsh-web-app / dsh-client-ui 系列依赖。

### [绿] 追溯结构完整性

所有已登记 AC 行结构合规。

### [绿] 功能完整性（未实现清零）

无未实现功能。

### [黄] 验收准则覆盖率

覆盖率 95.8%（覆盖 44 / 部分 4 / 总数 48），发布前必须 100%。

### [绿] 缺陷闭环留痕

已登记缺陷 52 条，关闭字段合规。

### [绿] 数据编码完整性

落盘数据无编码损坏。

```
DATA_INTEGRITY PASS root=C:\Users\Jay\.bosom-friend\bosom-friend
```

### [绿] 安装包只读保护

1 个回滚安装包与基线一致（存放目录 C:/Users/Jay/BosomFriend-Releases）。

### [红] 模型配置链路

退出码 1。

```
  [PASS] 打包主进程文件存在（6 个）
  [PASS] 打包主进程不持有第二份模型配置 — 唯一权威：服务端 llm-user.json → kernel-client 注入 AGNES_API_KEY
== 5/5 内核真的注册了这条路由 ==
  [FAIL] 内核接受路由 bf-bf-verify — TransportClosedError: JSON-RPC input closed
exit code: 1
stderr tail:
node:internal/modules/cjs/loader:1520
  throw err;
  ^

Error: Cannot find module 'C:\
== 已恢复运行前的模型配置 ==
LLM_CONFIG_GATE FAIL (1)

```

### [绿] 业务域 CRUD 四步

五个业务域增/查/改/删 + 落盘 + 空态全部通过。

```
CRUD_MATRIX PASS checks=34 pass=34 fail=0
```

### [绿] 知识库蒸馏链路

检索命中、注入留痕、蒸馏样本、JSONL 导出全部通过。

```
KNOWLEDGE_DISTILL PASS checks=12 pass=12 fail=0
```

### [绿] AI 智能体任务动作

中断真的停、继续能接上、分享有效期与只读页全部通过。

```
AGENT_TASK_ACTIONS PASS checks=28 pass=28 fail=0
```

### [红] 服务端功能测试

退出码 1：      Tests  1 failed | 94 passed | 5 skipped (100)。

```
 FAIL  test/functional/delete-honesty.spec.ts > 更新记录：每版都能在通知中心看到 > 通知列表带出更新日志，且最新一版在最前
AssertionError: 最新一版应排在最前: expected 'v0.2.46｜删除更可靠' to contain '0.2.43'
Expected: "0.2.43"
Received: "v0.2.46｜删除更可靠"
 ❯ test/functional/delete-honesty.spec.ts:106:41
    104|     const logs = res.data.filter(item => item.type === 'changelog')
    105|     expect(logs.length, '至少要有一版更新记录').toBeGreaterThan(0)
    106|     expect(logs[0]!.title, '最新一版应排在最前').toContain('0.2.43')
       |                                         ^
    107|     expect(logs[0]!.content, '要写清用户能感知的变化').toContain('长视频')
    108|     expect(logs[0]!.time, '必须带发布日期').toMatch(/^\d{4}-\d{2}-\d{2}$/)
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯
```

### [绿] cordis 补丁一致性

两层补丁挂同一插件时 id 与共享配置一致（只有桌面独有的键可不同）。

```
CORDIS_PATCHES PASS shared=1
```

### [黄] 启动页进度条

环境不满足（缺 electron 或产品服务起不来），无法执行启动页验收。

```
找不到产品服务入口：C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\lib\types\bin.js
```

### [绿] 性能档位判定

判定边界、上限夹取、环境透传与落盘留痕全部通过。

```
PERF_PROFILE PASS checks=15 fail=0
```

### [黄] 内核握手超时

缺少内核入口产物，无法执行握手超时验收。

```
找不到内核入口：C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\lib\bin-desktop.mjs
```

### [绿] 内核运行时兜底解压

工具阶梯、失败文案与真实解压（含 ENOENT 负向对照）全部通过。

```
KERNEL_EXTRACT PASS checks=22 fail=0
```

### [绿] 抖音真实播放量采集

投稿分析接口的解析、占位 0 与 19 位 id 精度全部锁死。

### [绿] 内核工具可执行

10 个内核业务工具全部真跑一遍：注册齐全、execute 不抛错、render 出文本。仓库构建产物 内核工具验收：14/14 通过；装机运行时 内核工具验收：14/14 通过

```
仓库构建产物 内核工具验收：14/14 通过
装机运行时 内核工具验收：14/14 通过
```

### [红] 专业 E2E 套件

退出码 1：  6 failed。

```
    specs\publish-dialog.spec.ts:32:3 › 发布弹窗 · 唯一宿主 › 账号日历入口打开的是同一个实例（不会多出一份） ──────────────────────
  44 passed (15.3m)
(node:11676) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:30160) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:28596) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:16180) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:23728) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:32336) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
```

### [绿] 中断时序矩阵

五个时间点的停止（含收尾窗口）终态稳定、停晚了如实落 completed。

```
ABORT_MATRIX PASS checks=24 pass=24 fail=0
```

### [绿] S 级前端验收

frontend-supreme-gate.mjs 通过。

```
FRONTEND_SUPREME_GATE PASS files=7 violations=0 missing=0

```

### [绿] 产品版本单源一致

版本权威 desktop/package.json；桌面壳、前端与已出货 dist 一致。

```
product version authority: desktop 0.2.46
registry DisplayName carries 0.2.46
PRODUCT_VERSION_VERIFY PASS
```
