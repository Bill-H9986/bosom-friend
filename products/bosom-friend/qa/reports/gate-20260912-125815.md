# 门禁红绿灯报告

- 时间：2026-09-12T04:58:15.809Z
- 执行方式：--full（含 S 级前端验收）
- Git 提交：de03664acf2056ef58f1f55b0cd8bdee67442a72（工作区有未提交改动，本报告只对这份工作区有效）
- 执行人：Jay
- 主机：DESKTOP-2OHKO6T
- 结论：红灯：禁止合并 / 发布

## 检查结果

### [绿] DSH 内核组合（启动器）

dsh-base 已挂载，未发现 dsh-web-app / dsh-client-*。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] DSH 内核入口（并行新路径）

内核入口组合正确：dsh-base + dsh-bosom-friend-kernel，无 Web UI。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] 禁止直连模型接口

未发现直连 chat/completions。

### [绿] 依赖边界（禁 DSH Web UI 包）

未发现 dsh-web-app / dsh-client-ui 系列依赖。

### [绿] 追溯结构完整性

所有已登记 AC 行结构合规。

### [绿] 功能完整性（未实现清零）

无未实现功能。

### [黄] 验收准则覆盖率

覆盖率 34.4%（覆盖 8 / 部分 15 / 总数 45），发布前必须 100%；未覆盖：AC-001-1、AC-001-2、AC-002-1、AC-002-2、AC-002-3、AC-006-1、AC-006-2、AC-007-1、AC-008-2、AC-008-3。

### [绿] 缺陷闭环留痕

已登记缺陷 35 条，关闭字段合规。

### [绿] 数据编码完整性

落盘数据无编码损坏。

```
DATA_INTEGRITY PASS root=C:\Users\Jay\.bosom-friend\bosom-friend
```

### [绿] 安装包只读保护

5 个回滚安装包与基线一致。

### [绿] 模型配置链路

保存→落盘→DSH 设置→内核路由 全部通过。

```
  [PASS] 顶层 model 镜像当前提供方模型 — agnes-2.5-flash
== 3/5 写入 DSH 设置与凭据文档 ==
  [PASS] settings.yaml 存在 llm-pi-ai 段
  [PASS] settings.yaml 注册了路由 bf-bf-verify
  [PASS] settings.yaml 写入 baseURL
  [PASS] .credentials.yaml 写入密钥引用名
== 4/5 唯一权威：打包主进程不得再持有一份模型配置 ==
  [PASS] 打包主进程文件存在（4 个）
  [PASS] 打包主进程不持有第二份模型配置 — 唯一权威：服务端 llm-user.json → kernel-client 注入 AGNES_API_KEY
== 5/5 内核真的注册了这条路由 ==
  [PASS] 内核接受路由 bf-bf-verify — {"name":"deepseek-harness-sdk-runtime","version":"0.0.1"}
== 已恢复运行前的模型配置 ==
LLM_CONFIG_GATE PASS

```

### [绿] 业务域 CRUD 四步

五个业务域增/查/改/删 + 落盘 + 空态全部通过。

```
CRUD_MATRIX PASS checks=34 pass=34 fail=0
```

### [绿] 知识库蒸馏链路

检索命中、注入留痕、蒸馏样本、JSONL 导出全部通过。

```
KNOWLEDGE_DISTILL PASS checks=12 pass=12 fail=0
```

### [红] AI 智能体任务动作

退出码 1：AGENT_TASK_ACTIONS FAIL checks=24 pass=23 fail=1。

```
FAIL 中断·只保留已送达客户端的部分正文 | 落库 618 字 / 客户端收到 550 字
PASS 继续·同一任务接着跑完 | status=completed 消息=4
PASS 继续·新消息追加在中断之后 | 中断时=2 继续后=4
PASS 任务·评分落库并回读（单层 data） | code=0 rating=5 双层=false
PASS 任务·不存在的任务评分接口如实报错 | code=18100
PASS AC-014-2 评分/收藏刷新后仍保持 | 回读 favorite=true rating=5；落盘 favorite=true rating=5
PASS 中断·探针任务已清理 | code=0
AGENT_TASK_ACTIONS FAIL checks=24 pass=23 fail=1
FAILED: 中断·只保留已送达客户端的部分正文
SKIPPED: 收尾窗口中断：第 1 次停止没赶上生成（已跑完）、收尾窗口中断：第 2 次停止没赶上生成（已跑完）、收尾窗口中断：第 3 次停止没赶上生成（已跑完）、AC-005-2：模型暂时不可用（大模型调用失败：DeepSeek Harness）
```

### [红] 服务端功能测试

退出码 1：      Tests  2 failed | 33 passed (35)。

```
    "wxSph",
    "xhs",
-   "xianyu",
  ]
 ❯ test/functional/content-params.spec.ts:243:38
    241|     const res = await h.call<Array<{ platform: string, channel: boolea…
    242|     const channels = res.data.filter(item => item.channel === true).ma…
    243|     expect(channels, '产品频道白名单只认这几个').toEqual(['KWAI', 'douyin', 'wxSph…
       |                                      ^
    244|
    245|     // 引擎带模块、但产品没作为频道开放的平台必须在列且 channel=false：
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯
```

### [绿] cordis 补丁一致性

两层补丁挂同一插件时 id 与共享配置一致（只有桌面独有的键可不同）。

```
CORDIS_PATCHES PASS shared=1
```

### [红] 专业 E2E 套件

退出码 1：  3 failed。

```
    Usage:
        npx playwright show-trace test-results\publish-dialog-发布弹窗-·-唯一宿主-账号日历入口打开的是同一个实例（不会多出一份）\trace.zip
    ────────────────────────────────────────────────────────────────────────────────────────────────
  3 failed
    specs\content-params.spec.ts:79:3 › 添加频道 · 平台清单 › 快手与闲鱼都可连接，但未接通发布的平台不进内容创作目标平台 ────────────────
    specs\publish-dialog.spec.ts:13:3 › 发布弹窗 · 唯一宿主 › 内容创作「一键发布」打开弹窗，且同一时刻只有一个实例 ───────────────────
    specs\publish-dialog.spec.ts:26:3 › 发布弹窗 · 唯一宿主 › 账号日历入口打开的是同一个实例（不会多出一份） ──────────────────────
  10 passed (5.3m)
(node:35904) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:32992) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:12776) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
```

### [绿] 中断时序矩阵

五个时间点的停止（含收尾窗口）终态稳定、停晚了如实落 completed。

```
ABORT_MATRIX PASS checks=22 pass=22 fail=0
```

### [绿] S 级前端验收

frontend-supreme-gate.mjs 通过。

```
FRONTEND_SUPREME_GATE PASS files=7 violations=0 missing=0

```

### [绿] 产品版本单源一致

版本权威 desktop/package.json；桌面壳、前端与已出货 dist 一致。

```
product version authority: desktop 0.2.41
registry DisplayName carries 0.2.41
PRODUCT_VERSION_VERIFY PASS
```
