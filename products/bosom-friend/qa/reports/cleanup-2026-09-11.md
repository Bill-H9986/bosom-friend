# 项目清理报告 2026-09-11

> 触发原因：C 盘可用空间只剩 14.4 GB，安装包（380~570 MB/个）已无法继续产出。
> 原则：**能归档的不删、能再生的才删**；不删源码、不删验收证据、不删受保护回滚包、不删出包资产。

## 结果

| 指标 | 清理前 | 清理后 |
| --- | --- | --- |
| C 盘可用 | 14.4 GB | **74.05 GB**（释放 59.7 GB） |
| 根目录散落日志 | 104 个 | 1 个（被运行中的服务占用） |
| 历史安装包（仓库根 release/） | 33 个 | 6 个 |
| 桌面端历史构建（desktop/release/） | 41 个 | 2 个 |
| 重复内核副本（vendor-kernel*） | 5 份 | 1 份 |
| untracked now: 1196 ; free 72.83 GB

## 一、删除清单（可再生或对应版本已过期）

| 对象 | 数量 | 释放 | 判定依据 |
| --- | --- | --- | --- |
| `release/*.exe`（除保留项） | 27 个 | ~10 GB | 历史安装包；回滚网另由 `qa/baseline/installer-checksums.json` 保护 |
| `products/bosom-friend/desktop/release/0.2.x` | 39 个构建 | ~41.7 GB | 每版约 1.1 GB 解包产物，可由出包脚本重建 |
| `qa/evidence/duration-matrix` | 1 | 3985 MB | 2026-09-04 时长矩阵截图，对应版本已过期 |
| `qa/evidence/parallel-2026-09-04` | 1 | 1189 MB | 伪造 HOME 的 node_modules 副本，超长路径已导致 git 报 Filename too long |
| `desktop/vendor-kernel`、`vendor-kernel2`、`vendor-kernel3`、`vendor-kernel4` | 4 | 3.03 GB | 同一内核的重复 scratch 副本，出包脚本无引用；保留最新的 vendor-kernel5 |

## 二、归档清单（未删除，已移出仓库）

归档位置：`C:/Users/Jay/Desktop/_bf-archive-2026-09-11/`

- 根目录 104 个 `*.log`（历史运行与门禁日志）
- `.tmp-model-probe.mjs`、`_cookie-check.mjs`、`_fix-uninstall.ps1`（一次性探针）
- `.workbuddy/`（含两个 `git-rescue-2026-09-07.bundle` Git 救援包，**未删除**）

回滚方式：把文件移回原位即可，归档目录结构与原位置一一对应。

## 三、明确保留（禁止清理）

| 对象 | 原因 |
| --- | --- |
| `release/` 中 5 个受保护安装包（0.13.5 / 0.13.6 / 0.13.7 / 0.13.8 / 0.2.0） | 回滚安全网；门禁「安装包只读保护」逐包校验 SHA256，删或改都会标红 |
| `release/BosomFriend-Setup-0.2.31.exe` | 最近一次深度 QA 的测试基线 |
| `products/bosom-friend/desktop/release/0.2.40`、`0.2.39` | 最新两版构建，用于升级覆盖验收 |
| `products/bosom-friend/desktop/dist/`（engine-portable、kernel-runtime-extras） | 出包资产，删除后下次出包需重新准备 |
| `desktop/vendor-kernel5` | 最新的内核 scratch 副本 |
| `qa/evidence/gate/` | RELEASE_GATE.md 30 条硬验收的截图证据 |
| `qa/evidence/` 其余小目录、`qa/reports/`、全部源码与 `node_modules` | 证据与依赖，不是垃圾 |

## 四、遗留

- 根目录 `bf-dev-31280.log` 被运行中的开发服务占用未能归档；停服后把它移到 `C:/Users/Jay/Desktop/_bf-archive-2026-09-11/` 即可。
- 全部 74 个清理项执行完毕，失败项 0（见 `%TEMP%/bf-cleanup-2026-09-11-1554.txt` 逐项记录）。

## 五、验证

- `node --check` 通过（`run-gate.mjs`、`run-gate-triple.mjs`、`verify-product-version.mjs`）。
- 开发版 `http://127.0.0.1:31280/bosom-friend/` 清理期间保持 HTTP 200，未受影响。
- 5 个受保护安装包的存在性与哈希由门禁「安装包只读保护」逐包校验，结论见 `qa/reports/latest.md`。
