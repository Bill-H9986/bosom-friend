# 门禁红绿灯报告

- 时间：2026-09-14T07:40:09.073Z
- 执行方式：快速门禁
- Git 提交：17605e7e2702db8ebdf6e302212e0809d7fc9d67（工作区有未提交改动，本报告只对这份工作区有效）
- 执行人：Jay
- 主机：DESKTOP-2OHKO6T
- 结论：黄灯：可继续开发，发布前必须处理

## 检查结果

### [绿] DSH 内核组合（启动器）

dsh-base 已挂载，未发现 dsh-web-app / dsh-client-*。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] DSH 内核入口（并行新路径）

内核入口组合正确：dsh-base + dsh-bosom-friend-kernel，无 Web UI。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] 禁止直连模型接口

未发现直连 chat/completions。

### [绿] 依赖边界（禁 DSH Web UI 包）

未发现 dsh-web-app / dsh-client-ui 系列依赖。

### [绿] 追溯结构完整性

所有已登记 AC 行结构合规。

### [绿] 功能完整性（未实现清零）

无未实现功能。

### [黄] 验收准则覆盖率

覆盖率 95.8%（覆盖 44 / 部分 4 / 总数 48），发布前必须 100%。

### [绿] 缺陷闭环留痕

已登记缺陷 46 条，关闭字段合规。

### [绿] 数据编码完整性

落盘数据无编码损坏。

```
DATA_INTEGRITY PASS root=C:\Users\Jay\.bosom-friend\bosom-friend
```

### [绿] 安装包只读保护

1 个回滚安装包与基线一致（存放目录 C:/Users/Jay/BosomFriend-Releases）。

### [黄] 模型配置链路

开发版未运行，无法执行端到端检查。

```

[TypeError: fetch failed] {
  [cause]: Error: connect ECONNREFUSED 127.0.0.1:31280
      at TCPConnectWrap.afterConnect [as oncomplete] (node:net:1706:16) {
    errno: -4078,
    code: 'ECONNREFUSED',
    syscall: 'connect',
    address: '127.0.0.1',
    port: 31280
  }
}

Node.js v24.18.0

```

### [黄] 业务域 CRUD 四步

开发版未运行，无法执行四步检查。

```
CRUD_MATRIX SKIP 应用未运行（http://127.0.0.1:31280）：fetch failed
```

### [黄] 知识库蒸馏链路

开发版未运行，无法执行蒸馏链路检查。

```
KNOWLEDGE_DISTILL SKIP 应用未运行（http://127.0.0.1:31280）：fetch failed
```

### [黄] AI 智能体任务动作

开发版未运行或模型不可用，无法执行中断/分享检查。

```
AGENT_TASK_ACTIONS SKIP 应用未运行（http://127.0.0.1:31280）：fetch failed
```

### [绿] 服务端功能测试

路由契约、统计口径、批量删除语义、上传票据、路由表不变量全部通过（进程内，秒级）。

```
      Tests  69 passed (69)
```

### [绿] cordis 补丁一致性

两层补丁挂同一插件时 id 与共享配置一致（只有桌面独有的键可不同）。

```
CORDIS_PATCHES PASS shared=1
```

### [黄] 启动页进度条

环境不满足（缺 electron 或产品服务起不来），无法执行启动页验收。

```
找不到 electron 可执行文件：C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\project\bosom-friend-electron\node_modules\electron\dist\electron.exe
```

### [绿] 性能档位判定

判定边界、上限夹取、环境透传与落盘留痕全部通过。

```
PERF_PROFILE PASS checks=15 fail=0
```

### [黄] 内核握手超时

缺少内核入口产物，无法执行握手超时验收。

```
找不到内核入口：C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\lib\bin-desktop.mjs
```

### [绿] 内核运行时兜底解压

工具阶梯、失败文案与真实解压（含 ENOENT 负向对照）全部通过。

```
KERNEL_EXTRACT PASS checks=22 fail=0
```

### [绿] 抖音真实播放量采集

投稿分析接口的解析、占位 0 与 19 位 id 精度全部锁死。

### [绿] 内核工具可执行

10 个内核业务工具全部真跑一遍：注册齐全、execute 不抛错、render 出文本。仓库构建产物 内核工具验收：14/14 通过；装机运行时 内核工具验收：14/14 通过

```
仓库构建产物 内核工具验收：14/14 通过
装机运行时 内核工具验收：14/14 通过
```

### [黄] 专业 E2E 套件

开发版未运行，无法执行 Playwright 套件。

### [黄] 中断时序矩阵

开发版未运行或模型不可用，无法执行时序矩阵。

```
ABORT_MATRIX SKIP 应用未运行（http://127.0.0.1:31280）：fetch failed
```

### [黄] S 级前端验收

未执行：需要 --full，且应用处于可运行状态。

### [绿] 产品版本单源一致

版本权威 desktop/package.json；桌面壳、前端与已出货 dist 一致。

```
product version authority: desktop 0.2.42
registry DisplayName carries 0.2.42
PRODUCT_VERSION_VERIFY PASS
```
