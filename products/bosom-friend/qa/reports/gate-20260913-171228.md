# 门禁红绿灯报告

- 时间：2026-09-13T09:12:28.826Z
- 执行方式：--full（含 S 级前端验收）
- Git 提交：4ea5cd9fc175f269ddebb27fdc582c4e31ad2d80（工作区有未提交改动，本报告只对这份工作区有效）
- 执行人：Jay
- 主机：DESKTOP-2OHKO6T
- 结论：红灯：禁止合并 / 发布

## 检查结果

### [绿] DSH 内核组合（启动器）

dsh-base 已挂载，未发现 dsh-web-app / dsh-client-*。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] DSH 内核入口（并行新路径）

内核入口组合正确：dsh-base + dsh-bosom-friend-kernel，无 Web UI。

```
C:\Users\Jay\Desktop\Bosom friend APP\products\bosom-friend\launcher\src\bin-kernel.ts
```

### [绿] 禁止直连模型接口

未发现直连 chat/completions。

### [绿] 依赖边界（禁 DSH Web UI 包）

未发现 dsh-web-app / dsh-client-ui 系列依赖。

### [绿] 追溯结构完整性

所有已登记 AC 行结构合规。

### [绿] 功能完整性（未实现清零）

无未实现功能。

### [黄] 验收准则覆盖率

覆盖率 94.8%（覆盖 43 / 部分 5 / 总数 48），发布前必须 100%。

### [绿] 缺陷闭环留痕

已登记缺陷 38 条，关闭字段合规。

### [绿] 数据编码完整性

落盘数据无编码损坏。

```
DATA_INTEGRITY PASS root=C:\Users\Jay\.bosom-friend\bosom-friend
```

### [绿] 安装包只读保护

5 个回滚安装包与基线一致。

### [绿] 模型配置链路

保存→落盘→DSH 设置→内核路由 全部通过。

```
  [PASS] 顶层 model 镜像当前提供方模型 — agnes-2.5-flash
== 3/5 写入 DSH 设置与凭据文档 ==
  [PASS] settings.yaml 存在 llm-pi-ai 段
  [PASS] settings.yaml 注册了路由 bf-bf-verify
  [PASS] settings.yaml 写入 baseURL
  [PASS] .credentials.yaml 写入密钥引用名
== 4/5 唯一权威：打包主进程不得再持有一份模型配置 ==
  [PASS] 打包主进程文件存在（5 个）
  [PASS] 打包主进程不持有第二份模型配置 — 唯一权威：服务端 llm-user.json → kernel-client 注入 AGNES_API_KEY
== 5/5 内核真的注册了这条路由 ==
  [PASS] 内核接受路由 bf-bf-verify — {"name":"deepseek-harness-sdk-runtime","version":"0.0.1"}
== 已恢复运行前的模型配置 ==
LLM_CONFIG_GATE PASS

```

### [绿] 业务域 CRUD 四步

五个业务域增/查/改/删 + 落盘 + 空态全部通过。

```
CRUD_MATRIX PASS checks=34 pass=34 fail=0
```

### [绿] 知识库蒸馏链路

检索命中、注入留痕、蒸馏样本、JSONL 导出全部通过。

```
KNOWLEDGE_DISTILL PASS checks=12 pass=12 fail=0
```

### [绿] AI 智能体任务动作

中断真的停、继续能接上、分享有效期与只读页全部通过。

```
AGENT_TASK_ACTIONS PASS checks=28 pass=28 fail=0
```

### [绿] 服务端功能测试

路由契约、统计口径、批量删除语义、上传票据、路由表不变量全部通过（进程内，秒级）。

```
      Tests  58 passed (58)
```

### [绿] cordis 补丁一致性

两层补丁挂同一插件时 id 与共享配置一致（只有桌面独有的键可不同）。

```
CORDIS_PATCHES PASS shared=1
```

### [绿] 启动页进度条

进度语义（单调不减 / 不确定态不推进 / 阶段推进）与失败可读原因全部通过。

```
SPLASH_PROGRESS PASS checks=14 fail=0
```

### [绿] 性能档位判定

判定边界、上限夹取、环境透传与落盘留痕全部通过。

```
PERF_PROFILE PASS checks=15 fail=0
```

### [绿] 内核握手超时

内核不回应时按预算如实报错，不再无限等待。

```
KERNEL_HANDSHAKE PASS checks=3 fail=0
```

### [绿] 抖音真实播放量采集

投稿分析接口的解析、占位 0 与 19 位 id 精度全部锁死。

### [绿] 内核工具可执行

10 个内核业务工具全部真跑一遍：注册齐全、execute 不抛错、render 出文本。仓库构建产物 内核工具验收：14/14 通过；装机运行时 内核工具验收：14/14 通过

```
仓库构建产物 内核工具验收：14/14 通过
装机运行时 内核工具验收：14/14 通过
```

### [红] 专业 E2E 套件

退出码 null：存在失败用例。

```
  ✓  35 specs\agent-conversation.spec.ts:129:3 › AC-017-2 连续操作回看，截图与操作一一对应无缺漏 › 刷新页面后仍能从会话里回看到带截图的操作记录 (37.2s)
  ✓  36 specs\agent-crud.spec.ts:8:3 › AI 智能体任务 CRUD › 任务：创建 → 读取 → 评分/收藏 → 删除 (15.4s)
  ✓  37 specs\content-crud.spec.ts:10:3 › 内容创作 CRUD › 首屏落在默认素材组，列表可见（DEF-024 不变量） (16.2s)
  ✓  38 specs\content-crud.spec.ts:21:3 › 内容创作 CRUD › 素材组：新增 → 改名 → 删除，列表与服务端一致 (25.6s)
  ✓  39 specs\content-crud.spec.ts:51:3 › 内容创作 CRUD › 素材：上传靶子 → 页面批量删除 → 服务端总数 -1 且不回魂 (22.7s)
  ✓  40 specs\content-params.spec.ts:38:3 › 内容创作 · 分辨率与画面比例 › 图片档位与视频档位一致，成品尺寸随比例切换（门槛 10/11/12） (26.4s)
  ✓  41 specs\content-params.spec.ts:79:3 › 添加频道 · 平台清单 › 添加频道只列产品频道白名单，未接通发布的平台不给入口 (23.1s)
  ✓  42 specs\content-params.spec.ts:120:3 › 设置 · 自定义模型收纳列表 › 模型服务以收纳卡呈现：默认收起，点开就地编辑且只开一个 (23.8s)
  ✓  43 specs\dashboard-honesty.spec.ts:10:3 › 数据中心 · 诚实性可见 › 播放量没采到时，页面给出原因而不是显示成 0 真值 (16.2s)
  ✓  44 specs\llm-config.spec.ts:20:3 › 模型配置 · 单一权威 › 启动不回灌浏览器副本，服务端配置原样不动 (16.1s)
(node:30172) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
(node:13276) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
```

### [绿] 中断时序矩阵

五个时间点的停止（含收尾窗口）终态稳定、停晚了如实落 completed。

```
ABORT_MATRIX PASS checks=24 pass=24 fail=0
```

### [绿] S 级前端验收

frontend-supreme-gate.mjs 通过。

```
FRONTEND_SUPREME_GATE PASS files=7 violations=0 missing=0

```

### [绿] 产品版本单源一致

版本权威 desktop/package.json；桌面壳、前端与已出货 dist 一致；本机未安装态跳过 1 项。

```
product version authority: desktop 0.2.42
PRODUCT_VERSION_VERIFY SKIP 本机注册表已安装版本与开发版本 0.2.42 不同；出厂验收请加 --strict-installed，开发机不算缺陷
PRODUCT_VERSION_VERIFY PASS
```
