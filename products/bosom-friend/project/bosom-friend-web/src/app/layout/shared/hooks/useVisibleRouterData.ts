/**
 * useVisibleRouterData - open-source visible route data
 */
'use client'

import { visibleRouterData } from '@web/app/layout/routerData'

export function useVisibleRouterData() {
  return visibleRouterData
}
